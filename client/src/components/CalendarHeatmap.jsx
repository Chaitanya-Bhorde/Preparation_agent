'use strict';
// Legacy year-grid heatmap REMOVED (Phase heatmap rebuild).
// All heatmaps now use the canonical month-grid ContributionHeatmap
// (client/src/components/ContributionHeatmap.jsx + utils/heatmapDate.js).
// This stub keeps the old import path alive without rendering fake grids.
export default function CalendarHeatmap() {
  return null;
}
