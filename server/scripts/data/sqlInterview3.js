// SQL Interview Questions Part 3 - DML statements and filtering
module.exports = [
  { topic: 'INSERT', question: 'How do you insert many rows efficiently, and what does INSERT ... SELECT give you?', answer: 'Use one multi-row INSERT with a values list, or INSERT INTO ... SELECT to copy from another table or query. Both generate a single statement, which reduces parsing, network round trips and log flushes compared with a loop of single-row inserts. Wrap large loads in a transaction and drop or disable non-essential indexes to speed them up further.', explanation: 'Batching is the key insight interviewers look for.', difficulty: 'medium', followUps: ['How do you upsert rows portably?', 'What is bulk insert versus batch insert?'] },
  { topic: 'UPDATE', question: 'What is the danger of UPDATE without WHERE, and how do you make an update safe?', answer: 'Without a WHERE clause every row is modified, which is a classic production incident. Make updates safe by running the same predicate as a SELECT first to confirm the row count, wrapping the change in a transaction and verifying before COMMIT, and taking a backup or using an audit trigger on critical tables.', explanation: 'Selecting first plus transactions shows production maturity.', difficulty: 'easy', followUps: ['How do you update rows using a value from another table?', 'What is UPDATE ... FROM in PostgreSQL?'] },
  { topic: 'DELETE', question: 'Compare DELETE, TRUNCATE and DROP.', answer: 'DELETE removes selected rows, fires triggers, is fully logged per row and can be rolled back. TRUNCATE deallocates pages quickly, cannot take a WHERE clause, resets identity counters in most engines and is minimally logged. DROP removes the table definition itself together with data, indexes and permissions.', explanation: 'This three-way comparison is a frequent screening question.', difficulty: 'easy', followUps: ['Why is TRUNCATE usually faster?', 'How do foreign keys restrict TRUNCATE?'] },
  { topic: 'SELECT', question: 'What is the logical order of execution of a SELECT statement?', answer: 'FROM and JOINs build the row source, WHERE filters rows, GROUP BY groups them, aggregates are computed, HAVING filters groups, window functions are evaluated, SELECT computes output columns, DISTINCT removes duplicates, ORDER BY sorts and finally LIMIT/OFFSET paginates.', explanation: 'This ordering explains why aliases and aggregates fail in WHERE.', difficulty: 'medium', followUps: ['Why can an aggregate not appear in WHERE?', 'Where do window functions sit relative to HAVING?'] },
  { topic: 'WHERE', question: 'Why can you not use an aggregate or a column alias in WHERE?', answer: 'WHERE is evaluated before grouping and before the SELECT list is computed, so aggregates do not exist yet and aliases are undefined. Use HAVING for aggregate conditions and a derived table or CTE when you need to filter on a computed alias.', explanation: 'Tying the rule to the logical execution order is the strong answer.', difficulty: 'medium', followUps: ['How does a CTE solve the alias problem?', 'Does WHERE or HAVING run first?'] },
  { topic: 'GROUP BY', question: 'What does GROUP BY do, and how does it constrain the SELECT list?', answer: 'GROUP BY collapses rows sharing the same key values into one row per group, and aggregates are computed within each group. Every column in the SELECT list must either appear in the GROUP BY clause or be wrapped in an aggregate, otherwise the query is ambiguous and PostgreSQL rejects it outright.', explanation: 'The rule: everything selected is either grouped or aggregated.', difficulty: 'easy', followUps: ['What happens in MySQL with ONLY_FULL_GROUP_BY disabled?', 'How do you group by a computed expression?'] },
{ topic: 'HAVING', question: 'How do HAVING and WHERE differ, and can they be combined?', answer: 'WHERE filters individual rows before grouping and cannot reference aggregates; HAVING filters groups after aggregation and can use aggregates. Combining them is efficient because WHERE reduces the rows entering the grouping step while HAVING removes groups you do not need in the result.', explanation: 'The performance argument for pushing filters into WHERE is a bonus.', difficulty: 'easy', followUps: ['Is HAVING without GROUP BY legal?', 'How does predicate pushdown help here?'] },
  { topic: 'ORDER BY', question: 'How does ORDER BY affect performance and how do you paginate large result sets?', answer: 'ORDER BY on an indexed column can follow index order and avoid a sort; otherwise the engine performs an expensive filesort. For pagination prefer keyset pagination (WHERE id > last_seen ORDER BY id LIMIT n) over large OFFSET values, because OFFSET still produces and discards every preceding row.', explanation: 'Keyset pagination separates experience from theory.', difficulty: 'hard', followUps: ['Why is OFFSET 100000 slow?', 'How does a covering index help ORDER BY?'] },
  { topic: 'DISTINCT', question: 'When is DISTINCT a code smell, and what usually replaces it?', answer: 'DISTINCT often hides duplicated rows caused by an incorrect join, such as joining a parent to many children and then deduplicating the fan-out. The real fix is to aggregate in a subquery, use EXISTS for existence checks or correct the join granularity. DISTINCT also forces a sort or hash step, so it costs performance.', explanation: 'Diagnosing join fan-out is the skill being tested.', difficulty: 'medium', followUps: ['How do multi-column DISTINCT semantics work?', 'DISTINCT versus GROUP BY here?'] },
  { topic: 'Aggregate Functions', question: 'How do COUNT(*), COUNT(col) and COUNT(DISTINCT col) differ when NULLs are present?', answer: 'COUNT(*) counts rows regardless of NULLs, COUNT(col) counts only rows where col is not NULL, and COUNT(DISTINCT col) counts unique non-NULL values. SUM, AVG, MIN and MAX also ignore NULL, so AVG divides by the number of non-NULL values rather than the row count, which surprises people who expect NULL to behave like zero.', explanation: 'The AVG denominator subtlety is a favourite trap question.', difficulty: 'medium', followUps: ['How do you average treating NULL as zero?', 'Which aggregates can be parallelised?'] },
];