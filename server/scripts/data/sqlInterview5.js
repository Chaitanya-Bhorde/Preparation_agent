// SQL Interview Questions Part 5 - NULL handling, indexes, views and transactions
module.exports = [
  { topic: 'NOT NULL', question: 'What does NOT NULL actually guarantee, and what are the three-valued logic implications?', answer: 'NOT NULL only forbids the special value NULL; empty strings, zero and dates in the past are still valid. SQL uses three-valued logic, so NULL propagates through comparisons: expressions such as col = NULL and col <> 5 both evaluate to UNKNOWN and filter the row out. That is why NOT IN with a NULL in the list returns no rows and why IS NULL must be used instead of equality.', explanation: 'Three-valued logic is the concept behind most NULL surprises.', difficulty: 'medium', followUps: ['How does COALESCE help?', 'Why is NULL <> NULL not true?'] },
  { topic: 'CHECK', question: 'What can a CHECK constraint validate and what are its limits?', answer: 'CHECK validates a boolean predicate per row, such as age >= 18 or salary > 0, and can reference several columns of the same row. It cannot reference other tables or contain subqueries, so cross-table rules need foreign keys, triggers or application logic. Note that some engines historically parsed CHECK without enforcing it, so verify behaviour on your version.', explanation: 'Explaining the no-subquery limit shows precise knowledge.', difficulty: 'medium', followUps: ['How do you enforce a cross-table rule?', 'Can a CHECK be added to an existing table?'] },
  { topic: 'Indexes', question: 'Explain clustered versus non-clustered indexes and when an index will not be used.', answer: 'A clustered index defines the physical row order and therefore exists once per table, while non-clustered indexes are separate structures whose leaves hold the key plus a pointer or the full row. The optimiser skips an index when the predicate has low selectivity, the column is wrapped in a function, an implicit conversion changes the type, statistics are stale or the query needs most of the table anyway, so a scan is cheaper.', explanation: 'Listing the selectivity and sargability reasons is the expected depth.', difficulty: 'hard', followUps: ['What is a covering index?', 'How do composite index column order rules work?'] },
  { topic: 'Views', question: 'Why use views, and when should you choose a materialized view?', answer: 'Views encapsulate complex joins and rename or restrict columns, giving a stable interface that can stay unchanged while base tables are refactored, and they can restrict rows for security. Choose a materialized view when the query is expensive and slightly stale data is acceptable, because results are stored physically and then refreshed periodically, accepting the trade-off of refresh cost and staleness.', explanation: 'The staleness trade-off is the point being probed.', difficulty: 'medium', followUps: ['When can a view be updatable?', 'How does a refresh strategy affect read latency?'] },
  { topic: 'Stored Procedures', question: 'What are the advantages and disadvantages of putting business logic in stored procedures?', answer: 'Advantages: fewer network round trips, precompiled plans, reuse across many clients and a central place to enforce rules close to the data. Disadvantages: vendor-specific syntax that locks you in, harder version control and code review, harder unit testing and scaling limits because the work runs on the database server, and debugging is awkward compared with application code.', explanation: 'A balanced answer with concrete operational costs scores best.', difficulty: 'hard', followUps: ['How do you version database code?', 'When is logic better in the application tier?'] },
];