// OS Interview Questions Part 2 - scheduling, synchronization and deadlock topics
module.exports = [
  { topic: 'FCFS', question: 'Show with an example how the convoy effect hurts FCFS, and what you would do about it.', answer: 'Take P1 with burst 20 ms arriving first and P2, P3 with bursts 2 ms each arriving right after. FCFS runs P1 first, so waiting times are 0, 20 and 22 ms and average waiting time is 14 ms. If the short jobs ran first the average would be about 2.7 ms. The fix is to allow preemption (SRTF or Round Robin) so short interactive jobs are not blocked behind a long CPU-bound job.', explanation: 'Always compute average waiting time for both orders - the numbers make the argument.', difficulty: 'medium', followUps: ['Is FCFS starvation-free?', 'What is the response time under FCFS?'] },
  { topic: 'SJF', question: 'SJF is provably optimal for average waiting time - state the proof intuition and its practical problem.', answer: 'Proof intuition: at every scheduling decision, running the shortest remaining job first minimises the number of jobs that wait behind it, so the total waiting time cannot be reduced by swapping a longer job ahead of a shorter one (an exchange argument). The practical problem is that burst times are not known in advance; real systems estimate them with exponential averaging of past bursts, which only approximates optimality and can starve long jobs.', explanation: 'Interviewers want the exchange-argument idea plus the burst-estimation caveat.', difficulty: 'hard', followUps: ['How is the next burst estimated by exponential averaging?', 'Does SJF remove starvation?'] },
  { topic: 'SRTF', question: 'How does SRTF differ from SJF and why is its context switching cost higher?', answer: 'SRTF is the preemptive version of SJF: whenever a new process arrives with a shorter remaining burst than the running process, the CPU is preempted immediately. This gives lower average waiting time than SJF but causes more context switches because every short arrival can interrupt the current process, and each switch wastes CPU time saving and restoring PCB state, plus cache and TLB pollution.', explanation: 'Answer in terms of remaining burst time and switch overhead.', difficulty: 'medium', followUps: ['Does SRTF guarantee optimal average waiting time?', 'How would you set the time quantum if you had to approximate it?'] },
  { topic: 'Round Robin', question: 'How do you choose the time quantum in Round Robin, and what happens at the extremes?', answer: 'The quantum should be slightly larger than a typical CPU burst so most processes finish within one quantum, giving response time close to q while keeping switching overhead small. If q is very large Round Robin degenerates into FCFS with poor response time; if q is very small the system spends most of its time context switching and throughput collapses. A common rule of thumb is 10-100 ms, with overhead kept under about 1% of the quantum.', explanation: 'Mention that quantum choice is a response-time versus throughput trade-off.', difficulty: 'hard', followUps: ['Is Round Robin starvation-free?', 'Why does Round Robin behave better for interactive workloads?'] },
  { topic: 'Priority Scheduling', question: 'Explain how aging solves starvation in priority scheduling.', answer: 'With static priorities a low-priority process can wait indefinitely because higher-priority processes keep arriving, which is starvation (indefinite blocking). Aging increases the priority of a waiting process gradually, for example by one level every fixed interval, so a process that waits long enough eventually reaches the highest priority and gets the CPU. The cost is that the scheduling decision is no longer a pure priority rule and needs per-process bookkeeping.', explanation: 'Name starvation as the problem and aging as the classical fix.', difficulty: 'medium', followUps: ['How does priority inversion relate to this?', 'What is priority inheritance?'] },
  { topic: 'Synchronization', question: 'Why is a race condition possible even on a single-core machine, and how does the OS prevent it?', answer: 'A race condition occurs when the correctness of the result depends on the interleaving of threads, and even on one core a timer interrupt can preempt a thread halfway through a read-modify-write sequence such as count++. The OS prevents it with atomic primitives: disabling interrupts in short kernel critical sections, test-and-set or compare-and-swap instructions, spin locks and semaphores for longer sections, and monitors or mutexes for user-level synchronisation.', explanation: 'The count++ example plus a named atomic primitive is the expected answer.', difficulty: 'medium', followUps: ['What is the difference between a spin lock and a mutex?', 'Why are interrupts disabled only briefly?'] },
  { topic: 'Critical Section', question: 'List the three requirements of a correct critical-section solution and say how Peterson\'s algorithm satisfies them.', answer: 'The requirements are mutual exclusion (at most one process inside), progress (a process outside cannot block others from entering forever) and bounded waiting (a bound exists on how long a process waits). Peterson\'s algorithm uses the flag[] array to express intent and the turn variable to break ties, which provides mutual exclusion and ensures that a waiting process cannot be overtaken indefinitely; it needs only shared memory and works for two processes.', explanation: 'Naming all three requirements before explaining is what gets full marks.', difficulty: 'medium', followUps: ['Why does Peterson\'s algorithm fail with out-of-order execution on modern CPUs?', 'What is the bakery algorithm?'] },
  { topic: 'Mutex', question: 'Compare a mutex with a binary semaphore and explain when you would choose each.', answer: 'Both allow one holder at a time, but a mutex expresses ownership: only the thread that locked it may unlock it, which enables recursion detection, priority inheritance and debugging of misuse. A binary semaphore has no ownership concept and is a signalling primitive, so one thread can wait and a different thread can post it. Use a mutex for protecting a shared resource and a semaphore (or condition variable) for ordering or signalling between threads.', explanation: 'The ownership distinction is the crux of the answer.', difficulty: 'medium', followUps: ['What is a recursive mutex?', 'Why can a mutex priority-inherit but a semaphore cannot?'] },
  { topic: 'Semaphore', question: 'Explain the wait and signal operations and how a counting semaphore bounds concurrency.', answer: 'A semaphore S is an integer with two atomic operations: wait(S) (P) decrements S and blocks while S is negative, and signal(S) (V) increments S and wakes a blocked process if any. Initialising a counting semaphore to N allows at most N processes into the critical section at once, which makes it ideal for pooling resources such as database connections or buffer slots, while a binary semaphore initialised to 1 provides mutual exclusion.', explanation: 'Say the two operations are atomic, otherwise the semaphore itself races.', difficulty: 'medium', followUps: ['What does a negative semaphore value mean?', 'How would you implement the producer-consumer problem with semaphores?'] },
  { topic: 'Deadlock', question: 'State the four necessary conditions for deadlock and give a real system scenario for each.', answer: 'Mutual exclusion: a database row is locked by one transaction only. Hold and wait: a thread keeps its file lock while requesting a second file. No preemption: the OS cannot forcibly take a mutex from a thread. Circular wait: thread A waits for the lock held by B while B waits for the lock held by A. All four must hold simultaneously, so breaking any single condition prevents deadlock.', explanation: 'Give a concrete example per condition so the answer is not recited from a book.', difficulty: 'medium', followUps: ['Which condition is easiest to break in practice?', 'How does a timeout break deadlock?'] },
  { topic: 'Deadlock Prevention', question: 'How do you prevent deadlock by attacking each of the four necessary conditions?', answer: 'Mutual exclusion: make the resource shareable or use lock-free structures (hardest to remove). Hold and wait: require all resources to be requested up front or release everything before requesting more. No preemption: allow the OS to preempt and roll back a process holding resources. Circular wait: impose a global order on resources and always acquire locks in that order, which is the most practical production technique.', explanation: 'Rank the techniques: circular-wait ordering and hold-and-wait removal are the realistic ones.', difficulty: 'hard', followUps: ['Why does requesting all resources up front reduce concurrency?', 'How does a two-phase commit borrow this idea?'] },
  { topic: 'Deadlock Avoidance', question: 'How does deadlock avoidance differ from prevention, and what does the safe sequence represent?', answer: 'Prevention constrains how resources may be requested so deadlock is structurally impossible, while avoidance allows general requests but refuses any that would leave the system in an unsafe state, decision by decision. A safe sequence is an ordering of processes such that each can obtain its remaining maximum claim using the currently available plus released resources; if such an order exists the state is safe. The cost is that avoidance needs the maximum claim of every process in advance and runs an expensive safety check per request.', explanation: 'The safe-sequence definition is the key sentence.', difficulty: 'hard', followUps: ['What does unsafe mean - is it deadlock?', 'Why is avoidance rarely used in general-purpose OSes?'] },
  { topic: "Banker's Algorithm", question: 'Explain how the Banker\'s algorithm decides whether to grant a request.', answer: 'It maintains Available, Max, Allocation and Need matrices. On a request from Pi it first checks that the request does not exceed Need and Available, then tentatively allocates it and runs the safety algorithm: simulate repeated passes where a process whose Need is within Available finishes and releases its Allocation, building a safe sequence. If every process can be completed the allocation is granted, otherwise it is rolled back and Pi waits.', explanation: 'Mention the O(n^2 m) cost of the safety check to show understanding of its overhead.', difficulty: 'hard', followUps: ['Compute a safe sequence for a small example.', 'What happens if two processes request simultaneously?'] },
  { topic: 'Memory Management', question: 'Compare contiguous allocation with paging, and explain why fragmentation drives the choice.', answer: 'Contiguous allocation gives each process one block, so logical addresses are simple base-plus-offset but external fragmentation grows as holes appear between allocated blocks, and compaction is expensive. Paging divides physical memory into fixed frames and processes into equal pages, so internal fragmentation is limited to at most one partial page per allocation and there is no external fragmentation at all. The cost is page-table memory and an extra memory reference per access, usually removed by a TLB.', explanation: 'Name internal versus external fragmentation explicitly.', difficulty: 'medium', followUps: ['What is the difference between internal and external fragmentation?', 'Why does segmentation suffer external fragmentation?'] },
];