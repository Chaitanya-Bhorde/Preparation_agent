// SQL Interview Questions Part 4 - joins, set operators and constraints
module.exports = [
  { topic: 'Joins', question: 'Explain the difference between INNER, LEFT, RIGHT, FULL and CROSS joins with an example.', answer: 'INNER JOIN returns only matching pairs; LEFT JOIN keeps every row from the left table and fills NULL where the right side has no match; RIGHT JOIN is the mirror image; FULL OUTER JOIN keeps unmatched rows from both sides; CROSS JOIN produces the Cartesian product of both inputs and is only useful with a deliberate filter or for generating combinations.', explanation: 'Interviewers often follow up with a query asking for unmatched rows.', difficulty: 'easy', followUps: ['How do you find rows with no children?', 'What does a join on a non-unique column do to row counts?'] },
  { topic: 'Joins', question: 'Why does adding a WHERE condition on the right table silently turn a LEFT JOIN into an INNER JOIN?', answer: 'The outer join first produces NULL-extended rows for unmatched left rows, and the WHERE clause is applied afterwards, so any predicate that rejects NULL (for example r.status = "active") discards those rows. To keep the semantics, put the condition in the ON clause or explicitly allow NULL in the WHERE clause.', explanation: 'This is the single most common outer-join bug in production SQL.', difficulty: 'hard', followUps: ['How does moving the predicate to ON change results?', 'When would you use a NOT EXISTS instead?'] },
  { topic: 'UNION ALL', question: 'When should you use UNION ALL instead of UNION?', answer: 'UNION ALL concatenates result sets and keeps duplicates, while UNION additionally performs a duplicate elimination step that requires a sort or hash, adding cost. Use UNION ALL when the inputs are known to be disjoint or duplicates are acceptable, and reserve UNION for when deduplication is genuinely part of the requirement.', explanation: 'Interviewers want the performance reasoning, not just the definition.', difficulty: 'medium', followUps: ['How do you deduplicate two UNION ALL inputs yourself?', 'What are the column-count and type rules?'] },
  { topic: 'IN', question: 'What are the pitfalls of using IN with a subquery versus EXISTS?', answer: 'IN compares each outer value against the subquery result and returns UNKNOWN when the list contains NULL, which can silently exclude rows. EXISTS short-circuits as soon as a match is found and is generally better for correlated existence checks. Also avoid huge literal IN lists, because they defeat index statistics and bloat the plan.', explanation: 'The NULL trap in NOT IN is the classic follow-up question.', difficulty: 'medium', followUps: ['Why is NOT IN dangerous with NULLs?', 'How does IN behave with a multi-column list?'] },
  { topic: 'BETWEEN', question: 'What are the traps of BETWEEN, especially with dates?', answer: 'BETWEEN is inclusive on both ends, so BETWEEN 1 AND 10 includes both bounds. For timestamps BETWEEN DATE AND DATE includes midnight of the end day only, silently dropping the rest of that day; the fix is a half-open range (>= start AND < next_day) or casting to a date. BETWEEN also reverses silently if the lower bound is greater than the upper bound.', explanation: 'The date and time inclusivity trap is what interviewers are testing.', difficulty: 'medium', followUps: ['How do you query a full day of timestamp data?', 'Does BETWEEN use an index on the column?'] },
  { topic: 'LIKE', question: 'How does LIKE interact with indexes and how do you implement case-insensitive searches?', answer: 'A pattern with a literal prefix ("abc%") can use a B-tree index range scan, but a leading wildcard ("%abc") cannot, so it degenerates into a scan. For case-insensitive search, normalise the data (store lower case) or use a case-insensitive collation, and remember that wrapping the column in LOWER() disables index usage unless a functional index exists.', explanation: 'Mentioning functional indexes shows deeper optimizer knowledge.', difficulty: 'medium', followUps: ['What is a trigram or full-text index for?', 'How does collation affect equality and LIKE?'] },
{ topic: 'CASE', question: 'How do you pivot rows into columns and implement conditional logic with CASE?', answer: 'Conditional aggregation performs the pivot: SUM(CASE WHEN month = 1 THEN amount ELSE 0 END) AS jan, repeated per column, grouped by the entity. CASE also implements if-then-else logic inside any expression, and a searched CASE with several WHEN clauses is evaluated top to bottom, so ordering the clauses matters. Always add an ELSE to avoid NULL results.', explanation: 'The pivot-by-conditional-aggregation pattern appears in most analytics interviews.', difficulty: 'medium', followUps: ['How do you pivot when columns are dynamic?', 'What is the difference between simple and searched CASE?'] },
  { topic: 'Constraints', question: 'What types of constraints exist and why are they better than application-only validation?', answer: 'Constraints include PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, CHECK and DEFAULT. They enforce rules for every writer, including other services, scripts and manual queries, so they protect integrity even when application code has bugs. Application validation still matters for user-friendly errors and for rules the database cannot express.', explanation: 'The defence-in-depth framing is the expected answer.', difficulty: 'easy', followUps: ['When would you disable a constraint?', 'How do you enforce a rule across tables?'] },
  { topic: 'Primary Key', question: 'What makes a good primary key, and what is the trade-off between surrogate and natural keys?', answer: 'A good primary key is unique, NOT NULL, stable (never updated) and narrow. Surrogate keys such as an auto-increment integer or sequence are stable and compact and keep index pages dense; natural keys carry business meaning but can change and are often wider. In clustered storage such as InnoDB the primary key is the physical order, so a random UUID harms insert locality.', explanation: 'Talking about clustered index locality demonstrates real database knowledge.', difficulty: 'medium', followUps: ['Why do random UUIDs fragment InnoDB?', 'What is a composite primary key?'] },
  { topic: 'Foreign Key', question: 'What do ON DELETE CASCADE and ON DELETE RESTRICT do, and what are the risks?', answer: 'CASCADE automatically deletes dependent child rows when the parent is removed, keeping referential integrity with minimal application code, but a single accidental parent delete can wipe out large volumes of data. RESTRICT or NO ACTION blocks the delete while children exist, forcing the application to decide explicitly. SET NULL keeps the child but loses the association, which requires nullable columns.', explanation: 'Interviewers like candidates who weigh convenience against blast radius.', difficulty: 'medium', followUps: ['How do you soft-delete instead?', 'Do foreign keys require an index on the child column?'] },
];