// Python Notes - covers every Python topic seeded in TOPICS.python
module.exports = [
  { topic: 'Python Introduction', title: 'What is Python?', content: 'Python is a high-level, dynamically typed, interpreted language created by Guido van Rossum in 1991. It emphasises readability with indentation-based blocks and ships a large standard library, which makes it the default choice for scripting, data work, automation and backend APIs.', keyPoints: ['Interpreted bytecode executed by the CPython virtual machine', 'Dynamically and strongly typed', 'Indentation defines blocks instead of braces', 'Huge standard library plus PyPI ecosystem'], interviewTips: ['Explain "dynamically typed but strongly typed" - no implicit type coercion', 'Mention Python is used for AI/ML, data engineering and backend (Django, FastAPI)'] },
  { topic: 'Data Types', title: 'Python Data Types', content: 'Core built-in types are int (arbitrary precision), float, complex, bool, str, bytes, list, tuple, set, frozenset, dict and NoneType. Mutable types (list, dict, set) can change in place; immutable types (int, str, tuple, frozenset) cannot, which makes them hashable and safe as dictionary keys.', keyPoints: ['Everything is an object, including functions and classes', 'Mutable: list, dict, set, bytearray', 'Immutable: int, float, str, tuple, frozenset', 'type() returns the class, isinstance() checks inheritance-aware type'], interviewTips: ['Say that immutability is a precondition for hashability', 'Mention NoneType and that None is a singleton compared with is'] },
  { topic: 'Variables and Scope', title: 'Variables, Names and Scope (LEGB)', content: 'A Python variable is a name bound to an object, not a typed storage box, so the same name can be rebound to another type. Name lookup follows the LEGB rule: Local, Enclosing, Global, Built-in. The global and nonlocal keywords allow rebinding outer names.', keyPoints: ['Assignment binds a name to an object reference', 'LEGB order: Local -> Enclosing -> Global -> Built-in', 'global rebinds a module-level name; nonlocal rebinds an enclosing function name', 'Default mutable arguments are evaluated once - a classic bug'], interviewTips: ['Demonstrate the mutable default argument trap explicitly', 'Explain that functions are objects, so closures capture names, not values'] },
  { topic: 'Operators', title: 'Python Operators', content: 'Python provides arithmetic (+ - * / // % **), comparison, logical (and or not), bitwise (& | ^ ~ << >>), membership (in, not in), identity (is, is not), assignment and the conditional expression a if cond else b. The walrus operator := assigns inside an expression.', keyPoints: ['/ is true division, // is floor division, ** is exponent', 'is compares identity; == compares value', 'and/or return one of the operands, not a boolean', 'Chained comparison: 0 <= x < 10 is valid Python'], interviewTips: ['Explain why you should use is only with None, True and False', 'Mention operator overloading through dunder methods'] },
  { topic: 'Control Flow', title: 'Control Flow in Python', content: 'Python supports if/elif/else, for over any iterable, while, break, continue, else on loops (runs when no break occurred) and the match/case statement (Python 3.10+) for structural pattern matching.', keyPoints: ['for iterates over iterables, not C-style counters', 'Loop else runs only if the loop was not broken', 'range(start, stop, step) produces integers lazily', 'match/case supports patterns, guards and capture'], interviewTips: ['Use enumerate instead of manual index counters', 'Mention that else-on-loop is uncommon but genuinely useful for search loops'] },
  { topic: 'Lists', title: 'Python Lists', content: 'A list is an ordered, mutable, heterogeneous sequence backed by a dynamic array. append and pop at the end are amortised O(1), indexing is O(1) and insert/delete in the middle is O(n). Lists support slicing, which returns a shallow copy.', keyPoints: ['Mutable, ordered, allows duplicates', 'append O(1), insert(0, x) O(n)', 'Slicing creates a new list; lst[:] is a shallow copy', 'sort() mutates in place, sorted() returns a new list'], interviewTips: ['Warn about aliasing: b = a does not copy', 'Mention list comprehensions as the idiomatic construction tool'] },
  { topic: 'Tuples', title: 'Python Tuples and Namedtuples', content: 'A tuple is an ordered, immutable sequence, so it is hashable (if its contents are) and can be a dictionary key. Tuples are used for fixed records, multiple return values and packing/unpacking, and collections.namedtuple adds field names for readability.', keyPoints: ['Immutable and hashable, unlike list', 'Unpacking: a, b = (1, 2)', 'Tuple of one element needs a trailing comma', 'Namedtuples give attribute access and remain memory efficient'], interviewTips: ['Explain why immutability enables dictionary keys and set membership', 'Mention that tuple assignment is the idiomatic Python swap'] },
  { topic: 'Dictionaries', title: 'Python Dictionaries', content: 'A dict maps hashable keys to values with average O(1) lookup, backed by a hash table. Since Python 3.7 insertion order is guaranteed. dict implements the Map abstraction and is the backbone of Python objects (instance attributes are stored in a dict).', keyPoints: ['Average O(1) get/set/delete, O(n) worst case', 'Insertion order preserved from Python 3.7', 'Keys must be hashable; values can be anything', 'setdefault, get and defaultdict simplify missing-key logic'], interviewTips: ['Mention Counter and defaultdict as interview-friendly helpers', 'Explain that dict keys need __hash__ and __eq__ consistency'] },
  { topic: 'Sets', title: 'Python Sets and Frozensets', content: 'A set is an unordered collection of unique hashable elements with average O(1) membership testing. frozenset is its immutable, hashable counterpart. Sets support union, intersection, difference and symmetric_difference with both operators and methods.', keyPoints: ['No duplicates, no indexing, fast membership tests', 'union |, intersection &, difference -, symmetric_difference ^', 'set removes duplicates from an iterable in one line', 'frozenset can be a dict key or an element of another set'], interviewTips: ['Use sets to turn an O(n^2) duplicate check into O(n)', 'Note that sets are unordered, so iteration order is not guaranteed'] },
  { topic: 'Strings', title: 'Python Strings and Formatting', content: 'str is an immutable sequence of Unicode code points. Operations return new strings, and since indexing is O(1), slicing costs O(k) for k characters copied. f-strings (Python 3.6+) are the modern, fast formatting mechanism.', keyPoints: ['Immutable, Unicode, sequence of code points', 'Useful methods: split, join, strip, replace, startswith, find', 'f-strings embed expressions: f"{name}: {score:.2f}"', 'join is far faster than repeated += in a loop'], interviewTips: ['Explain why strings are immutable (hashing, interning, thread safety)', 'Mention that multiline strings use triple quotes'] },
  { topic: 'Functions', title: 'Functions, Arguments and Closures', content: 'Functions are first-class objects defined with def, supporting positional, keyword, default, *args and **kwargs parameters. Nested functions create closures that capture enclosing variables, and functools.partial and functools.reduce are common functional helpers.', keyPoints: ['def defines, lambda creates small anonymous functions', '*args collects a tuple, **kwargs collects a dict', 'Keyword-only arguments come after *', 'Closures capture variables, and the value is read at call time'], interviewTips: ['Mention that Python has no method overloading by signature', 'Explain the late-binding closure trap inside loops'] },
  { topic: 'Lambda and Map Filter', title: 'Lambda, map, filter and reduce', content: 'lambda creates a single-expression anonymous function. map applies a function to each element, filter keeps elements where the predicate is true, and functools.reduce folds a sequence into one value. Both map and filter are lazy iterators in Python 3.', keyPoints: ['lambda body must be a single expression', 'map and filter return iterators, not lists', 'reduce moved to functools in Python 3', 'Comprehensions are usually more readable than map/filter'], interviewTips: ['Show the same result with map/filter and with a comprehension', 'Mention that map with multiple iterables stops at the shortest'] },
  { topic: 'Comprehensions', title: 'List, Dict and Set Comprehensions', content: 'Comprehensions build a collection from an iterable in one expression: [x*x for x in nums if x > 0] for lists, {k: v for ...} for dicts and {x for ...} for sets. Generator expressions use parentheses and stream values lazily without building a list.', keyPoints: ['Faster and clearer than manual append loops', 'Conditions filter, the leading expression transforms', 'Nested comprehensions flatten nested loops', 'Generator expressions avoid materialising large results'], interviewTips: ['Mention that a comprehension has its own scope in Python 3', 'Warn that deeply nested comprehensions hurt readability'] },
  { topic: 'Modules and Packages', title: 'Modules, Packages and Imports', content: 'A module is a .py file, a package is a directory of modules with an __init__.py (namespace packages may omit it). Imports bind names, and the import system uses sys.path plus the module cache sys.modules. if __name__ == "__main__" guards executable scripts.', keyPoints: ['import module, from module import name, import as alias', 'sys.path determines where Python looks for modules', '__init__.py runs when the package is imported', 'Circular imports are a common design smell'], interviewTips: ['Explain absolute versus relative imports', 'Mention virtual environments to isolate dependencies'] },
  { topic: 'Exception Handling', title: 'Python Exception Handling', content: 'try/except/else/finally handles errors. except catches specific exception classes, else runs when no exception occurred and finally always runs for cleanup. Custom exceptions subclass Exception, and raise ... from err chains the original cause.', keyPoints: ['Catch specific exceptions, never a bare except', 'finally is for cleanup; use with for context managers', 'raise from preserves the cause chain', 'EAFP (ask forgiveness) is idiomatic versus LBYL'], interviewTips: ['Explain why bare except hides real bugs', 'Mention context managers and __exit__ for deterministic cleanup'] },
  { topic: 'File Handling', title: 'File Handling and Context Managers', content: 'open() returns a file object with a mode (r, w, a, rb, wb) and encoding. The with statement closes the file deterministically even on exception. pathlib.Path offers an object-oriented, platform-independent alternative to os.path string manipulation.', keyPoints: ['with open(...) as f guarantees close()', 'Text mode decodes using an encoding; binary mode returns bytes', 'for line in f streams a file line by line without loading it all', 'pathlib.Path supports read_text, write_text and glob'], interviewTips: ['Always specify encoding="utf-8" for portability', 'Mention that a/append never truncates while w truncates'] },
  { topic: 'OOP in Python', title: 'Object-Oriented Programming in Python', content: 'Classes are created with class and instances with __init__. Python supports inheritance including multiple inheritance, encapsulation by convention with a leading underscore, and polymorphism through duck typing. Dunder methods such as __str__, __repr__, __eq__ and __len__ integrate objects with built-in syntax.', keyPoints: ['self is an explicit first parameter', 'Name mangling with __private hides attributes from outside', 'super() follows the MRO for cooperative multiple inheritance', '@property provides computed and validated attributes'], interviewTips: ['Explain duck typing: behaviour matters, not the declared type', 'Compare @classmethod, @staticmethod and instance methods'] },
  { topic: 'Inheritance', title: 'Inheritance and the MRO', content: 'A subclass extends a base class with class Child(Parent). Multiple inheritance is resolved by the C3 linearisation algorithm, exposed as Class.__mro__. super() delegates to the next class in that order, which makes cooperative multiple inheritance possible without the diamond ambiguity.', keyPoints: ['isinstance is inheritance aware; type() compares the exact class', 'MRO uses C3 linearisation and avoids diamond ambiguity', 'super() means "next class in the MRO", not "the parent class"', 'Abstract base classes (ABC) enforce contracts at instantiation'], interviewTips: ['Print Class.__mro__ to demonstrate resolution order', 'Prefer composition when the relationship is has-a'] },
  { topic: 'Iterators and Generators', title: 'Iterators, Generators and yield', content: 'An iterator implements __iter__ and __next__ and raises StopIteration when exhausted. A generator is a function containing yield that returns a lazy iterator, pausing and resuming while preserving local state, which enables memory-efficient pipelines over large or infinite data.', keyPoints: ['for loops call iter() then next() until StopIteration', 'yield suspends the function and keeps its frame alive', 'Generator expressions are the inline form: (x*x for x in data)', 'yield from delegates iteration to another iterable'], interviewTips: ['Use a generator to process a huge log file without loading it', 'Mention send() for coroutine-style two-way generators'] },
  { topic: 'Decorators', title: 'Decorators and functools.wraps', content: 'A decorator is a callable that takes a function and returns a wrapper, applied with @decorator syntax. Decorators add logging, timing, caching, authentication or retries without touching the function body. functools.wraps preserves the original name and docstring, and parameterised decorators need an extra factory layer.', keyPoints: ['A decorator is sugar for func = decorator(func)', 'functools.wraps copies __name__, __doc__ and annotations', 'Class-based decorators implement __call__', 'Parameterised decorators return a decorator from a decorator'], interviewTips: ['Be ready to write a timer decorator on the spot', 'Mention functools.lru_cache as ready-made memoisation'] },
  { topic: 'Context Managers', title: 'Context Managers', content: 'A context manager implements __enter__ and __exit__ and is used with the with statement to guarantee setup and teardown. contextlib.contextmanager converts a generator function into a context manager, and contextlib.suppress ignores listed exceptions.', keyPoints: ['__exit__ receives exception type, value and traceback', 'Returning True from __exit__ suppresses the exception', 'with manages files, locks, DB sessions and transactions', 'Several managers can be combined in one with statement'], interviewTips: ['Show a locked-resource example to explain deterministic cleanup', 'Mention that Python 3.10 supports parenthesised context managers'] },
  { topic: 'Multithreading', title: 'Threading and the GIL', content: 'The Global Interpreter Lock lets only one thread execute Python bytecode at a time in CPython, so threads provide concurrency but not parallelism for CPU-bound work. Threads still help IO-bound work because the GIL is released during blocking I/O. threading offers Thread, Lock, RLock, Event, Queue and ThreadPoolExecutor.', keyPoints: ['The GIL serialises CPU-bound bytecode execution', 'IO-bound tasks benefit because the GIL is released while waiting', 'Use multiprocessing or native extensions for CPU parallelism', 'Locks prevent races on shared mutable state'], interviewTips: ['State the rule: threads for IO, processes for CPU', 'Note that races still exist in Python when locks are omitted'] },
  { topic: 'Multiprocessing', title: 'Multiprocessing and Concurrency Choices', content: 'multiprocessing creates separate interpreter processes with their own memory and GIL, enabling true CPU parallelism. Processes communicate through pipes, queues or shared memory with higher start-up and marshalling overhead than threads. asyncio is the third option, giving single-threaded cooperative concurrency for high-volume IO.', keyPoints: ['Separate memory avoids races but requires explicit IPC', 'multiprocessing.Pool maps work across CPU cores', 'asyncio suits thousands of concurrent network calls', 'Pickling overhead limits how much data can be passed'], interviewTips: ['Decision rule: CPU -> processes, blocking IO -> threads, many sockets -> asyncio', 'On Windows the spawn start method requires a guarded __main__'] },
  { topic: 'JSON and Serialization', title: 'JSON and Serialisation', content: 'The json module converts Python objects to JSON text with json.dumps and parses with json.loads, supporting dict, list, str, int, float, bool and None. pickle serialises arbitrary Python objects but is unsafe for untrusted input because unpickling can execute arbitrary code.', keyPoints: ['json is language neutral; pickle is Python specific', 'Pickle on untrusted data is a remote code execution risk', 'csv and sqlite3 handle tabular data in the standard library', 'Custom objects need a default= encoder for json'], interviewTips: ['Say JSON for APIs, pickle only for trusted internal caches', 'Mention dataclasses.asdict as a simple serialisation helper'] },
];