// SQL Notes part 2 - categories, DDL/DML statements (topics missing from sqlNotes.js)
module.exports = [
  { topic: 'DDL', title: 'Data Definition Language', content: 'DDL defines and modifies the structure of database objects: CREATE, ALTER, DROP, TRUNCATE and RENAME. Statements are auto-committed, so they cannot be rolled back in most databases, and in production they take metadata locks that can block concurrent traffic.', keyPoints: ['CREATE, ALTER, DROP, TRUNCATE, RENAME', 'Auto-committed and generally not rollback-able', 'DDL takes locks - use online or concurrent variants where available', 'Schema changes should be versioned as migrations'], interviewTips: ['Compare TRUNCATE (DDL, fast, no WHERE) with DELETE (DML, logged, filterable)', 'Mention that MySQL 8 DDL is atomic'] },
  { topic: 'DML', title: 'Data Manipulation Language', content: 'DML changes data inside existing objects: INSERT, UPDATE, DELETE and also the SELECT used to read it. DML statements run inside transactions, so they can be committed or rolled back, and each row change is written to the redo/undo logs.', keyPoints: ['INSERT, UPDATE, DELETE (and MERGE, UPSERT)', 'Transactional and rollback-able', 'Row-level locks are held until commit', 'Batch large changes to avoid long lock chains and log growth'], interviewTips: ['Explain the difference between a logical delete and DELETE', 'Mention MERGE/UPSERT for insert-or-update'] },
  { topic: 'DQL', title: 'Data Query Language', content: 'DQL is the read side of SQL, centred on SELECT with the clauses FROM, WHERE, GROUP BY, HAVING, ORDER BY and LIMIT, plus joins and subqueries. Logical processing order differs from written order: FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY, LIMIT.', keyPoints: ['SELECT with filtering, grouping, sorting and paging', 'Logical order explains why column aliases work in ORDER BY but not WHERE', 'DQL never modifies data', 'Window functions extend DQL without collapsing rows'], interviewTips: ['State the logical processing order when explaining alias visibility', 'Mention that LIMIT without ORDER BY gives non-deterministic rows'] },
  { topic: 'DCL', title: 'Data Control Language', content: 'DCL manages access rights with GRANT to add privileges and REVOKE to remove them. Privileges can target the database, a table, a column or a routine, and roles group privileges for easier administration. Least privilege is the guiding principle.', keyPoints: ['GRANT and REVOKE manage permissions', 'Scopes: global, database, table, column, routine', 'Roles bundle privileges for groups of users', 'Follow least privilege and audit grants regularly'], interviewTips: ['Explain how views restrict access more safely than column grants', 'Mention WITH GRANT OPTION and why it is risky'] },
  { topic: 'TCL', title: 'Transaction Control Language', content: 'TCL controls transaction boundaries: BEGIN/START TRANSACTION, COMMIT, ROLLBACK, SAVEPOINT and RELEASE SAVEPOINT. COMMIT makes changes durable, ROLLBACK undoes them, and savepoints allow partial rollback inside a transaction.', keyPoints: ['COMMIT makes changes permanent', 'ROLLBACK undoes uncommitted work', 'SAVEPOINT allows partial rollback', 'Isolation level is chosen per transaction/session'], interviewTips: ['Describe partial rollback with savepoints', 'Mention implicit commit on DDL in many databases'] },
  { topic: 'CREATE', title: 'CREATE Statement', content: 'CREATE builds objects: tables, views, indexes, schemas, sequences and procedures. A table definition declares columns, data types, defaults, constraints and keys, and can include storage options such as engine and charset.', keyPoints: ['Syntax: CREATE TABLE t (col type constraints, ...)', 'CREATE TABLE AS SELECT copies structure and data', 'IF NOT EXISTS makes scripts idempotent', 'Define NOT NULL, DEFAULT and CHECK at creation time for safety'], interviewTips: ['Explain SELECT INTO versus CREATE TABLE AS SELECT', 'Mention temporary tables for staging data'] },
  { topic: 'ALTER', title: 'ALTER Statement', content: 'ALTER modifies existing objects: ALTER TABLE ADD/DROP/MODIFY COLUMN, ADD CONSTRAINT, RENAME, and ALTER INDEX or ALTER VIEW. Structural changes lock metadata, so large tables need online DDL or a shadow-table migration to avoid downtime.', keyPoints: ['ADD COLUMN is fast; DROP or MODIFY may rebuild the table', 'Use online DDL / gh-ost / pt-online-schema-change for hot tables', 'New NOT NULL columns need a default or a backfill step', 'Always test migrations on a copy at production scale'], interviewTips: ['Describe the expand-migrate-contract pattern for zero downtime', 'Mention that adding an index can still lock writes in some engines'] },
  { topic: 'DROP', title: 'DROP and TRUNCATE', content: 'DROP removes an object entirely along with its data, indexes and privileges, while TRUNCATE removes all rows but keeps the object definition and resets identity counters. Both are DDL and auto-committed, so they require care and confirmed backups.', keyPoints: ['DROP = remove object + data + metadata', 'TRUNCATE = fast delete of all rows, keeps structure', 'DROP TABLE IF EXISTS avoids errors in scripts', 'DROP with CASCADE removes dependent objects'], interviewTips: ['Compare DROP, TRUNCATE and DELETE in one table', 'Mention the risk of dropping a parent table with foreign keys'] },
];