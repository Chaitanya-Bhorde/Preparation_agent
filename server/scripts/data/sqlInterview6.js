// SQL Interview Questions Part 6 - UNIQUE, triggers, window functions, CTEs, transactions
module.exports = [
  { topic: 'UNIQUE', question: 'How does a UNIQUE constraint differ from a PRIMARY KEY, and can a UNIQUE index improve queries?', answer: 'UNIQUE forbids duplicate values but normally allows multiple NULLs (PostgreSQL) or exactly one (SQL Server treats NULL as a value); a PRIMARY KEY is NOT NULL plus UNIQUE and only one is allowed per table. A UNIQUE constraint creates a unique index, so equality and range lookups on that column get index scans for free - it is both an integrity rule and a performance tool.', explanation: 'The NULL handling difference is the classic trap.', difficulty: 'medium', followUps: ['How do you allow multiple NULLs in SQL Server?', 'Can a UNIQUE constraint span several columns?'] },
  { topic: 'Triggers', question: 'When are database triggers appropriate, and what problems do they cause?', answer: 'A trigger fires automatically on INSERT, UPDATE or DELETE (BEFORE or AFTER, per row or per statement). They suit audit logging, denormalised counters, enforcing rules that constraints cannot express and keeping derived columns in sync. Problems: they execute hidden from the caller, so debugging is hard, cascading triggers create hard-to-trace behaviour, they add latency to every write, and they often escape version control and review.', explanation: 'The hidden-execution cost is the key trade-off to state.', difficulty: 'medium', followUps: ['What is a mutation table error in Oracle?', 'How do you document trigger chains?'] },
  { topic: 'Window Functions', question: 'How do window functions differ from GROUP BY, and how do you find the top-N per group?', answer: 'GROUP BY collapses rows into one output row per group, while a window function computes over a partition and keeps every input row, adding a calculated column. Use ROW_NUMBER() OVER (PARTITION BY dept ORDER BY salary DESC) and filter rn <= 3 in a subquery or CTE to get the top three earners per department; RANK() leaves gaps for ties, DENSE_RANK() does not, and NTILE(n) buckets rows.', explanation: 'The keep-every-row property is the core of the answer.', difficulty: 'hard', followUps: ['When do LAG and LEAP replace a self join?', 'What does the frame clause ROWS BETWEEN do?'] },
  { topic: 'CTE', question: 'What is a CTE, when does it beat a subquery, and how does a recursive CTE work?', answer: 'A WITH clause defines a named result set readable once in the statement, which improves clarity, allows referencing the same derived set repeatedly and is required for recursion. A recursive CTE has an anchor query plus a recursive part that unions against itself until no rows are produced, which is the standard SQL way to walk hierarchies such as org charts or bill-of-materials. Some engines materialise CTEs, which can help or hurt performance versus inline views.', explanation: 'Anchor plus recursive term and a termination condition is the expected detail.', difficulty: 'hard', followUps: ['How do you prevent infinite recursion?', 'Does a CTE always materialise in PostgreSQL?'] },
  { topic: 'Transactions', question: 'How do you use transactions correctly in application code, and what isolation problems exist?', answer: 'Begin a transaction only around a unit of work, keep it short to reduce lock holding, commit on success and roll back on any error including in your catch block; avoid user interaction inside the transaction and beware of implicit commits from DDL. Isolation anomalies are dirty reads, non-repeatable reads and phantom reads; isolation levels READ UNCOMMITTED to SERIALIZABLE trade concurrency for safety, with SNAPSHOT/MVCC as the modern default.', explanation: 'Short transactions plus explicit error handling is the practical guidance.', difficulty: 'hard', followUps: ['What anomaly does each isolation level prevent?', 'How does MVCC avoid read locks?'] },
];