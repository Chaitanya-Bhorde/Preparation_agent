// Java Notes - covers every Java topic seeded in TOPICS.java
module.exports = [
  { topic: 'Java Introduction', title: 'What is Java?', content: 'Java is a class-based, object-oriented, platform-independent language created by James Gosling at Sun Microsystems in 1995. Source code compiles to bytecode (.class) that runs on any JVM, which is why Java follows "Write Once, Run Anywhere".', keyPoints: ['Object-oriented, statically and strongly typed', 'Compiles to platform-independent bytecode', 'Runs inside the JVM, not directly on the OS', 'Used for backend services, Android and enterprise apps'], interviewTips: ['Say "platform independent at source and bytecode level, not at JVM level"', 'Know the main() signature: public static void main(String[] args)'] },
  { topic: 'JVM JDK JRE', title: 'JVM vs JRE vs JDK', content: 'JVM is the abstract machine that executes bytecode. JRE = JVM + core libraries needed to run a program. JDK = JRE + development tools like javac, javadoc and jdb. So JDK contains JRE and JRE contains JVM.', keyPoints: ['JVM: executes bytecode, manages memory and GC', 'JRE: JVM + standard class libraries (run only)', 'JDK: JRE + compiler and tools (development)', 'JVM is platform dependent; bytecode is platform independent'], interviewTips: ['Answer the containment order JDK > JRE > JVM immediately', 'Be ready to name JVM memory areas: heap, stack, metaspace, PC register'] },
  { topic: 'Data Types and Variables', title: 'Primitive and Reference Types', content: 'Java has 8 primitives: byte(1), short(2), int(4), long(8), float(4), double(8), char(2), boolean. Everything else is a reference type: a variable holds an address pointing to an object on the heap. Variables have a type, a name and a scope.', keyPoints: ['8 primitives with fixed sizes, unlike C', 'Integer literals default to int, decimals default to double', 'Local variables must be initialised before use', 'Wrapper classes (Integer, Double) box primitives for collections'], interviewTips: ['Remember char is 16-bit unsigned (Unicode), not 8-bit', 'Explain autoboxing and the Integer cache (-128 to 127) if asked'] },
  { topic: 'Operators', title: 'Java Operators', content: 'Java operators include arithmetic (+ - * / %), relational (== != < >), logical (&& || !), bitwise (& | ^ ~ << >> >>>), assignment (= += -=), ternary (?:) and instanceof. Short-circuit && and || evaluate the right operand only when needed.', keyPoints: ['== compares references for objects and values for primitives', '&& and || short-circuit; & and | do not', '>>> is unsigned right shift (fills with zeros)', 'Ternary is the only three-operand operator'], interviewTips: ['Be ready for "why does == fail for Strings?" - reference vs content comparison', 'Know that integer division truncates toward zero'] },
  { topic: 'Control Flow', title: 'Control Flow Statements', content: 'Java supports if/else, switch (including switch expressions with arrow labels), for, enhanced for-each, while, do-while, break, continue and labelled break. do-while is exit-controlled and always executes its body at least once.', keyPoints: ['switch works with byte, short, char, int, String and enum', 'for-each cannot structurally modify the underlying collection', 'do-while is exit-controlled; while is entry-controlled', 'Labelled break/continue escapes nested loops'], interviewTips: ['Mention switch cases need compile-time constants (or enums)', 'Fall-through is the classic switch trap - always show break or arrow syntax'] },
  { topic: 'Arrays', title: 'Arrays in Java', content: 'An array is a fixed-size, zero-indexed container of same-type elements. Arrays are objects on the heap with a public final length field. Memory is contiguous so random access is O(1), but the size cannot change after creation.', keyPoints: ['Fixed size, same type, zero-indexed', 'length is a final field, not a method', 'ArrayIndexOutOfBoundsException is a runtime error', 'Helpers: Arrays.toString, Arrays.sort, Arrays.copyOf'], interviewTips: ['Say "arrays are objects in Java" and mention default values (0, false, null)', 'Know a 2D array is an array of arrays, so rows may have different lengths'] },
  { topic: 'Strings', title: 'String, StringBuilder, StringBuffer', content: 'String is immutable and literals live in the String Constant Pool. Any modification creates a new object. StringBuilder is mutable and not thread-safe; StringBuffer is mutable and synchronized. Use StringBuilder inside loops to avoid allocating many temporary Strings.', keyPoints: ['String is final and immutable', 'String pool reuses literals; new String() always allocates', 'equals() compares content, == compares references', 'StringBuilder is fast, StringBuffer is thread-safe'], interviewTips: ['List immutability benefits: caching, thread safety, safe map keys', 'Mention intern() returns the pooled instance'] },
  { topic: 'OOP Concepts', title: 'Four Pillars of OOP', content: 'Object-oriented programming organises code around objects. Java implements four pillars: encapsulation (bundle state with behaviour and hide internals), inheritance (reuse via an is-a relationship), polymorphism (one interface, many implementations) and abstraction (expose the what, hide the how).', keyPoints: ['Encapsulation: private fields + controlled accessors', 'Inheritance: extends for classes, implements for interfaces', 'Polymorphism: compile-time (overloading) and runtime (overriding)', 'Abstraction: abstract classes and interfaces'], interviewTips: ['Give one concrete example per pillar, e.g. a BankAccount class', 'Clarify Java allows single class inheritance but many interfaces'] },
  { topic: 'Inheritance', title: 'Inheritance in Java', content: 'Inheritance lets a subclass reuse and extend a superclass with extends. Java supports single inheritance of classes plus multi-level inheritance, and avoids diamond-shaped multiple class inheritance; interfaces provide that capability instead.', keyPoints: ['super() calls the parent constructor and must be the first statement', 'Private members are not accessible in the subclass', 'final classes cannot be extended; final methods cannot be overridden', 'Constructors are not inherited'], interviewTips: ['Explain the diamond problem to justify why Java dropped multiple class inheritance', 'Mention Object is the root of every class hierarchy'] },
  { topic: 'Polymorphism', title: 'Compile-time vs Runtime Polymorphism', content: 'Overloading (same name, different parameter lists) is resolved at compile time by static binding. Overriding (subclass replaces a superclass method with the same signature) is resolved at runtime using the actual object type through dynamic dispatch.', keyPoints: ['Overloading is compile-time; only parameters differentiate it', 'Overriding is runtime and needs the same signature', 'Overriding cannot reduce visibility or widen exceptions', 'static, final and private methods cannot be overridden'], interviewTips: ['Never claim overloading is achieved by changing the return type', 'Show @Override and explain how it catches signature typos'] },
  { topic: 'Abstraction and Interfaces', title: 'Abstract Classes vs Interfaces', content: 'An abstract class can hold state, constructors and a mix of abstract and concrete methods, but a class can extend only one. An interface declares a contract and can hold constants, static, default and private methods (Java 8+), and a class can implement many interfaces.', keyPoints: ['Abstract class: partial implementation, single inheritance', 'Interface: capability contract, multiple inheritance', 'default methods allow backward-compatible API evolution', 'Interface fields are implicitly public static final'], interviewTips: ['Rule of thumb: shared state -> abstract class, shared capability -> interface', 'Know that an interface cannot have a constructor'] },
  { topic: 'Encapsulation', title: 'Encapsulation and Access Modifiers', content: 'Encapsulation hides internal state behind methods so invariants can be enforced. Java offers private, default (package-private), protected and public access. The idiom is private fields plus public getters/setters only where mutation is legitimate.', keyPoints: ['protected = same package + subclasses', 'Default access is visible only inside the package', 'Setters can validate and fields can be made read-only', 'Encapsulation gives loose coupling and easy refactoring'], interviewTips: ['Say immutability is the strongest form of encapsulation', 'Give a setter example that rejects a negative balance'] },
  { topic: 'Exception Handling', title: 'Checked vs Unchecked Exceptions', content: 'Throwable splits into Error (JVM level, normally unrecoverable) and Exception. Checked exceptions must be declared or caught; unchecked exceptions extend RuntimeException and signal programming bugs. try-with-resources automatically closes AutoCloseable resources.', keyPoints: ['finally runs except on System.exit or JVM crash', 'try-with-resources prevents leaked file and socket handles', 'Never swallow exceptions with an empty catch block', 'Custom exceptions extend Exception (checked) or RuntimeException'], interviewTips: ['Explain the choice: recoverable business rule -> checked, bug -> unchecked', 'Mention suppressed exceptions when close() also throws'] },
  { topic: 'Collections Framework', title: 'Java Collections Framework', content: 'The framework provides interfaces (List, Set, Queue, Deque, Map) and implementations (ArrayList, LinkedList, HashSet, TreeSet, HashMap, TreeMap, ArrayDeque, PriorityQueue). Iteration uses Iterator or for-each, and Collections/Arrays provide utility methods.', keyPoints: ['List: ordered, duplicates allowed', 'Set: no duplicates; TreeSet keeps sorted order', 'Map is not a Collection; keys are unique', 'Fail-fast iterators throw ConcurrentModificationException'], interviewTips: ['Default to ArrayList and HashMap until you need ordering or thread safety', 'Mention fail-safe iterators used by ConcurrentHashMap'] },
  { topic: 'ArrayList vs LinkedList', title: 'ArrayList vs LinkedList', content: 'ArrayList is backed by a growable array: O(1) random access, O(n) insert/delete in the middle and good cache locality. LinkedList is a doubly linked list: O(n) random access, O(1) insert/delete once positioned, with extra memory per node.', keyPoints: ['ArrayList.get(i) is O(1); LinkedList.get(i) is O(n)', 'LinkedList also implements Deque, so it works as a queue or stack', 'ArrayList grows roughly 1.5x per resize', 'ArrayList wins for the vast majority of use cases'], interviewTips: ['State the amortised O(1) cost of ArrayList.add at the end', 'Mention LinkedList has no capacity or trimToSize concept'] },
  { topic: 'HashMap', title: 'HashMap Internals', content: 'HashMap stores entries in an array of buckets. hashCode() selects the bucket and equals() confirms the key match. Colliding entries chain together; once a bucket exceeds 8 nodes with capacity at least 64, the chain converts to a balanced tree so lookups degrade to O(log n) instead of O(n).', keyPoints: ['Average O(1) get/put; O(n) worst case', 'Default capacity 16, load factor 0.75', 'Treeify threshold 8, untreeify threshold 6', 'Not thread-safe; use ConcurrentHashMap for concurrency'], interviewTips: ['Explain why equals() must be overridden whenever hashCode() is', 'Mention HashMap allows one null key and multiple null values'] },
{ topic: 'Generics', title: 'Generics in Java', content: 'Generics add compile-time type safety by parameterising classes and methods, e.g. List<String>. Type erasure removes generic information at compile time, so generics work only with reference types and the compiler inserts the required casts.', keyPoints: ['Compile-time type checking with no runtime overhead', 'Type erasure: List<String> and List<Integer> share one class', 'PECS: Producer extends, Consumer super', 'You cannot create generic arrays or catch generic exception types'], interviewTips: ['Explain type erasure in one clear sentence', 'Use List<? extends Number> to demonstrate a wildcard'] },
  { topic: 'Multithreading', title: 'Threads and Executors', content: 'A Java thread is an independent path of execution. Threads are created by extending Thread or implementing Runnable/Callable. Production code should use ExecutorService thread pools rather than raw threads, and Future/CompletableFuture to collect results.', keyPoints: ['Runnable has no return value; Callable returns Future<T>', 'ExecutorService manages pool sizing and thread reuse', 'Daemon threads exit automatically with the JVM', 'Thread priorities are hints, not guarantees'], interviewTips: ['Prefer implementing Runnable - explain it keeps single inheritance free', 'Mention shutdown() followed by awaitTermination() for a clean pool exit'] },
  { topic: 'Thread Lifecycle', title: 'Thread Lifecycle and States', content: 'Java threads move through NEW, RUNNABLE, BLOCKED (waiting for a monitor lock), WAITING (wait/join without timeout), TIMED_WAITING (sleep/join with timeout) and TERMINATED. Thread.getState() reports the current state for debugging.', keyPoints: ['NEW -> RUNNABLE -> TERMINATED is the happy path', 'BLOCKED is monitor contention; WAITING is wait() or join()', 'sleep() keeps locks; wait() releases the monitor', 'A TERMINATED thread cannot be restarted'], interviewTips: ['Draw the state machine - interviewers frequently ask for it', 'Explain that RUNNABLE covers both ready and currently running'] },
  { topic: 'Synchronization', title: 'Synchronization and Locks', content: 'Synchronization controls access to shared mutable state. A synchronized method or block holds the object monitor so only one thread enters the critical section. java.util.concurrent adds ReentrantLock, ReadWriteLock, Semaphore, CountDownLatch and atomic variables for finer control.', keyPoints: ['Every object has an intrinsic monitor lock', 'ReentrantLock supports tryLock with timeout and fairness', 'volatile guarantees visibility but not atomicity', 'AtomicInteger uses CAS and avoids blocking'], interviewTips: ['Say volatile is not a replacement for synchronized when compound atomicity is needed', 'Explain deadlock avoidance using consistent lock ordering'] },
  { topic: 'Lambda Expressions', title: 'Lambda Expressions and Functional Interfaces', content: 'A lambda is a concise implementation of a functional interface - an interface with exactly one abstract method, such as Runnable, Comparator or the java.util.function types Function, Predicate, Consumer and Supplier. Lambdas enable functional-style programming.', keyPoints: ['Syntax: (params) -> expression or block', 'Only functional interfaces can be lambda targets', 'Method references (Class::method) are shorthand lambdas', 'Lambdas capture effectively final variables only'], interviewTips: ['Name Function, Predicate, Consumer and Supplier with their shapes', 'Explain why captured local variables must be effectively final'] },
  { topic: 'Streams API', title: 'Java Streams API', content: 'The Streams API processes data declaratively with a source, lazy intermediate operations (filter, map, sorted, distinct) and one eager terminal operation (collect, forEach, reduce, count). Streams never modify the source collection.', keyPoints: ['Intermediate operations are lazy; terminal operations trigger work', 'A stream is single-use', 'parallelStream() uses the ForkJoinPool common pool', 'Collectors.groupingBy and toMap are common interview tools'], interviewTips: ['Show filter+map+collect in one line as your standard example', 'Explain when forEachOrdered is required with parallel streams'] },
  { topic: 'File I/O', title: 'Java File I/O and NIO', content: 'Classic java.io offers byte streams (InputStream/OutputStream) and character streams (Reader/Writer, with BufferedReader for line reads). java.nio adds Path, Files, channels and memory-mapped buffers for higher-performance non-blocking I/O.', keyPoints: ['Byte streams for binary, character streams for text', 'Wrap streams in Buffered* wrappers for performance', 'try-with-resources closes streams reliably', 'Files.readAllLines and Files.writeString are the modern shortcuts'], interviewTips: ['Mention NIO.2 (Path, Files) as the preferred modern API', 'Discuss serialization and the serialVersionUID pitfall'] },
  { topic: 'Garbage Collection', title: 'Garbage Collection and Memory Model', content: 'The JVM heap is split into the young generation (Eden plus two survivor spaces) and the old generation. Minor GC copies live young objects between survivor spaces and promotes long-lived ones; major GC cleans the old generation and causes stop-the-world pauses.', keyPoints: ['GC roots: stack references, static fields, JNI references', 'Liveness is decided by reachability, not reference counting', 'G1 is the default collector in modern JDKs; ZGC targets very low pauses', 'System.gc() is only a hint to the JVM'], interviewTips: ['Explain strong, soft, weak and phantom references with one use case each', 'Note finalize() is deprecated - prefer try-with-resources or Cleaner'] },
];