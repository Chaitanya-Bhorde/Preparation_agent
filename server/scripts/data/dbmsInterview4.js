// DBMS Interview Questions Part 4 - transaction states and B+ tree
module.exports = [
  { topic: 'Transaction States', question: 'Walk through every state a transaction passes through, including the failure path.', answer: 'A transaction starts in the Active state and executes its read and write operations. After the final statement it enters Partially Committed, waiting for its log records to reach stable storage. If the log is flushed successfully it enters Committed and its effects are permanent. If any statement or the flush fails it enters Failed, the recovery manager rolls back its writes, and it finally reaches Aborted (Terminated), after which it may be restarted as a new transaction.', explanation: 'Draw the state diagram: Active -> Partially Committed -> Committed, with Failed -> Aborted as the failure path.', difficulty: 'easy', followUps: ['Why is Partially Committed not the same as Committed?', 'What actions happen during rollback?'] },
  { topic: 'Transaction States', question: 'Why is the Partially Committed state necessary if the transaction has already executed all its statements?', answer: 'The behaviour of the transaction is only guaranteed once the commit log record reaches stable storage. Until that flush completes, a system crash would erase the transaction from the log and force a rollback, so the database cannot promise the changes are durable. The Partially Committed state marks exactly that window between "all work done in memory" and "durably recorded on disk".', explanation: 'The point is durability, which is one of the four ACID properties.', difficulty: 'hard', followUps: ['What is the role of the WAL rule (write-ahead logging)?', 'How does a checkpoint reduce recovery time?'] },
  { topic: 'Transaction States', question: 'How does a cascading rollback happen, and how do you prevent it?', answer: 'If transaction T2 reads a value written by uncommitted T1 and T1 then aborts, T2 must also be rolled back even if T2 is ready to commit. If other transactions read T2\'s writes the rollback cascades further. Strict two-phase locking (holding all exclusive locks until commit) or the recovery manager tracking dirty reads and dependants both prevent this, which is why most databases use strict 2PL by default.', explanation: 'Cascading rollback is the practical cost of not using strict 2PL.', difficulty: 'hard', followUps: ['What is a cascadeless schedule?', 'What is a recoverable schedule?'] },
  { topic: 'B+ Tree', question: 'Describe the structure of a B+ tree and explain why it is the default index for relational databases.', answer: 'A B+ tree has internal nodes holding only separator keys and pointers, while all record pointers live in the leaves, which are linked in sorted order. Every lookup costs one root-to-leaf traversal, so search, insert and delete are all O(log n) with a very small constant because of high fan-out. Because the leaves are linked, a range query scans sequentially instead of re-traversing from the root, which makes BETWEEN, ORDER BY and prefix LIKE queries efficient.', explanation: 'Mention that internal nodes hold no data, which is what maximises fan-out.', difficulty: 'medium', followUps: ['How does a B+ tree handle a leaf overflow?', 'What is a clustered versus a secondary index?'] },
  { topic: 'B+ Tree', question: 'What happens in a B+ tree during a leaf split and when are internal node splits required?', answer: 'When a leaf overflows, it splits into two leaves, the middle key is copied up into the parent as a separator, and the new right leaf is linked into the leaf chain to preserve sequential order. If the parent also overflows it splits the same way, propagating upward, and if the root splits the tree grows by one level. Deletions remove the entry and may merge or redistribute underfull leaves, with the height shrinking only when the root loses its last key.', explanation: 'Note that the separator key is copied up for leaves but moved up for internal nodes.', difficulty: 'hard', followUps: ['Why might a database not shrink the tree after deletes?', 'How does the fill factor affect write amplification?'] },
  { topic: 'B+ Tree', question: 'How would you decide between a B+ tree index and a hash index for a workload?', answer: 'Hash indexes give O(1) equality lookups but cannot serve range queries, sorting or prefix matching, and they degrade badly with skew or poor hash functions. B+ tree indexes give O(log n) for equality and also support ranges, ORDER BY, and covering scans, at the cost of extra comparisons and maintained ordering on writes. Choose hash for pure key-value equality lookups, and B+ tree whenever the query needs ordering or ranges, which is the common case.', explanation: 'Saying "range queries decide it" is the crisp answer.', difficulty: 'hard', followUps: ['Why does MongoDB use both index types?', 'What is a covered index scan?'] },
];