// System Design Notes Part 3 - advanced patterns and design questions
module.exports = [
  { topic: 'Monitoring and Logging', title: 'Monitoring, Logging and Observability', content: 'Observability rests on three signals: metrics (numeric time series such as request rate, error rate, latency), logs (structured per-request records) and traces (span trees following one request through services). Alerts fire on symptoms users feel, like error-rate SLO burn, not on every CPU spike.', keyPoints: ['Golden signals: latency, traffic, errors, saturation', 'Structured JSON logs are searchable; add request IDs', 'Distributed tracing finds which service is slow', 'Dashboards per service plus one global overview'], interviewTips: ['Explain RED (rate, errors, duration) for services', 'Mention correlation IDs to follow a request across services'] },
  { topic: 'Distributed Transactions', title: 'Distributed Transactions and Sagas', content: 'Two-phase commit blocks on a coordinator and is usually avoided across services. The saga pattern breaks a workflow into local transactions, each publishing an event that triggers the next step, with compensating transactions undoing previous steps on failure (e.g. refund the payment if shipping fails).', keyPoints: ['Choreography: services react to each others events', 'Orchestration: a central saga coordinator issues commands', 'Compensation must be idempotent and retry-safe', 'Deliveries are at-least-once, so handlers deduplicate'], interviewTips: ['Contrast 2PC blocking with saga compensation', 'Give an order-payment-shipping saga example end to end'] },
  { topic: 'Consistent Hashing', title: 'Consistent Hashing', content: 'Consistent hashing maps both servers and keys onto a hash ring, so each key belongs to the next server clockwise. Adding or removing a server moves only about 1/N of keys instead of remapping everything, which keeps caches warm. Virtual nodes smooth out uneven distribution.', keyPoints: ['Used by DynamoDB, Cassandra, memcached clients, CDNs', 'Virtual nodes (100-200 per server) balance load', 'Replication: store each key on the next N servers', 'Ring lookups need a coordinator or gossip protocol'], interviewTips: ['Quote the "only 1/N keys move" benefit precisely', 'Explain why virtual nodes fix skewed hotspots'] },
  { topic: 'Design a URL Shortener', title: 'Design: URL Shortener (bit.ly)', content: 'Requirements: shorten long URLs, redirect fast, optional analytics and expiry. Encode a numeric ID in base62 (a-zA-Z0-9) to get short codes; a counter or key-generation service hands out IDs. Store the mapping in a key-value store; reads dominate massively, so cache hot codes in Redis and front everything with a CDN.', keyPoints: ['Base62 of an auto-increment ID: tiny, collision-free', '301 vs 302 redirect: SEO versus click tracking', 'Estimate: read-heavy, ~100:1 read/write ratio', 'Rate limit creation; validate URLs against blocklists'], interviewTips: ['Start with estimates: QPS and storage per year', 'Discuss custom aliases and expiry as extensions'] },
  { topic: 'Design a Chat Application', title: 'Design: Chat Application (WhatsApp)', content: 'Connections are long-lived WebSockets through stateful gateway servers; a session service tracks which gateway holds each user. Messages go to the recipients gateway (or a queue if offline), are persisted keyed by conversation with an incrementing sequence for ordering, and clients ack receipt for delivery and read ticks.', keyPoints: ['WebSocket gateways scale with a registry of user -> server', 'Message ordering: server-assigned sequence per conversation', 'Offline delivery via persistent queue and push notification', 'Fan-out to group members can be async'], interviewTips: ['Explain heartbeat/reconnect handling for mobile clients', 'Discuss end-to-end encryption as a layer on transport'] },
  { topic: 'Design a News Feed', title: 'Design: News Feed (Facebook/Twitter)', content: 'Users post to a store; feeds are assembled by fan-out. Push (write-time fan-out) pre-computes each followers feed on write for fast reads but is expensive for celebrities; pull (read-time fan-out) merges followed users posts on request for cheap writes but slower reads. Hybrid: push for normal users, pull for high-follower accounts, with a ranking service ordering candidates.', keyPoints: ['Fan-out on write vs on read is the core trade-off', 'Celebrity problem solved by hybrid fan-out', 'Feed cache: per-user list of post IDs, hydrated on view', 'Ranking features: recency, affinity, engagement'], interviewTips: ['Walk through both fan-out models with costs before choosing', 'Mention deduplication and pagination by post ID'] },
  { topic: 'Design a Rate Limiter', title: 'Design: Distributed Rate Limiter', content: 'Count requests per client in a central store; Redis is standard because INCR and EXPIRE are atomic. Use a token bucket or sliding window per rule (user, IP, endpoint), enforce at the API gateway so all instances share limits, and return 429 with Retry-After. For high scale, run local buckets that sync to the central store periodically.', keyPoints: ['Atomicity matters: check-and-set must not race', 'Rules hierarchy: global, per-user, per-endpoint', 'Fail-open or fail-close is a business decision', 'Headers X-RateLimit-* help well-behaved clients'], interviewTips: ['Explain the race in a naive GET-then-SET limiter', 'Discuss what happens when Redis is down'] },
  { topic: 'Design a Payment System', title: 'Design: Payment System', content: 'Payments demand correctness: use idempotency keys so retries never double-charge, record every state change in an append-only ledger, and reconcile asynchronously with the PSP via webhooks and periodic batch matching. Money movement flows through pending, captured, refunded states, and everything is auditable.', keyPoints: ['Idempotency key on every payment request', 'Double-entry ledger as the source of truth', 'PSP webhooks drive state transitions', 'PCI compliance: never store raw card numbers'], interviewTips: ['Lead with idempotency and exactly-once money movement', 'Mention reconciliation as the safety net nobody can skip'] },
  { topic: 'Design a File Storage Service', title: 'Design: File Storage (Dropbox/S3)', content: 'Files are stored as objects with metadata in a database and blobs in object storage, addressed by content hash for deduplication. Uploads go through presigned URLs directly to storage; large files are chunked so uploads resume. Downloads are served via CDN with signed, expiring URLs for access control.', keyPoints: ['Chunking enables resume and delta sync', 'Content-addressable storage dedupes identical blocks', 'Presigned URLs keep the app server out of the data path', 'Metadata (ACL, versions) lives in a relational DB'], interviewTips: ['Discuss sync protocol: poll vs long-poll vs websockets', 'Mention versioning and soft delete for recoverability'] },
  { topic: 'Design a Notification System', title: 'Design: Notification System', content: 'Producers publish events to a queue; a fan-out service resolves which channels each user opted into (email, SMS, push), applies preferences and throttling, then hands messages to channel providers through retrying workers. Deduplication and rate limits per user prevent spam, and delivery receipts update an analytics store.', keyPoints: ['Queue decouples producers from channel workers', 'User preferences and quiet hours are honoured', 'At-least-once delivery needs dedup keys', 'Templates per channel; provider failover for reliability'], interviewTips: ['Start with channels, preferences and throttling', 'Explain prioritisation: OTP beats marketing'] },
];