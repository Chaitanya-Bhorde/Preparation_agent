// SQL Notes Part 4 - DISTINCT and join variants
module.exports = [
  { topic: 'DISTINCT', title: 'DISTINCT Clause', content: 'DISTINCT removes duplicate rows from the result using value equality across all selected columns, so SELECT DISTINCT a, b deduplicates on the pair (a, b). It runs after WHERE/GROUP BY and before ORDER BY, and it usually requires a sort or hash step, which costs performance on large results.', keyPoints: ['DISTINCT applies to the whole selected row, not one column', 'COUNT(DISTINCT col) counts unique non-null values', 'GROUP BY can achieve the same deduplication plus aggregation', 'DISTINCT on a wide projection is expensive'], interviewTips: ['Explain that DISTINCT is not a fix for a wrong join that multiplied rows', 'Mention that DISTINCT ON is PostgreSQL-specific'] },
  { topic: 'INNER JOIN', title: 'INNER JOIN', content: 'INNER JOIN returns only rows where the join condition matches in both tables. It is the default join type when you write JOIN. Implementation choices are nested loop (good for small inputs with indexes), hash join (good for large unsorted inputs) and merge join (good when both sides are sorted).', keyPoints: ['Only matched rows survive', 'Join condition normally compares a foreign key to a primary key', 'ON filters pairs; WHERE filters the joined result', 'Indexing the join column on both sides matters most for performance'], interviewTips: ['Explain the difference between ON and WHERE for outer joins', 'Mention that adding a WHERE on the outer table can silently turn an outer join into an inner join'] },
  { topic: 'LEFT JOIN', title: 'LEFT (OUTER) JOIN', content: 'LEFT JOIN returns every row from the left table and the matching columns from the right table, filling NULLs when there is no match. It is used for optional relationships, such as listing all customers with their last order when some customers have never ordered.', keyPoints: ['All left rows are preserved', 'Non-matching right columns become NULL', 'Filtering the right table in WHERE removes the NULL rows - put such predicates in ON instead', 'LEFT JOIN plus IS NULL finds rows with no match'], interviewTips: ['Show the anti-join pattern: LEFT JOIN ... WHERE right.id IS NULL', 'Warn that COUNT(right.col) ignores NULLs while COUNT(*) counts the row'] },
  { topic: 'RIGHT JOIN', title: 'RIGHT (OUTER) JOIN', content: 'RIGHT JOIN preserves all rows of the right table and fills NULLs for missing left-side matches. Because RIGHT JOIN is exactly LEFT JOIN with the tables swapped, most teams standardise on LEFT JOIN for readability.', keyPoints: ['All right rows are preserved', 'Equivalent to LEFT JOIN with operands reversed', 'NULLs appear on the left side when there is no match', 'Rarely needed if you keep a consistent join direction'], interviewTips: ['Say you would rewrite it as LEFT JOIN for consistency in a codebase', 'Mention that some ORMs do not support RIGHT JOIN well'] },
  { topic: 'FULL JOIN', title: 'FULL (OUTER) JOIN', content: 'FULL JOIN returns matched rows plus unmatched rows from both sides, filling NULLs where the other side is missing. It answers reconciliation questions such as comparing two data sources. MySQL has no FULL JOIN keyword, so it is emulated with a LEFT JOIN UNION a RIGHT JOIN.', keyPoints: ['Union of LEFT and RIGHT results', 'Both sides can contribute NULL-filled rows', 'Emulate in MySQL with LEFT JOIN UNION RIGHT JOIN (or anti-join UNION)', 'Useful for diffing two tables or staging versus production'], interviewTips: ['Give the reconciliation use case: find records present in one system only', 'Mention that UNION (not UNION ALL) is needed to remove duplicated matched rows'] },
  { topic: 'Self Join', title: 'Self Join', content: 'A self join joins a table to itself using two aliases so different rows can be compared. Typical uses are employee-manager hierarchies, comparing a row to the previous row or finding pairs that share a value. Aliases are mandatory to disambiguate the two roles.', keyPoints: ['Use two aliases: employees e JOIN employees m ON e.manager_id = m.id', 'Used for hierarchies and row-to-row comparison', 'Same table, so indexes on the join column help a lot', 'Recursive CTEs handle deep hierarchies better than repeated self joins'], interviewTips: ['Give the employee-manager query immediately - it is the canonical example', 'Mention deleting duplicates with a self join on id comparison'] },
  { topic: 'Correlated Subqueries', title: 'Correlated Subqueries', content: 'A correlated subquery references a column from the outer query, so it is evaluated once per outer row rather than once overall. This makes it powerful for "per group" logic but expensive, because it can execute many times unless the optimiser rewrites it as a semi-join or a window function.', keyPoints: ['Inner query depends on the outer row', 'EXISTS and NOT EXISTS are natural correlated forms', 'Frequently rewritable as a join or a window function', 'Often the slowest pattern in a query plan'], interviewTips: ['Show the same answer with JOIN plus GROUP BY and compare plans', 'Explain that NOT EXISTS handles NULLs better than NOT IN'] },
];