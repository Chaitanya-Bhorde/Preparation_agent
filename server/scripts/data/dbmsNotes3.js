// DBMS Notes Part 3 - transactions, storage and advanced query topics
module.exports = [
  { topic: 'Transaction States', title: 'Transaction States', content: 'A transaction passes through active (executing), partially committed (last statement executed), committed (changes durable) and failed (cannot continue) states, ending in terminated. Before commit, undo is possible by reading the log; after a failure the recovery manager performs undo or redo.', keyPoints: ['Active -> Partially Committed -> Committed -> Terminated', 'Failed leads to rollback and then Terminated', 'The transaction log enables undo before commit and redo after commit', 'A killed transaction must be rolled back, never left half applied'], interviewTips: ['Draw the state diagram and mention where log records are written', 'Explain why partially committed still needs a forced log write'] },
  { topic: 'Concurrency Control', title: 'Concurrency Control Techniques', content: 'Concurrency control allows overlapping transactions while keeping results serializable. Two-phase locking (growing then shrinking) is the classic method; timestamp ordering and optimistic validation are the alternatives; MVCC keeps multiple versions so readers never block writers.', keyPoints: ['2PL with strict variant avoids cascading rollbacks', 'Shared locks allow reads, exclusive locks allow writes', 'Timestamp ordering aborts transactions that read or write too late', 'MVCC powers PostgreSQL, MySQL InnoDB and Oracle readers'], interviewTips: ['Explain growing and shrinking phases and why strict 2PL is preferred', 'Mention deadlocks as the main cost of locking'] },
  { topic: 'Serializability', title: 'Serializability', content: 'A schedule is serializable when its effect equals that of some serial execution of the same transactions. Conflict serializability checks a precedence graph for cycles using read-write conflicts; view serializability is broader but harder to test. Recoverable and cascadeless schedules add safety.', keyPoints: ['Conflict serializable iff the precedence graph is acyclic', 'Read-write, write-read and write-write are the three conflicts', 'Recoverable: a transaction commits only after the transactions it read from', 'Cascadeless schedules avoid cascading rollbacks'], interviewTips: ['Draw the precedence graph and show the cycle detection', 'Distinguish blind writes, which cause non-conflict-serializable but view-serializable schedules'] },
  { topic: 'B-Tree', title: 'B-Tree and B+-Tree Overview', content: 'A B-tree of order m is a balanced search tree where every node holds up to m-1 keys and m children, all leaves sit at the same depth and every node is at least half full. Data pointers may live in internal nodes, so range scans are less efficient than in a B+-tree.', keyPoints: ['Height stays O(log n) because the tree is balanced and fat', 'Both internal and leaf nodes store data pointers', 'Supports search, sequential access, insert and delete in O(log n)', 'B+-tree moves all data pointers to leaves for better range scans'], interviewTips: ['Highlight that balanced height is the reason databases use it for disk pages', 'Compare with a B+-tree to answer "which one does MySQL use and why"'] },
  { topic: 'B+ Tree', title: 'B+ Tree', content: 'A B+-tree stores all records in the leaf level and keeps only routing keys in internal nodes. Leaves are linked in a doubly linked list, so range and sorted scans are efficient, and each node typically holds hundreds of keys, giving a very shallow tree for disk access.', keyPoints: ['Internal nodes are an index; real data is only in leaves', 'Linked leaves make range queries sequential', 'Higher fanout means fewer disk reads per lookup (often 3-4 levels)', 'This is the default index structure of MySQL InnoDB and PostgreSQL'], interviewTips: ['Explain why a linked leaf list accelerates BETWEEN and ORDER BY queries', 'Mention fill factor and how inserts cause splits'] },
{ topic: 'Views', title: 'Views and Materialized Views', content: 'A view is a stored query that behaves like a virtual table, so it costs no storage and always reflects current data. Joins, filters and column subsets can be hidden behind it for security and simplicity. A materialized view stores the computed result physically and must be refreshed.', keyPoints: ['Virtual table, no data stored (non-materialized)', 'Used for security, simplification and abstraction', 'A view is updatable only under strict conditions', 'Materialized views trade storage and refresh time for read speed'], interviewTips: ['Explain using a view to expose only non-sensitive columns', 'Mention WITH CHECK OPTION and why some views cannot be updated'] },
  { topic: 'Database Security', title: 'Database Security', content: 'Database security covers authentication, authorization (GRANT and REVOKE privileges), least privilege, views for row/column filtering, encryption at rest and in transit, auditing and protection against SQL injection through parameterised queries.', keyPoints: ['Authentication identifies users; authorization limits what they can do', 'GRANT/REVOKE with roles implements least privilege', 'Encryption protects data at rest (TDE) and in transit (TLS)', 'Prepared statements defeat SQL injection'], interviewTips: ['Name the four levels of access control: DB, table, column, row (via views)', 'Mention auditing and why shared application accounts are an anti-pattern'] },
  { topic: 'Recovery', title: 'Crash Recovery: ARIES and Logs', content: 'Recovery restores a consistent database after failure. Write-ahead logging records undo (before image) and redo (after image) entries before data pages are flushed, so the analysis, redo and undo phases can rebuild state. Checkpoints bound the work needed after a crash.', keyPoints: ['WAL rule: log record must reach disk before the data page', 'Analysis -> Redo -> Undo three-phase recovery', 'Checkpoints shorten recovery time', 'Shadow paging is the alternative that avoids undo/redo logs'], interviewTips: ['Explain why WAL guarantees atomicity and durability together', 'Mention what happens to in-flight transactions during undo'] },
  { topic: 'Relational Algebra', title: 'Relational Algebra', content: 'Relational algebra is the procedural foundation of SQL. Core operators are selection (sigma), projection (pi), union, set difference, Cartesian product and rename, with join as derived shorthand. Query optimisers rewrite algebra trees into cheaper equivalent forms.', keyPoints: ['sigma filters rows, pi keeps columns (and removes duplicates)', 'Union, difference and intersection require union-compatible relations', 'Natural join matches on equally named attributes', 'Division answers "for all" style queries'], interviewTips: ['Write the algebra for a simple join query and then the equivalent SQL', 'Explain that optimisers use algebraic equivalences such as pushing selections down'] },
];