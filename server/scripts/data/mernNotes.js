// MERN Stack Notes Part 1 - MERN overview, HTML, CSS, JavaScript, DOM
module.exports = [
  { topic: 'MERN Introduction', title: 'What is the MERN Stack?', content: 'MERN is a full-stack JavaScript stack: MongoDB (document database), Express (Node HTTP framework), React (component-based UI library) and Node.js (server-side JavaScript runtime). Because one language covers client, server and database queries, teams share models, validation logic and utility code.', keyPoints: ['MongoDB: schema-flexible JSON document store', 'Express: routing, middleware and REST endpoints on Node', 'React: declarative component UI with a virtual DOM', 'Node.js: non-blocking, event-driven JavaScript runtime'], interviewTips: ['Explain the request flow: React fetch -> Express route -> Mongoose -> MongoDB -> JSON response', 'Know MERN vs MEAN: React replaces Angular'] },
  { topic: 'HTML Basics', title: 'HTML Fundamentals', content: 'HTML describes document structure with elements nested to form the DOM tree. Semantics matter: header, nav, main, section, article, aside and footer describe meaning, while div and span are generic containers. Forms collect input, and attributes supply behaviour such as href, src, alt and type.', keyPoints: ['Elements with block, inline and inline-block display behaviour', 'Semantic tags improve accessibility and SEO', 'Forms: input types, labels, name attributes and validation attributes', 'The browser parses HTML into the DOM tree React renders into'], interviewTips: ['Mention accessibility attributes: alt text, label for, ARIA roles', 'Explain the difference between block and inline elements'] },
  { topic: 'CSS Basics', title: 'CSS Fundamentals', content: 'CSS controls presentation through cascade, specificity and inheritance. Modern layout uses Flexbox for one-dimensional alignment and Grid for two-dimensional layout. The box model (content, padding, border, margin) plus box-sizing: border-box is the foundation of predictable sizing.', keyPoints: ['Specificity order: inline > id > class > element', 'Flexbox for rows/columns, Grid for two-dimensional layout', 'box-sizing: border-box keeps widths predictable', 'Relative units (rem, em, %, vh, vw) support responsive design'], interviewTips: ['Explain specificity with a concrete conflict example', 'Mention media queries and mobile-first design'] },
  { topic: 'JavaScript Basics', title: 'JavaScript Core Concepts', content: 'JavaScript is a dynamically typed, single-threaded language with prototype-based objects. var is function-scoped and hoisted; let and const are block-scoped. Functions are first-class values, and this depends on how a function is called unless you use arrow functions which capture lexical this.', keyPoints: ['let/const are block scoped, var is function scoped', 'Types: number, string, boolean, null, undefined, symbol, bigint, object', 'this depends on call site; arrow functions inherit this', '== coerces types, === compares value and type'], interviewTips: ['Explain hoisting and the temporal dead zone for let/const', 'Mention prototypal inheritance and Object.create'] },
  { topic: 'ES6 Features', title: 'Modern ES6+ Features', content: 'ES6 added let/const, arrow functions, template literals, destructuring, default parameters, rest/spread, classes, modules and promises. Later versions added async/await, optional chaining, nullish coalescing, and Object/Array helpers that make React code concise.', keyPoints: ['Destructuring: const { a, b } = obj, const [x, y] = arr', 'Spread copies/merges: { ...state, name } - essential for immutable React updates', 'Modules: import/export replace global scripts', 'Optional chaining ?. and nullish coalescing ??'], interviewTips: ['Show immutable state update with spread as your go-to React example', 'Explain why spread is a shallow copy'] },
  { topic: 'JavaScript Async', title: 'Async JavaScript: Callbacks, Promises, async/await', content: 'JavaScript handles concurrency with an event loop. Callbacks were the first async pattern and caused callback hell, so promises added chaining with then/catch/finally, and async/await makes asynchronous code read synchronously with try/catch error handling.', keyPoints: ['Event loop: call stack, task queue and microtask queue', 'Promises have three states: pending, fulfilled, rejected', 'await pauses the async function, never the thread', 'Promise.all (fail fast) vs Promise.allSettled (never throws)'], interviewTips: ['Explain that await does not block the JS thread', 'Mention that unhandled promise rejections crash Node in newer versions'] },
  { topic: 'JavaScript DOM', title: 'DOM Manipulation and Events', content: 'The DOM is the in-memory tree of the page. querySelector and getElementById find nodes, while createElement, appendChild, textContent and classList mutate them. Events bubble from target to ancestors, so delegation attaches one listener to a parent instead of many children.', keyPoints: ['querySelector/querySelectorAll return single/multiple matches', 'Event phases: capture, target, bubbling', 'Event delegation uses bubbling to handle dynamic children', 'In React you rarely touch the DOM directly; refs cover the exceptions'], interviewTips: ['Explain delegation with a list of dynamically added buttons', 'Mention that direct DOM updates and React state can conflict'] },
{ topic: 'React Introduction', title: 'React Fundamentals', content: 'React is a JavaScript library for building user interfaces from components. It maintains a virtual DOM, diffs it against the previous render, and applies only the minimal set of real DOM updates. Declarative code (describe UI for a given state) replaces manual DOM manipulation.', keyPoints: ['Components are functions returning JSX', 'State changes trigger re-render', 'Virtual DOM diffing minimises real DOM work', 'Unidirectional data flow makes reasoning easier'], interviewTips: ['Explain the render-commit pipeline and React 18 automatic batching', 'Mention that React is a library, not a full framework like Angular'] },
  { topic: 'JSX', title: 'JSX Under the Hood', content: 'JSX is syntax sugar that compiles to React.createElement (or the jsx runtime) calls, producing plain objects describing the UI. It is not HTML: class becomes className, expressions go in braces, and components must return a single root or a fragment.', keyPoints: ['JSX compiles to function calls, not strings', 'className/ htmlFor replace class/for', 'Curly braces embed any JS expression', 'Fragments <>...</> avoid extra DOM nodes'], interviewTips: ['Show the compiled output of a small JSX snippet', 'Mention keys and why array children need them'] },
  { topic: 'Components', title: 'Functional Components', content: 'Modern React uses functional components: plain functions receiving props and returning JSX. Class components still exist in legacy code with lifecycle methods, but function components plus hooks cover all use cases with less boilerplate and better reuse via custom hooks.', keyPoints: ['PascalCase naming is required for JSX usage', 'Function + hooks is the modern standard', 'Composition over inheritance is the React model', 'One component per file keeps modules clean'], interviewTips: ['Be able to convert a class component to hooks live', 'Explain why hooks cannot be called conditionally'] },
  { topic: 'Props', title: 'Props and Prop Drilling', content: 'Props are read-only inputs passed parent to child; mutating them is forbidden because components must be pure functions of their props and state. Passing props through many unrelated layers is prop drilling; context or component composition fixes it.', keyPoints: ['Props are immutable from the child perspective', 'children prop enables wrapper/layout components', 'PropTypes or TypeScript documents contracts', 'Prop drilling signals a state-design problem'], interviewTips: ['Demonstrate the children pattern with a Card component', 'Explain when context beats prop drilling and when it does not'] },
  { topic: 'State and useState', title: 'State with useState', content: 'useState returns the current value and a setter. Updates are asynchronous and batched; the functional form setCount(c => c + 1) is required when the next value depends on the previous one. State is local to the component instance and resets when the component unmounts.', keyPoints: ['Setter with function form avoids stale closures', 'React batches multiple setStates in one render', 'Objects must be replaced immutably ({...obj, key: v})', 'Derived values do not need duplicate state'], interviewTips: ['Explain why direct mutation does not trigger re-render', 'Show lifting state up between two sibling components'] },
  { topic: 'useEffect', title: 'useEffect and the Dependency Array', content: 'useEffect runs side effects after render: subscriptions, timers, logging, data fetching. The dependency array controls re-execution - empty array means once after mount, omitted means every render. The returned cleanup function runs before the next effect and on unmount, preventing leaks.', keyPoints: ['Effect runs after paint, useLayoutEffect before paint', 'Every state/prop used inside belongs in deps', 'Cleanup prevents duplicate subscriptions and race leaks', 'Fetching needs an ignore flag against race conditions'], interviewTips: ['Walk through the classic stale-closure interval bug and fix', 'Explain StrictMode double-invoking effects in development'] },
  { topic: 'React Hooks', title: 'Hooks Rules and Custom Hooks', content: 'Hooks must run in the same order every render, so call them only at the top level of components or custom hooks - never inside conditions or loops. A custom hook is any function starting with use that composes built-in hooks, letting logic be shared without changing component trees.', keyPoints: ['Order stability is what lets React match hook state', 'useCallback memoises functions, useMemo memoises values, useRef holds mutable values without re-render', 'Custom hooks turn repeated logic into reusable units', 'eslint-plugin-react-hooks enforces the rules'], interviewTips: ['Write a useFetch custom hook with loading/error states', 'Explain useRef vs useState with a timer example'] },
  { topic: 'React Router', title: 'React Router and SPA Navigation', content: 'React Router maps URL paths to components without page reloads. Routes are declarative (createBrowserRouter in v6.4+), useParams reads path segments, useNavigate performs programmatic navigation, and nested routes with Outlet compose layouts.', keyPoints: ['Dynamic segments: /users/:id', 'Nested routes and Outlet share layouts', 'useSearchParams handles query strings', 'Lazy routes code-split with React.lazy'], interviewTips: ['Explain client-side routing vs server routing', 'Show a protected route wrapper checking auth'] },
  { topic: 'Redux Toolkit', title: 'Redux Toolkit and Modern State Management', content: 'Redux Toolkit (RTK) is the official, opinionated way to write Redux: configureStore sets up the store with devtools and middleware, createSlice generates actions and reducers together using Immer, and createAsyncThunk handles async flows. RTK Query adds a data-fetching layer with caching.', keyPoints: ['createSlice removes action type boilerplate', 'Immer lets you "mutate" state safely in reducers', 'createAsyncThunk dispatches pending/fulfilled/rejected actions', 'RTK Query replaces most hand-rolled thunks for API data'], interviewTips: ['Explain the unidirectional data flow: dispatch -> reducer -> new state -> UI', 'Say when Redux is overkill (small app) and what you use instead (Context/RTK Query)'] },
  { topic: 'Context API', title: 'Context API for Cross-Component State', content: 'Context lets a provider share a value with every descendant without prop drilling. createContext defines it, Provider supplies it, and useContext consumes it. Every consumer re-renders when the provider value changes, so context suits low-frequency data such as theme, auth and locale.', keyPoints: ['Solves prop drilling, not high-frequency state', 'Memoise the provider value or consumers re-render on every parent render', 'Split contexts so unrelated consumers do not re-render together', 'A custom hook (useAuth) wraps useContext for a clean API'], interviewTips: ['Compare Context vs Redux: context is dependency injection, Redux is an external store with middleware', 'Mention the re-render cost of one large context value'] },
  { topic: 'React Performance', title: 'React Performance Optimisation', content: 'React re-renders a component when its parent renders or state changes. Optimise by memoising components (React.memo), values (useMemo) and callbacks (useCallback), code-splitting with React.lazy, virtualising long lists and debouncing inputs. Measure first with the DevTools Profiler - never optimise blindly.', keyPoints: ['React.memo skips re-render when props are shallow-equal', 'useMemo/useCallback have their own cost - apply them to real bottlenecks', 'Keys must be stable; index keys break lists that reorder', 'React.lazy plus Suspense splits the bundle per route'], interviewTips: ['Describe the workflow: record, find wide commits, fix, re-measure', 'Mention list virtualisation (react-window) for thousands of rows'] },
  { topic: 'Node.js Introduction', title: 'Node.js Fundamentals', content: 'Node.js is a JavaScript runtime built on the V8 engine with an event-driven, non-blocking I/O model. A single-threaded event loop serves thousands of concurrent connections by delegating file and network I/O to the OS and the libuv thread pool - ideal for I/O-bound APIs, not for CPU-heavy computation.', keyPoints: ['V8 runs JavaScript; libuv provides the event loop and thread pool', 'Non-blocking I/O avoids one-thread-per-connection cost', 'One JS thread per process; worker_threads add CPU parallelism', 'npm is the package ecosystem; CommonJS and ESM are both supported'], interviewTips: ['Explain why Node suits I/O-bound APIs but struggles with CPU-bound work', 'Mention cluster mode to use every CPU core'] },
  { topic: 'Node Event Loop', title: 'The Node.js Event Loop', content: 'The event loop cycles through phases: timers (setTimeout/setInterval), pending callbacks, poll (I/O events), check (setImmediate) and close callbacks. Microtasks (Promises, process.nextTick) drain completely between phases, which is why nextTick can starve the loop if overused.', keyPoints: ['Timers -> pending -> poll -> check -> close, then repeat', 'Promise callbacks and nextTick run before the next phase', 'Long synchronous code blocks the loop - no other request is served', 'setImmediate runs after poll; setTimeout(fn, 0) runs in the timers phase'], interviewTips: ['Order a setTimeout, setImmediate and Promise chain correctly', 'Explain how one blocking route degrades the entire server'] },
  { topic: 'Node Modules and npm', title: 'Node Modules, npm and package.json', content: 'require()/import load modules; Node resolves core modules first, then node_modules. package.json declares dependencies and scripts, while package-lock.json pins exact versions for reproducible installs. Semantic versioning (major.minor.patch) governs upgrades; ^ and ~ control how far updates may drift.', keyPoints: ['^ allows minor+patch updates, ~ patch only', 'npm ci installs strictly from the lockfile - use it in CI', 'Commit the lockfile, never node_modules', 'The require cache means a module body executes once'], interviewTips: ['Explain dependencies vs devDependencies', 'Describe resolving a security alert in a transitive dependency'] },
  { topic: 'Express Basics', title: 'Express.js Basics', content: 'Express is a minimal web framework: app creates the server, app.use mounts middleware, and handlers for get/post/put/delete respond to requests. Data arrives on req (params, query, body) and leaves via res (status, json). Middleware executes strictly in registration order.', keyPoints: ['express.json() must be registered before routes or req.body stays undefined', 'Router instances group related routes', 'Error handlers have four arguments (err, req, res, next)', 'Order matters: parsers first, then routes, error handler last'], interviewTips: ['Sketch a minimal server with one route and one error handler', 'Explain middleware chaining and next()'] },
  { topic: 'Express Routing', title: 'Express Routing Patterns', content: 'Routes combine an HTTP verb with a path: static segments, parameters (/users/:id via req.params), query strings (?page=2 via req.query) and wildcards. Express.Router keeps feature modules self-contained, and .route().get().post() chains handlers per resource.', keyPoints: ['req.params for path segments, req.query for ?key=value', 'Register specific paths before generic ones', 'app.use(\'/api/users\', usersRouter) prefixes a whole router', 'A catch-all route after everything else serves 404s'], interviewTips: ['Design a RESTful router for a resource with nested child routes', 'Explain how you would version an API (/api/v1)'] },
  { topic: 'Express Middleware', title: 'Express Middleware in Depth', content: 'Middleware is a function(req, res, next) that can inspect the request, mutate it, end the response or call next(). Global middleware (logging, CORS, body parsing) runs for all routes; router- and route-level middleware narrows the scope. Errors forwarded via next(err) skip to the error-handling middleware.', keyPoints: ['next() without arguments continues; next(err) jumps to error handling', 'Common built-ins: express.json, express.static, express.urlencoded', 'Third-party: cors, helmet, morgan, multer, express-rate-limit', 'Middleware can terminate the chain by sending a response'], interviewTips: ['Write an auth middleware that verifies a JWT and attaches req.user', 'Explain why helmet and rate limiting belong at the top of the stack'] },
  { topic: 'REST API Design', title: 'REST API Design Principles', content: 'REST models resources as URLs manipulated through HTTP verbs: GET is safe and idempotent, PUT and DELETE are idempotent, POST is not. Return meaningful status codes (200/201/204, 400/401/403/404/409/500), keep responses consistent, version the API, and document it (OpenAPI).', keyPoints: ['Nouns in paths (/users/42), verbs in the HTTP method', 'Pagination, filtering and sorting via query parameters', '201 + Location header for creation; 204 for no content', 'Consistent error envelope: { success, message, errors }'], interviewTips: ['Design an endpoint set for an orders resource including nested items', 'Explain idempotency and why PUT is safer to retry than POST'] },
  { topic: 'MongoDB Basics', title: 'MongoDB Basics', content: 'MongoDB stores BSON documents in collections inside databases. There is no fixed schema, so documents in one collection may differ, and related data can be embedded or referenced. The aggregation pipeline transforms documents through stages such as $match, $group and $sort.', keyPoints: ['Document model: JSON-like, flexible schema', 'Embed for read-together data, reference for large or shared data', 'Index anything queried often; _id is indexed by default', 'Aggregation stages: $match, $group, $project, $lookup, $sort, $limit'], interviewTips: ['Explain embedding vs referencing with a blog-posts example', 'Write a $group aggregation to count orders per user'] },
  { topic: 'Mongoose', title: 'Mongoose ODM', content: 'Mongoose adds schema enforcement, validation, casting, middleware (hooks) and population to MongoDB. Schemas define fields, types, defaults and validators; models provide the query API (find, findOne, create, updateOne, deleteOne). Virtuals compute derived fields, and populate resolves references.', keyPoints: ['Schema validates shape; strict mode drops unknown fields', 'Pre/post hooks run on save, validate and delete', 'populate replaces ObjectId references with documents', 'timestamps: true adds createdAt and updatedAt'], interviewTips: ['Show a schema with validation, unique index and a pre-save hook', 'Explain the N+1 cost of populate in a loop'] },
  { topic: 'MongoDB Indexing', title: 'MongoDB Indexing and Query Performance', content: 'Indexes are B-tree structures that let MongoDB find documents without scanning the collection. Types include single-field, compound (ordered by ESR: Equality, Sort, Range), multikey for arrays, text and geospatial. explain("executionStats") shows whether a query used COLLSCAN or IXSCAN.', keyPoints: ['Every index slows writes and costs RAM - index what you query', 'Compound index order matters: equality fields first, then sort, then range', 'Covered queries read only the index and skip documents entirely', 'The _id index exists by default and is unique'], interviewTips: ['Design a compound index for a filter+sort query and justify the field order', 'Explain why a leading-wildcard regex cannot use an index'] },
  { topic: 'Authentication with JWT', title: 'JWT Authentication Flow', content: 'A JWT has a header, a payload of claims and a signature. The server signs it with a secret (HS256) or private key (RS256) and the client sends it in the Authorization: Bearer header. Access tokens should be short-lived and paired with a refresh token flow; revocation requires a server-side denylist.', keyPoints: ['JWTs are signed, not encrypted - never store secrets in the payload', 'Stateless auth scales but needs short expiry or a denylist for revocation', 'Store tokens in httpOnly cookies or memory, not localStorage (XSS risk)', 'Hash passwords with bcrypt or argon2 and per-user salts'], interviewTips: ['Compare JWT with server sessions and name the trade-offs', 'Explain an access plus refresh token rotation flow'] },
  { topic: 'Error Handling in Express', title: 'Centralised Error Handling in Express', content: 'Async route errors must reach next(err) - via try/catch or an asyncHandler wrapper (Express 4 does not catch rejected promises). A final error middleware with four arguments logs the error and returns one consistent JSON shape. Custom ApiError classes carry status codes so handlers stay clean.', keyPoints: ['An unhandled async error in Express 4 leaves the request hanging', 'Register exactly one error-handling middleware, last', 'Consistent shape: { success: false, message, errors }', 'Log the stack server-side; hide internals from clients in production'], interviewTips: ['Write an asyncHandler wrapper and the final error middleware from memory', 'Explain how you separate validation errors from unexpected errors'] },
  { topic: 'MERN Deployment', title: 'MERN Deployment and Production Readiness', content: 'Deploy the React build output on a static host or CDN and the Express API on a Node host (Render, Railway, AWS, Docker). Keep secrets in environment variables, enable HTTPS, restrict CORS to real origins, compress responses, run under PM2 or containers, and add health checks with aggregated logging.', keyPoints: ['Never commit .env; inject secrets from the platform', 'Serve the frontend build from nginx/CDN with the API behind a reverse proxy', 'NODE_ENV=production enables faster Express behaviour', 'Expose /health, monitor uptime and aggregate logs'], interviewTips: ['Walk through your pipeline from git push to production', 'Explain how database migrations run safely during deploys'] },
];