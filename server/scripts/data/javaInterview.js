// Java Interview Questions - one detailed question per Java topic
module.exports = [
  { topic: 'Java Introduction', question: 'Why is Java called platform independent, and where does it stop being platform independent?', answer: 'Source code compiles to bytecode, and that bytecode is identical on every machine, so one .class file runs anywhere a JVM exists. The JVM itself is platform dependent because it is built per OS, so portability is achieved above the JVM.', explanation: 'Interviewers want the nuance: bytecode is portable, the runtime is not.', difficulty: 'easy', followUps: ['What is the role of the class loader?', 'How does JIT improve on pure interpretation?'] },
  { topic: 'JVM JDK JRE', question: 'Explain the JVM memory layout and what happens to each area during GC.', answer: 'Per-thread areas are the PC register, JVM stack and native method stack. Shared areas are the heap for objects and metaspace for class metadata. GC works on the heap: minor GC reclaims the young generation and promotes survivors, while major GC compacts the old generation and causes longer stop-the-world pauses.', explanation: 'A structured answer shows you understand threading and GC behaviour together.', difficulty: 'medium', followUps: ['Why was PermGen replaced by Metaspace?', 'What is a stop-the-world pause?'] },
  { topic: 'Data Types and Variables', question: 'What is the difference between primitive types and wrappers, and what does autoboxing cost?', answer: 'Primitives store values directly and are fast but cannot go into collections; wrappers are heap objects with utility methods. Autoboxing wraps a primitive automatically, which adds allocation pressure and can throw NullPointerException when a null wrapper is unboxed.', explanation: 'Mentioning the Integer cache (-128 to 127) shows depth about == on wrappers.', difficulty: 'medium', followUps: ['Why is Integer.valueOf(127) == Integer.valueOf(127) true?', 'How does autoboxing slow down a hot loop?'] },
  { topic: 'Operators', question: 'Why should Strings never be compared with == and what is the correct approach?', answer: 'The == operator compares references, so it is true only when both variables point to the same object. Literals share the constant pool, but new String() and runtime concatenation create distinct objects. Use equals() for content comparison and equalsIgnoreCase when case does not matter.', explanation: 'This is a filter question in most Java interviews.', difficulty: 'easy', followUps: ['What does intern() do?', 'How is hashCode() computed for a String?'] },
  { topic: 'Control Flow', question: 'When should you prefer switch over an if-else chain?', answer: 'switch is clearer and faster for equality tests against many constants because the compiler can emit a jump table for integral types. Use if-else when conditions are ranges or involve method calls. Modern switch expressions also return values and use arrow labels to avoid fall-through.', explanation: 'The jump-table detail separates senior answers from junior ones.', difficulty: 'easy', followUps: ['Which types are allowed in switch?', 'How does pattern matching for switch change this?'] },
  { topic: 'Arrays', question: 'Why can arrays outperform ArrayList for index access, and when is ArrayList better?', answer: 'Both offer O(1) index access, but arrays avoid object header indirection, method call overhead and boxing for primitives. ArrayList wins when the size changes dynamically or you need the collection API. Arrays remain the right tool for fixed-size, performance-critical buffers.', explanation: 'Interviewers test the real trade-off, not a slogan.', difficulty: 'medium', followUps: ['How does ArrayList grow?', 'What is the cost of System.arraycopy?'] },
  { topic: 'Strings', question: 'Prove that String is immutable and explain why it matters for security.', answer: 'Methods such as toUpperCase return a new object and internal char arrays are copied, so the original never changes. It matters because file paths, class names and connection URLs are validated as Strings; if they could mutate after validation an attacker could bypass the check. Immutability also makes Strings safe to share across threads and reliable as map keys.', explanation: 'A concrete security example makes the answer memorable.', difficulty: 'medium', followUps: ['Is String really immutable if reflection is used?', 'Why should passwords be char[] instead of String?'] },
  { topic: 'OOP Concepts', question: 'Describe the four pillars of OOP using a single class of your choice.', answer: 'Take BankAccount: encapsulation keeps balance private behind validating deposit and withdraw methods, abstraction hides ledger logic behind those methods, inheritance lets SavingsAccount reuse the balance behaviour, and polymorphism lets each subclass override calculateInterest while callers hold a BankAccount reference.', explanation: 'One coherent example beats four disconnected definitions.', difficulty: 'medium', followUps: ['Where does composition beat inheritance?', 'What is the Liskov substitution principle?'] },
{ topic: 'Inheritance', question: 'What is the diamond problem and how does Java avoid it?', answer: 'If a class inherited from two parents that both define the same method, the compiler could not choose an implementation. Java permits only one superclass, so the ambiguity cannot arise. Interfaces can be implemented freely; since Java 8 conflicting default methods must be resolved by explicitly overriding the method in the implementing class.', explanation: 'Knowing the Java 8 default-method rule proves up-to-date knowledge.', difficulty: 'hard', followUps: ['How do you resolve two conflicting default methods?', 'Why is composition often preferred to inheritance?'] },
  { topic: 'Polymorphism', question: 'How does the JVM dispatch an overridden method call at runtime?', answer: 'For non-final, non-static instance methods the compiler emits invokevirtual. The JVM then resolves the method in the virtual method table of the actual object class rather than the declared reference type, so the most specific implementation runs.', explanation: 'Naming invokevirtual and the vtable is the expected depth.', difficulty: 'hard', followUps: ['What is invokespecial used for?', 'Why can private methods not be overridden?'] },
  { topic: 'Abstraction and Interfaces', question: 'When do you choose an abstract class over an interface in real design?', answer: 'Choose an abstract class when subclasses must share state, constructors or protected helpers and you own the hierarchy. Choose an interface when you are defining a capability that unrelated classes should implement, or when you need multiple inheritance of type. Interfaces with default methods now cover many cases that once required abstract classes.', explanation: 'State sharing is the decisive technical difference.', difficulty: 'medium', followUps: ['Why can an interface not have a constructor?', 'What problem do default methods solve for library authors?'] },
  { topic: 'Encapsulation', question: 'How do access modifiers help you design a maintainable API?', answer: 'Keep fields private so no caller can violate invariants, expose only the methods that form the public contract, use protected only for genuine extension points and default access for package-internal collaboration. The result is a small blast radius for change and the freedom to refactor internals without breaking callers.', explanation: 'Frame modifiers as API design tools, not just visibility rules.', difficulty: 'easy', followUps: ['What is the difference between protected and default?', 'How does immutability relate to encapsulation?'] },
  { topic: 'Exception Handling', question: 'When do you use a checked exception and when an unchecked one?', answer: 'Use a checked exception for a recoverable condition the caller can plausibly handle, such as a missing file or a failed remote call, so the compiler forces the decision. Use an unchecked exception for programming errors and precondition violations that should fail fast, such as illegal arguments or invalid state.', explanation: 'Tie the choice to caller responsibility, which is the underlying rule.', difficulty: 'medium', followUps: ['What is exception chaining?', 'Why is catching Throwable dangerous?'] },
  { topic: 'Collections Framework', question: 'How do you choose between List, Set, Queue and Map for a requirement?', answer: 'Use List when order and duplicates matter, Set when uniqueness matters, Queue or Deque for FIFO/LIFO processing, and Map for key lookup. Then refine: ArrayList for indexed access, ArrayDeque for queues, HashSet for speed, TreeSet or TreeMap for sorted iteration, LinkedHashSet for predictable order and ConcurrentHashMap for concurrency.', explanation: 'A decision-tree answer shows practical judgement.', difficulty: 'medium', followUps: ['HashMap vs LinkedHashMap?', 'When is CopyOnWriteArrayList appropriate?'] },
  { topic: 'ArrayList vs LinkedList', question: 'Why does LinkedList perform worse than ArrayList even for middle insertions?', answer: 'LinkedList must still traverse O(n) nodes to reach the position, each node adds memory overhead and pointer chasing ruins cache locality. ArrayList performs one O(n) arraycopy, which is a fast bulk memory move. LinkedList only wins when you already hold a node reference, such as an Iterator cursor, or when you need Deque semantics.', explanation: 'Cache locality is the detail most candidates miss.', difficulty: 'hard', followUps: ['How does ListIterator.remove() avoid traversal?', 'Which list would you use for a very large queue?'] },
  { topic: 'HashMap', question: 'Explain what happens internally when put() is called on a HashMap.', answer: 'The key hash is spread, the bucket index is computed as (n-1) & hash, and the chain is scanned with equals(). A matching key replaces the value, otherwise a node is appended or inserted into the tree. When size exceeds capacity times 0.75 the table doubles and rehashes, and a chain beyond 8 nodes with capacity at least 64 is treeified.', explanation: 'Naming the resize and treeify thresholds marks a strong answer.', difficulty: 'hard', followUps: ['Why is capacity always a power of two?', 'How does ConcurrentHashMap avoid a global lock?'] },
{ topic: 'Generics', question: 'What is type erasure and what limitations does it create?', answer: 'The compiler checks generic types and then erases them, so at runtime a List<String> is just a List. Limits: primitives cannot be type arguments, generic arrays cannot be created, generic exception types cannot be caught, instanceof cannot use a parameterised type, and overloads cannot differ only in type arguments.', explanation: 'Listing concrete limitations shows the concept is understood.', difficulty: 'hard', followUps: ['What are bridge methods?', 'Why does erasure complicate frameworks such as Jackson?'] },
  { topic: 'Multithreading', question: 'How do you size a thread pool for an IO-bound service?', answer: 'IO-bound tasks block, so the pool must exceed the core count; a common start is cores multiplied by a factor based on the wait-to-service time ratio. Bound the queue, choose an explicit rejection policy and monitor active threads plus queue depth. For CPU-bound work keep the pool close to the core count to avoid context switching.', explanation: 'Queue bounds and rejection policy signal production experience.', difficulty: 'hard', followUps: ['What rejection policies does ThreadPoolExecutor provide?', 'How do virtual threads change this calculation?'] },
  { topic: 'Thread Lifecycle', question: 'Describe the thread lifecycle and give an API causing each transition.', answer: 'NEW after construction, RUNNABLE after start(), BLOCKED while waiting for a synchronized monitor, WAITING after wait() or join() without timeout, TIMED_WAITING after sleep(), wait(ms) or join(ms), and TERMINATED when run() finishes or an uncaught exception escapes.', explanation: 'Pairing states with APIs proves you can debug concurrency problems.', difficulty: 'medium', followUps: ['How do you diagnose a BLOCKED thread in production?', 'Why is a thread dump useful?'] },
  { topic: 'Synchronization', question: 'How do you prevent a deadlock in a multi-lock code path?', answer: 'Establish a global lock ordering and always acquire locks in that order so no circular wait can form. Alternatively use tryLock with a timeout and back off, reduce lock scope, use one coarse lock where contention is low, or prefer lock-free structures such as ConcurrentHashMap and atomics.', explanation: 'Lock ordering plus timed tryLock is the pragmatic answer.', difficulty: 'hard', followUps: ['How does jstack detect deadlock?', 'What is lock striping?'] },
  { topic: 'Lambda Expressions', question: 'How do lambdas change the way you write comparators and callbacks?', answer: 'A lambda replaces an anonymous inner class with one expression, so Comparator.comparing(Person::getName).thenComparingInt(Person::getAge) replaces many boilerplate lines. Lambdas make higher-order APIs natural, because passing behaviour becomes shorter than writing a loop, and method references remove even the parameter list.', explanation: 'Mention type inference and effectively final capture to show the model behind it.', difficulty: 'easy', followUps: ['What are the four forms of method reference?', 'Why must captured locals be effectively final?'] },
  { topic: 'Streams API', question: 'How do you process a very large collection without loading it twice into memory?', answer: 'Build a lazy pipeline: filter and map are intermediate and do nothing until a terminal operation runs, and each element flows through every stage before the next element is read. For files use Files.lines inside try-with-resources so the source is streamed and closed, then collect with a bounded collector to keep peak memory low.', explanation: 'Emphasising laziness shows understanding beyond API memorisation.', difficulty: 'hard', followUps: ['What does short-circuiting mean in streams?', 'When should you avoid parallelStream()?'] },
  { topic: 'File I/O', question: 'How do you write to a shared file without corrupting it for readers?', answer: 'Write the full content to a temporary file in the same directory, flush and fsync it, then atomically move it into place with Files.move using ATOMIC_MOVE. Readers therefore never observe a half-written file, and a FileChannel lock handles multiple writer processes.', explanation: 'Temp file plus atomic rename is the standard production pattern.', difficulty: 'hard', followUps: ['What does fsync guarantee?', 'How does atomic rename behave on Windows versus POSIX?'] },
  { topic: 'Garbage Collection', question: 'What causes a memory leak in a garbage-collected language such as Java?', answer: 'A leak happens when objects stay reachable but are no longer useful: an unbounded static cache, a listener that is never deregistered, a ThreadLocal in a pooled thread, ClassLoader references in an application server or an unclosed resource. Because they are reachable they are never collected, so the heap fills and Full GC frequency climbs.', explanation: 'Add the diagnosis method: heap dumps and dominator tree analysis.', difficulty: 'hard', followUps: ['How do you find the leak with jmap or a profiler?', 'Why do ThreadLocals leak in thread pools?'] },
];