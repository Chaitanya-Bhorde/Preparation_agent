// System Design Interview Questions - one detailed question per System Design topic
module.exports = [
  { topic: 'System Design Introduction', question: 'Walk me through how you would start a system design interview.', answer: 'Clarify functional requirements (what features, which are out of scope), then non-functional ones (users, read/write ratio, latency and availability targets, consistency needs). Do back-of-envelope estimates for storage and QPS, draw the high-level architecture, then deep-dive into the components the interviewer cares about while calling out trade-offs explicitly.', explanation: 'Interviewers score structure and trade-off reasoning, not the final diagram.', difficulty: 'easy', followUps: ['What numbers do you memorise for estimates?', 'How do you know when to stop drilling deeper?'] },
  { topic: 'Scalability', question: 'How do you make a monolithic web application scalable as traffic grows?', answer: 'In order of usual impact: add a load balancer and run stateless app servers, move session state to Redis, add read replicas and a cache layer in front of the database, offload static assets to a CDN, then split hot functionality into separate services and shard the database only when other options are exhausted.', explanation: 'The ordering matters: caching and replicas are cheaper than sharding.', difficulty: 'medium', followUps: ['Why keep servers stateless?', 'When is sharding actually required?'] },
  { topic: 'Load Balancing', question: 'Compare L4 and L7 load balancing and where you would use each.', answer: 'L4 balancers route on IP and port, are extremely fast and protocol-agnostic, and suit TCP traffic or simple HTTP pass-through. L7 balancers parse HTTP and can route by path, host or headers, terminate TLS, compress and stick sessions; they cost more CPU but enable content-based routing and API gateways. Large systems often chain both: L4 in front for DDoS absorption and L7 for routing.', explanation: 'Mentioning the chaining pattern shows production experience.', difficulty: 'medium', followUps: ['How do health checks work?', 'What is connection draining?'] },
  { topic: 'Caching', question: 'Design the caching strategy for a product catalog API.', answer: 'Use cache-aside with Redis: check cache, on miss read Postgres and populate with a TTL. Version or key the cache by category and page so invalidation is granular. Add jittered TTLs and request coalescing to prevent stampedes on hot keys, use stale-while-revalidate for graceful degradation, and monitor hit ratio with a target above 90 percent before considering more replicas.', explanation: 'The hit-ratio target turns the answer from theory into engineering.', difficulty: 'hard', followUps: ['How would you invalidate on product update?', 'When is write-through worth it?'] },
  { topic: 'CDN', question: 'How would you serve user-uploaded images globally at low latency?', answer: 'Store originals in object storage (S3) as the source of truth, generate resized variants with an image pipeline, and serve all variants through a CDN with long max-age and content-hashed URLs so deploys bust the cache. For private images use signed URLs with short expiry so authorization stays server-side while bytes come from the edge.', explanation: 'Source of truth plus hashed URLs plus signed access is the full pattern.', difficulty: 'medium', followUps: ['How do you purge a bad image?', 'Where does image optimisation happen?'] },
  { topic: 'Database Sharding', question: 'When and how would you shard a database?', answer: 'Shard when a single node cannot handle the write volume or working set despite indexing, caching and replicas. Choose a shard key with high cardinality and even access distribution, such as tenant_id or hashed user_id, avoid keys that create hotspots, and plan resharding with a directory or consistent hashing up front. Accept that cross-shard queries and transactions become expensive, so co-locate related data on the same shard.', explanation: 'Shard-key choice is the make-or-break decision they want to hear.', difficulty: 'hard', followUps: ['How do you reshard without downtime?', 'What happens to secondary indexes?'] },
  { topic: 'Database Replication', question: 'Explain replication modes and how you route reads with replicas.', answer: 'Synchronous replication waits for replica acknowledgement, giving strong durability at the cost of write latency; asynchronous is faster but can lose recent writes on failover. Route strongly consistent reads (or read-your-own-writes) to the primary, and send tolerant reads to replicas while monitoring replication lag; if lag exceeds a threshold, fail back to the primary for consistency-sensitive pages.', explanation: 'Lag-aware read routing is the practical detail being tested.', difficulty: 'medium', followUps: ['How do you detect lag?', 'What is quorum replication?'] },
  { topic: 'CAP Theorem', question: 'How does CAP influence real architecture decisions?', answer: 'CAP says that during a partition you must pick consistency or availability. For checkout and payments choose CP because wrong balances are unacceptable; for social feeds or product views choose AP because stale content is tolerable but downtime is not. In normal operation both properties hold, so design partition behaviour explicitly and document what each endpoint degrades to.', explanation: 'Mapping consistency needs per endpoint is the expected maturity level.', difficulty: 'hard', followUps: ['What is PACELC?', 'Give an example of an AP and a CP store.'] },
  { topic: 'Consistency Models', question: 'Which consistency model would you pick for a shopping cart and why?', answer: 'Read-your-writes consistency at minimum: users must immediately see items they added, achieved by pinning cart reads and writes to the same primary or session-sticky route. Between users, eventual consistency is fine because two people rarely modify one cart. Critical paths like stock decrement stay strongly consistent to prevent overselling.', explanation: 'Per-operation consistency choice is the senior-level answer.', difficulty: 'hard', followUps: ['How do you implement session pinning?', 'What conflicts can eventual consistency create here?'] },
  { topic: 'SQL vs NoSQL', question: 'For a new e-commerce platform, how do you decide between SQL and NoSQL per component?', answer: 'Use PostgreSQL for orders, payments and inventory where ACID transactions and relational integrity are mandatory. Use a document store for the product catalog where attributes vary per category and reads are key-based. Add Redis for carts and sessions, and Elasticsearch for search. This polyglot approach matches each workload to the store whose guarantees it actually needs.', explanation: 'Mapping each domain to a store with reasons is the complete answer.', difficulty: 'medium', followUps: ['How do you keep catalog and search in sync?', 'What about reporting?'] },
  { topic: 'Message Queues', question: 'Design order processing with a message queue and handle failures.', answer: 'On checkout, persist the order and publish OrderCreated, then consumers handle payment, inventory and notification independently with retries. Make each consumer idempotent using an idempotency key or processed-message table, configure exponential backoff, and route repeatedly failing messages to a DLQ with alerting. This decouples steps so a slow email provider never blocks checkout.', explanation: 'Idempotency plus DLQ is the core of any queue design answer.', difficulty: 'hard', followUps: ['How do you guarantee ordering per order?', 'When would you use Kafka over RabbitMQ?'] },
  { topic: 'Microservices', question: 'When would you split a monolith into microservices, and how?', answer: 'Split only when independent deployability, team autonomy or different scaling profiles justify the distributed-system cost. Extract service boundaries along business capabilities (auth, orders, catalog), give each its own database, replace direct calls with well-defined APIs and events, and add the platform pieces needed: API gateway, service discovery, centralised logging, tracing and CI/CD per service.', explanation: 'Justifying with team and scaling needs shows judgement over fashion.', difficulty: 'medium', followUps: ['What is the strangler fig pattern?', 'How do sagas replace distributed transactions?'] },
  { topic: 'API Gateway', question: 'What responsibilities belong in an API gateway and what must stay out?', answer: 'In: authentication and token validation, rate limiting, request routing, response shaping, TLS termination, caching simple responses and request logging. Out: business logic and data transformations that belong to services, because a fat gateway becomes an untestable monolith and a deployment bottleneck. Keep the gateway stateless and horizontally scalable behind an L4 balancer.', explanation: 'The in/out split and statelessness requirement is what they probe.', difficulty: 'medium', followUps: ['How does the gateway authenticate JWTs?', 'What is backend-for-frontend?'] },
  { topic: 'Rate Limiting', question: 'Design rate limiting for a public API with millions of clients.', answer: 'Fix the dimension first: per API key or IP, per endpoint tier. Use a token bucket in Redis with atomic INCR/EXPIRE or a Lua script for sliding window precision, with counters sharded by key. Return 429 with Retry-After and limit headers, and enforce a two-layer design: a cheap edge limiter for coarse protection and a precise per-tenant limiter in the gateway.', explanation: 'Two-layer limiting balances cost and fairness at scale.', difficulty: 'hard', followUps: ['How does the algorithm behave across Redis failover?', 'Why token bucket over fixed window?'] },
  { topic: 'Horizontal vs Vertical Scaling', question: 'How would you scale a video transcoding service?', answer: 'Horizontally, because transcoding is embarrassingly parallel: workers pull jobs from a queue, each job is segmented into chunks transcoded in parallel, and autoscaling adds workers based on queue depth. Use spot instances to cut cost, checkpoint progress so preemption does not lose work, and keep the control plane (queue, metadata store) separate from the stateless workers.', explanation: 'Queue-driven autoscaling is the pattern they look for.', difficulty: 'medium', followUps: ['Why not vertical scaling here?', 'How do you handle a stuck worker?'] },
  { topic: 'Latency vs Throughput', question: 'A dashboard endpoint has p50 of 80ms but p99 of 4 seconds. How do you investigate?', answer: 'Percentile divergence means the tail is hitting something different: lock contention, GC pauses, cache misses that fall through to slow queries, or retries stacking up. Add distributed tracing with span timings, check database slow-query logs for the tail requests, look at connection pool saturation and hot-key cache misses, then fix the specific tail cause rather than raising timeouts.', explanation: 'Naming likely tail causes and the tracing method is the expected answer.', difficulty: 'hard', followUps: ['Why do retries worsen tail latency?', 'What is hedged request?'] },
];