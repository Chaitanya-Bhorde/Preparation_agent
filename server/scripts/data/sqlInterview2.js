// SQL Interview Questions Part 2 - joins, subqueries, constraints, advanced objects
module.exports = [
  { topic: 'INNER JOIN', question: 'How does the optimiser execute an INNER JOIN?', answer: 'It chooses among nested loop join (good when the inner side has a usable index and few rows), hash join (build a hash table on the smaller input, then probe - ideal for large unsorted sets) and merge join (both inputs sorted on the join key). Statistics, cardinality estimates and available indexes drive the choice, which is why stale statistics cause bad plans.', explanation: 'Naming the three physical join algorithms and when each wins is the expected depth.', difficulty: 'hard', followUps: ['Why is a hash join unsafe for non-equality joins?', 'How do you read a query plan to detect a bad join order?'] },
  { topic: 'LEFT JOIN', question: 'Why does adding a right-table condition in WHERE break a LEFT JOIN?', answer: 'The WHERE clause runs after the join, so rows where the right side is NULL are removed by a predicate such as WHERE b.status = "active", effectively converting the outer join into an inner join. Put the condition in the ON clause (or handle NULL explicitly) to keep the unmatched left rows.', explanation: 'This is one of the most common real-world SQL bugs.', difficulty: 'medium', followUps: ['What is the difference between a condition in ON versus WHERE?', 'How do you find left rows with no match?'] },
  { topic: 'RIGHT JOIN', question: 'When would you use a RIGHT JOIN instead of swapping the tables in a LEFT JOIN?', answer: 'Semantically they are equivalent, and readability usually favours LEFT JOIN with the "main" table first, which keeps the intent obvious and the ON clause adjacent to the preserved table. RIGHT JOIN is occasionally clearer when a query is built from a shared FROM clause by adding tables on the left. Consistency within a codebase matters more than the operator choice.', explanation: 'The answer should be about readability, not capability.', difficulty: 'easy', followUps: ['Does the optimiser treat them identically?', 'How do you refactor a RIGHT JOIN to a LEFT JOIN?'] },
  { topic: 'FULL JOIN', question: 'How do you emulate a FULL OUTER JOIN in MySQL?', answer: 'MySQL has no FULL OUTER JOIN, so combine a LEFT JOIN and a RIGHT JOIN with UNION, or union the two anti-joins (left-only rows and right-only rows) when you want just the mismatches. UNION removes the duplicated matched rows that both halves produce.', explanation: 'The UNION approach plus the anti-join variant covers the interviewer\'s follow-up.', difficulty: 'hard', followUps: ['Why is UNION rather than UNION ALL correct here?', 'How would you find rows missing on both sides in one query?'] },
  { topic: 'Self Join', question: 'Give a practical use of a self join.', answer: 'A self join compares rows of the same table: finding employees whose salary exceeds their manager\'s, pairs of employees in the same department, or the transitive closure of a hierarchy. Use distinct aliases for each role, and index the join column (for example manager_id) so the join does not scan the table repeatedly.', explanation: 'The employee-manager example is the standard and shows you can reason about aliases.', difficulty: 'medium', followUps: ['How do you find the top manager recursively?', 'How does a self join differ from a correlated subquery here?'] },
  { topic: 'Subqueries', question: 'Compare a subquery with a join for the same logical task.', answer: 'A join combines row sets and can duplicate rows when the relationship is one-to-many, so you may need DISTINCT; a subquery with IN or EXISTS expresses "has a related row" without duplicating the outer rows. Modern optimisers often rewrite one into the other, so the real criteria are correctness, readability and whether a semi-join is what you mean.', explanation: 'Framing it as semi-join semantics shows conceptual clarity.', difficulty: 'medium', followUps: ['What is a semi-join?', 'Which performs better when the subquery returns many rows?'] },
  { topic: 'Correlated Subqueries', question: 'How does a correlated subquery work and why can it be slow?', answer: 'A correlated subquery references a column of the outer query, so logically it is re-evaluated for each outer row, giving O(n*m) work with block nested loops. Rewrite it as a join, a derived table with GROUP BY, or a window function so the work is done in a single pass over the data.', explanation: 'Showing the rewrite is what proves the concept.', difficulty: 'hard', followUps: ['How does EXISTS stop early for a correlated subquery?', 'How do you rewrite a running-total correlated subquery as a window function?'] },
  { topic: 'UNION', question: 'What is the difference between UNION and UNION ALL, and what does UNION cost?', answer: 'UNION removes duplicate rows, which requires a sort or hash of the full result, while UNION ALL simply concatenates the inputs and is much cheaper. Column count, order and compatible types must match in both cases. Because UNION deduplicates, prefer UNION ALL unless duplicates must be removed.', explanation: 'Performance-first reasoning is what the interviewer is checking.', difficulty: 'easy', followUps: ['How does UNION interact with ORDER BY?', 'When is UNION ALL logically equivalent to UNION?'] },
  { topic: 'EXISTS', question: 'Why is EXISTS often better than IN for large subquery results?', answer: 'EXISTS is a semi-join: it stops as soon as one matching row is found and it handles NULLs safely. IN must materialise or hash the whole subquery result and behaves surprisingly when the list contains NULL (NOT IN with a NULL returns no rows at all). EXISTS also expresses intent more directly for "has at least one related row".', explanation: 'The NOT IN plus NULL trap is the follow-up you should raise yourself.', difficulty: 'medium', followUps: ['Why does NOT IN return no rows when NULL is present?', 'How do you write NOT EXISTS safely?'] },
];