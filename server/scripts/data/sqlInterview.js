// SQL Interview Questions Part 1 - fundamentals, DDL/DML, querying
module.exports = [
  { topic: 'SQL Introduction', question: 'What is SQL and how is it classified into DDL, DML, DCL and TCL?', answer: 'SQL (Structured Query Language) is the standard declarative language for relational databases. DDL defines structure (CREATE, ALTER, DROP, TRUNCATE), DML changes data (INSERT, UPDATE, DELETE), DQL retrieves data (SELECT), DCL controls permissions (GRANT, REVOKE) and TCL manages transactions (COMMIT, ROLLBACK, SAVEPOINT).', explanation: 'Interviewers want the categories mapped to statements, not a vague definition.', difficulty: 'easy', followUps: ['Which category does MERGE belong to?', 'Why is SELECT sometimes called DQL instead of DML?'] },
  { topic: 'DDL', question: 'How does DDL differ from DML in terms of transactions?', answer: 'DDL statements are auto-committed in most databases, so CREATE, ALTER and DROP cannot be rolled back in MySQL and Oracle. DML statements (INSERT, UPDATE, DELETE) participate in a transaction and can be rolled back until COMMIT. This is why schema changes must be applied carefully with backups and migration scripts.', explanation: 'The auto-commit rule is the practically important difference.', difficulty: 'medium', followUps: ['Can TRUNCATE be rolled back?', 'How do migration tools handle failed DDL in production?'] },
  { topic: 'DML', question: 'What is the difference between DELETE, TRUNCATE and DROP?', answer: 'DELETE is DML: it removes selected rows, fires triggers, writes each row to the log and can be rolled back. TRUNCATE is DDL: it deallocates all pages quickly, resets identity counters, does not fire row triggers and usually cannot be rolled back. DROP removes the table definition and its data entirely.', explanation: 'Classic three-way comparison question asked in nearly every SQL round.', difficulty: 'easy', followUps: ['Does TRUNCATE require the DROP privilege?', 'How would you delete all rows while keeping the identity value?'] },
  { topic: 'DQL', question: 'Explain the logical order in which a SELECT statement is evaluated.', answer: 'FROM and JOIN build the row set, WHERE filters rows, GROUP BY groups them, aggregate functions are computed, HAVING filters groups, SELECT computes the projection, DISTINCT removes duplicates, ORDER BY sorts, and LIMIT/OFFSET trims the result. Because of this order, column aliases from SELECT are visible to ORDER BY but not to WHERE.', explanation: 'Explaining the order justifies why aliases fail in WHERE and why HAVING exists.', difficulty: 'medium', followUps: ['Why can you not use an aggregate in WHERE?', 'Where is a window function evaluated?'] },
  { topic: 'DCL', question: 'How do GRANT and REVOKE implement database security?', answer: 'GRANT gives a privilege on an object to a user or role, for example GRANT SELECT, INSERT ON sales.orders TO analyst_role, optionally with GRANT OPTION to let the grantee pass it on. REVOKE removes it, and CASCADE also removes privileges that the grantee propagated. Roles bundle privileges so they can be assigned consistently.', explanation: 'Mention roles and least privilege to show security awareness.', difficulty: 'medium', followUps: ['What does WITH GRANT OPTION allow?', 'How do you audit effective privileges?'] },
  { topic: 'TCL', question: 'How do COMMIT, ROLLBACK and SAVEPOINT work together?', answer: 'COMMIT makes all changes of the current transaction durable and visible. ROLLBACK discards them and returns the database to the last commit. SAVEPOINT marks an intermediate point so ROLLBACK TO SAVEPOINT can undo only part of a transaction, which is useful for retrying one step without redoing the whole batch.', explanation: 'Savepoint retry logic is the practical detail interviewers like.', difficulty: 'medium', followUps: ['What is autocommit mode?', 'How does SET TRANSACTION ISOLATION LEVEL interact with TCL?'] },
  { topic: 'CREATE', question: 'How do you design a CREATE TABLE statement for a production table?', answer: 'Choose appropriate types (narrowest correct type, DECIMAL for money, no FLOAT), declare a primary key, add NOT NULL and DEFAULT values, add foreign keys with explicit ON DELETE behaviour, add CHECK constraints for domain rules, add UNIQUE constraints on natural keys and indexes for the columns you filter on. Also define the storage engine and character set explicitly.', explanation: 'The answer should read like a checklist, not just syntax.', difficulty: 'medium', followUps: ['Why avoid FLOAT for currency?', 'How do you choose column order in a composite index?'] },
  { topic: 'ALTER', question: 'What are the risks of ALTER TABLE on a large production table and how do you mitigate them?', answer: 'Some alterations rewrite the table and hold locks: adding a column with a default, changing a column type, adding a NOT NULL constraint or adding an index. Mitigate by checking whether the engine supports an INSTANT or INPLACE algorithm, running the change in a maintenance window, using online schema-change tools such as gh-ost or pt-online-schema-change, and always taking a backup first.', explanation: 'Showing awareness of online DDL tools proves production experience.', difficulty: 'hard', followUps: ['Which ALTER operations are metadata-only in MySQL 8?', 'How does a replica lag during a big ALTER?'] },
  { topic: 'DROP', question: 'When do you use DROP TABLE ... CASCADE or RESTRICT?', answer: 'DROP TABLE removes the table, its data, indexes and triggers. With RESTRICT (the default in many engines) the drop fails if another table has a foreign key pointing to it. With CASCADE the dependent objects are dropped as well, which is convenient in development but dangerous in production because losing a parent can silently delete children.', explanation: 'The safety framing is what matters in the answer.', difficulty: 'medium', followUps: ['How do you find all dependent foreign keys?', 'What happens to views referencing a dropped table?'] },
];