// CNS Notes part 2 - fills the 20 topics not covered by cnsNotes.js
module.exports = [
  { topic: 'Network Basics', title: 'Networking Fundamentals', content: 'A network connects hosts so they can exchange data. Key vocabulary: nodes, links, bandwidth (bits per second), latency (delay), packet and protocol. Networks are classified by scale as PAN, LAN, MAN and WAN, and by topology as star, bus, ring and mesh.', keyPoints: ['Bandwidth is capacity, latency is delay - they are independent', 'LAN is a single site, WAN spans cities and continents', 'A protocol defines message format, order and error handling', 'Hosts are identified by IP, applications by ports'], interviewTips: ['Distinguish bandwidth vs latency vs throughput clearly', 'Mention that the Internet is a network of networks (autonomous systems)'] },
  { topic: 'OSI vs TCP/IP', title: 'OSI vs TCP/IP Model', content: 'OSI has seven layers (Physical, Data Link, Network, Transport, Session, Presentation, Application) and is a reference model. TCP/IP has four layers (Link, Internet, Transport, Application) and is the implemented model. OSI is granular and theory-driven while TCP/IP is pragmatic and drives the real Internet.', keyPoints: ['OSI: 7 layers, reference only', 'TCP/IP: 4 layers, implemented', 'OSI session and presentation map into the TCP/IP application layer', 'Encapsulation adds headers as data moves down the stack'], interviewTips: ['Be ready to convert an OSI layer into its TCP/IP equivalent', 'Give the protocol at each layer: HTTP, TCP, IP, Ethernet'] },
  { topic: 'Network Topologies', title: 'Network Topologies', content: 'Topology is the physical or logical arrangement of nodes. Star uses a central switch and is easy to manage but has a single point of failure. Ring passes a token for deterministic performance. Bus is cheap but collision prone. Mesh gives the highest redundancy at the highest cabling cost.', keyPoints: ['Star: central switch, single point of failure', 'Mesh: most fault tolerant, most cabling', 'Ring: deterministic, breaks if a link fails unless dual ring', 'Logical topology may differ from physical wiring'], interviewTips: ['Pair each topology with a real deployment such as data-centre leaf-spine', 'Mention that modern Ethernet is logically a star built on a switch'] },
  { topic: 'IPv4', title: 'IPv4 Addressing', content: 'IPv4 is a 32-bit address split into four octets written in dotted decimal. It has a network portion and a host portion decided by the subnet mask or CIDR prefix. Classes A/B/C historically fixed the split before CIDR, and private ranges (10/8, 172.16/12, 192.168/16) sit behind NAT.', keyPoints: ['32-bit, about 4.3 billion addresses', 'CIDR /24 means 24 network bits and 8 host bits', 'Private and link-local ranges are not Internet routable', 'Special addresses: 127.0.0.1 loopback, 255.255.255.255 broadcast'], interviewTips: ['Show a quick subnet calculation such as hosts in a /26', 'Mention NAT as the reason IPv4 survived so long'] },
  { topic: 'IPv6', title: 'IPv6 Addressing', content: 'IPv6 uses 128-bit hexadecimal addresses written as eight groups with :: compression for a single run of zero groups. It removes NAT and broadcast, adds a fixed 40-byte header with extension headers, stateless autoconfiguration (SLAAC) and mandatory IPsec support.', keyPoints: ['128-bit addresses, roughly 3.4 x 10^38 of them', 'No broadcast; multicast and anycast instead', 'Built-in SLAAC stateless autoconfiguration', 'Simpler fixed header speeds router processing'], interviewTips: ['Explain why the fixed header improves routing performance', 'Mention dual-stack and tunnelling as migration strategies'] },
  { topic: 'Subnetting', title: 'Subnetting and CIDR', content: 'Subnetting divides a network into smaller logical networks by borrowing host bits for the network portion. The mask and CIDR prefix describe the split; the network address has all host bits zero and the broadcast address has all host bits one, so usable hosts are 2^h - 2.', keyPoints: ['CIDR /n gives 2^(32-n) addresses and 2^(32-n) - 2 usable hosts', 'Mask bit 1 = network part, 0 = host part', 'VLSM sizes each subnet independently', 'Same subnet means direct delivery; otherwise go via the gateway'], interviewTips: ['Practise converting between mask, prefix and host ranges', 'Explain why a /31 point-to-point link has no broadcast address'] },
{ topic: 'MAC Address', title: 'MAC Addresses', content: 'A MAC address is a 48-bit hardware identifier burned into a NIC and written as six hex pairs. The first 24 bits identify the vendor (OUI) and the last 24 bits the device. It works at Layer 2 to deliver frames inside a LAN and is flat, so it cannot route between networks.', keyPoints: ['48-bit flat address such as 00:1A:2B:3C:4D:5E', 'First 3 bytes are the OUI vendor prefix', 'Used by switches to forward frames', 'Randomised for privacy in modern WiFi'], interviewTips: ['Explain MAC learning in a switch forwarding table', 'Mention ARP as the bridge from IP to MAC'] },
  { topic: 'ARP', title: 'Address Resolution Protocol', content: 'ARP maps an IPv4 address to a MAC address inside a LAN. The sender broadcasts "who has 192.168.1.5?" and the owner replies with its MAC by unicast. Results are cached with a timeout and gratuitous ARP announces address changes.', keyPoints: ['Broadcast request, unicast reply', 'ARP cache entries expire as hardware changes', 'Works only inside one broadcast domain', 'ARP spoofing is a common LAN attack'], interviewTips: ['Mention proxy ARP and ARP poisoning as follow-ups', 'Compare it with IPv6 neighbour discovery (NDP)'] },
  { topic: 'DHCP', title: 'Dynamic Host Configuration Protocol', content: 'DHCP assigns IP configuration automatically using four UDP messages: Discover (client broadcast), Offer (server proposes), Request (client accepts) and Acknowledge. Leases expire and are renewed at 50% and 87.5% of lease time, and relay agents forward requests across subnets.', keyPoints: ['DORA: Discover, Offer, Request, Acknowledge', 'UDP 67 on the server, UDP 68 on the client', 'Leases renew before expiry at T1 and T2 timers', 'DHCP relay carries requests between subnets'], interviewTips: ['Recite the DORA sequence without hesitation', 'Mention static reservations and scope exclusions'] },
  { topic: 'HTTPS', title: 'HTTPS and TLS', content: 'HTTPS is HTTP over TLS. The TLS handshake authenticates the server with a certificate, negotiates a cipher suite and derives a shared session key with asymmetric cryptography, after which traffic is encrypted symmetrically for speed. Certificates are validated against a trusted CA chain with a hostname match.', keyPoints: ['Confidentiality, integrity and authentication in one protocol', 'Asymmetric crypto for the handshake, symmetric for bulk data', 'TLS 1.3 removed RSA key exchange and saves a round trip', 'HSTS forces browsers to always use HTTPS'], interviewTips: ['Describe the handshake order: ClientHello, ServerHello, certificate, key exchange, finished', 'Explain forward secrecy provided by ECDHE'] },
  { topic: 'FTP', title: 'FTP and Its Modes', content: 'FTP transfers files between client and server using two TCP connections: a control channel on port 21 for commands and a separate data channel. In active mode the server connects back to the client (port 20) while passive mode has the client open the data connection, which works better through NAT and firewalls.', keyPoints: ['Control connection port 21, data connection dynamic or port 20', 'Active mode: server connects to client', 'Passive mode: client connects to server', 'Credentials travel in clear text, so prefer SFTP or FTPS'], interviewTips: ['Explain why passive mode is firewall friendly', 'Compare FTP with SFTP over SSH'] },
  { topic: 'SMTP', title: 'SMTP, POP3 and IMAP', content: 'SMTP sends mail and pushes it to the recipient mail server on TCP 25 (587 for submission with STARTTLS). Mail is then retrieved by the client with POP3 (110) which typically downloads and deletes, or IMAP (143) which keeps mail synchronised on the server across devices.', keyPoints: ['SMTP is push for sending, ports 25 and 587', 'POP3 downloads to one device, port 110', 'IMAP keeps server state in sync, port 143', 'MIME encodes attachments and multiple content types'], interviewTips: ['Distinguish clearly between sending and retrieving protocols', 'Mention SPF, DKIM and DMARC for spam protection'] },
  { topic: 'TCP vs UDP', title: 'TCP vs UDP Selection', content: 'TCP is connection oriented with a handshake, sequence numbers, acknowledgements, retransmission and congestion control, giving reliable ordered delivery with more overhead and latency. UDP is connectionless with no guarantees but minimal overhead, and it is preferred for DNS, VoIP, gaming and streaming where speed beats perfection.', keyPoints: ['TCP: reliable, ordered, flow and congestion controlled', 'UDP: unreliable, unordered, low latency', 'Header size: TCP 20+ bytes, UDP 8 bytes', 'HTTP/3 runs QUIC over UDP for lower latency'], interviewTips: ['Justify each choice with the application requirements', 'Explain how QUIC adds reliability on top of UDP'] },
  { topic: 'TCP 3-Way Handshake', title: 'TCP Handshake and Teardown', content: 'TCP opens a connection with SYN (client sequence x), SYN-ACK (server sequence y, ack x+1) and ACK (ack y+1), which agrees on both initial sequence numbers and a maximum segment size. Closing needs four steps: FIN, ACK, FIN, ACK, because each direction shuts down independently; TIME_WAIT lasts two maximum segment lifetimes.', keyPoints: ['SYN, SYN-ACK, ACK agree sequence numbers', 'Options negotiate MSS, window scaling and SACK', 'Teardown is four steps; either side may close first', 'TIME_WAIT prevents old duplicates being accepted'], interviewTips: ['Draw the sequence diagram with numbers', 'Explain why a SYN flood exhausts the backlog queue'] },
  { topic: 'Flow Control', title: 'Flow Control and Sliding Window', content: 'Flow control protects the receiver from being overwhelmed. TCP uses a sliding window where the receiver advertises a window size telling the sender how many unacknowledged bytes it can still accept. Zero-window probes resend when the window closes, and window scaling extends the field beyond 64 KB.', keyPoints: ['Receiver advertised window prevents buffer overflow', 'Selective acknowledgement retransmits only missing segments', 'Window scaling, SACK and timestamps are TCP options', 'Flow control is end-to-end, unlike congestion control'], interviewTips: ['Distinguish flow control (receiver) from congestion control (network)', 'Explain the silly window syndrome'] },
  { topic: 'Congestion Control', title: 'TCP Congestion Control', content: 'Congestion control protects the network. TCP keeps a congestion window: it starts slow (slow start, doubling every RTT), switches to congestion avoidance (linear growth) at the threshold, and reacts to loss by halving the window (AIMD). Modern variants are CUBIC and BBR, and ECN marks packets instead of dropping them.', keyPoints: ['Slow start doubles cwnd; congestion avoidance adds linearly', 'AIMD: additive increase, multiplicative decrease', 'Fast retransmit on three duplicate ACKs; fast recovery avoids slow start', 'cwnd = min(receiver window, congestion window)'], interviewTips: ['Sketch the sawtooth of cwnd over time', 'Explain why loss-based control underperforms on high-latency links'] },
  { topic: 'Switching', title: 'Switching Techniques', content: 'Circuit switching reserves a dedicated path for the whole session (traditional telephony) and gives constant delay but wastes capacity. Packet switching sends self-contained packets that are routed independently, which is efficient and resilient and is what the Internet uses. Message switching stores and forwards whole messages and has poor latency.', keyPoints: ['Circuit: reserved path, constant delay, inefficient', 'Packet: statistical multiplexing, resilient, variable delay', 'Store-and-forward introduces per-hop delay', 'Virtual circuits add a connection identifier per flow'], interviewTips: ['Compare circuit and packet switching on efficiency and QoS', 'Mention MPLS as a virtual-circuit style technology'] },
  { topic: 'Hub vs Switch vs Router', title: 'Hub vs Switch vs Router', content: 'A hub repeats an incoming frame to every port in one collision domain. A switch learns MAC addresses and forwards frames only to the correct port, giving each port its own collision domain. A router forwards packets between different IP networks, breaking broadcast domains and choosing the best path.', keyPoints: ['Hub: Layer 1, floods everything, half duplex', 'Switch: Layer 2, MAC table, full duplex per port', 'Router: Layer 3, connects subnets, blocks broadcasts', 'Layer 3 switches combine switching speed with routing'], interviewTips: ['Say which layer each device works at', 'Explain broadcast domains versus collision domains'] },
  { topic: 'Firewall', title: 'Firewalls', content: 'A packet-filtering firewall inspects headers (IP, port, protocol) with stateless rules. A stateful firewall tracks connection state so return traffic is allowed automatically. Deep packet inspection examines payloads, and next-generation firewalls add application awareness and intrusion prevention.', keyPoints: ['Stateless filter: header rules only', 'Stateful: connection tracking table', 'Proxy/application firewall terminates and inspects sessions', 'Default deny is the secure baseline'], interviewTips: ['Explain why stateful rules are safer than opening both directions', 'Mention WAF for HTTP-layer attacks'] },
  { topic: 'Socket', title: 'Sockets and Programming', content: 'A socket is the programming interface to the transport layer, identified by the tuple (protocol, local IP, local port, remote IP, remote port). A TCP server creates, binds, listens, accepts and then reads and writes; a client creates and connects. Blocking sockets wait, non-blocking sockets need select/epoll, and ports below 1024 are privileged.', keyPoints: ['Socket = IP address + port + protocol', 'Server lifecycle: socket, bind, listen, accept, read/write, close', 'epoll/kqueue scale to many concurrent connections', 'TIME_WAIT and port exhaustion are common production issues'], interviewTips: ['Describe the accept loop and thread pooling', 'Mention SO_REUSEADDR and its purpose'] },
];