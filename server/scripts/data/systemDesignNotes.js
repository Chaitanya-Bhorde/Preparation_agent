// System Design Notes Part 1 - foundations: scaling, load balancing, caching, CDN, sharding, replication
module.exports = [
  { topic: 'System Design Introduction', title: 'How to Approach a System Design Interview', content: 'A system design interview tests structured thinking, not trivia. Follow a repeatable script: clarify functional and non-functional requirements, estimate scale (QPS, storage, bandwidth), sketch the API, design the data model, draw the high-level components, then deep-dive into bottlenecks, trade-offs and failure handling.', keyPoints: ['Always start with requirements and scale numbers', 'Back-of-envelope: 1M DAU x 10 requests/day ~= 115 QPS average, peak 3-5x', 'Storage: 100 bytes x 1M rows/day = 100 MB/day, ~36 GB/year', 'State trade-offs explicitly: consistency vs availability, latency vs cost'], interviewTips: ['Say "I will assume..." and write the assumptions down', 'Reserve the last 5 minutes for bottlenecks and monitoring'] },
  { topic: 'Scalability', title: 'Scalability Fundamentals', content: 'Scalability is the ability to handle growth by adding resources. Vertical scaling means a bigger machine (simple, but has a hard ceiling and single point of failure). Horizontal scaling means more machines (near-limitless, needs load balancing and stateless services). Statelessness and externalised session state are the prerequisites.', keyPoints: ['Vertical: more CPU/RAM on one node; limited and expensive', 'Horizontal: more nodes; needs load balancer and shared state store', 'Keep app servers stateless; store sessions in Redis or tokens', 'Identify the bottleneck first - usually the database, not the web tier'], interviewTips: ['Show a growth plan: single server -> app/db split -> LB + replicas -> cache + shards', 'Mention that scaling reads and writes are different problems'] },
  { topic: 'Load Balancing', title: 'Load Balancing', content: 'A load balancer distributes traffic across healthy backends, providing scalability and fault tolerance. Layer 4 balances on TCP/IP (fast, protocol unaware); layer 7 balances on HTTP details (path, header, cookie routing). Algorithms include round robin, least connections, weighted, IP hash and consistent hash.', keyPoints: ['L4 vs L7: transport speed vs content-aware routing', 'Health checks remove unhealthy nodes from rotation', 'Sticky sessions pin a user to a node; prefer stateless design', 'Active-passive LB pairs avoid the LB itself becoming a SPOF'], interviewTips: ['Mention DNS round robin, then AWS ALB/NLB as managed options', 'Explain session affinity and why sticky sessions hurt scaling'] },
  { topic: 'Caching', title: 'Caching Strategies', content: 'Caching stores computed or fetched data closer to the reader to cut latency and database load. Cache-aside (lazy loading) is the default: read cache, on miss read DB and populate. Write-through writes both synchronously; write-behind batches writes to the DB asynchronously.', keyPoints: ['Cache-aside: simplest, most common, app manages population', 'Write-through: consistent but slower writes', 'Write-behind: fast writes, risk of data loss', 'Eviction: LRU, LFU, TTL; always set a TTL and a size limit'], interviewTips: ['Explain cache stampede and fixes: locks, jittered TTL, request coalescing', 'Mention Redis for shared cache and Memcached for pure key-value speed'] },
  { topic: 'CDN', title: 'Content Delivery Networks', content: 'A CDN is a globally distributed cache of static and cacheable content served from edge locations near the user. Requests hit the nearest edge, which serves from cache or fetches from origin. Behaviour is controlled by cache-control headers and invalidation APIs.', keyPoints: ['Edge caching reduces latency and origin load', 'Push CDN uploads content; pull CDN fetches on first request', 'Cache-Control: max-age, s-maxage, stale-while-revalidate, immutable', 'Versioned filenames (app.3f2c1.js) make invalidation unnecessary'], interviewTips: ['Explain how to serve personalised content with a CDN (edge compute or API split)', 'Mention Vary and query strings when debugging cache misses'] },
  { topic: 'Database Sharding', title: 'Database Sharding', content: 'Sharding partitions one logical dataset across multiple databases so each holds a subset of rows. Choose a shard key with even distribution and query locality: hash-based sharding spreads load but scatters ranges; range-based keeps ranges together but risks hotspots; directory-based uses a lookup service for flexibility.', keyPoints: ['Shard key choice decides balance AND query patterns', 'Cross-shard joins and transactions become expensive', 'Resharding is painful - plan for growth or use consistent hashing', 'Common keys: user_id, tenant_id, region, time bucket'], interviewTips: ['Explain why auto-increment ids make bad shard keys (hotspot + coordination)', 'Mention application-level vs database-level sharding'] },
  { topic: 'Database Replication', title: 'Database Replication', content: 'Replication copies data to additional nodes for availability, read scaling and disaster recovery. Leader-follower (primary-replica) sends all writes to the leader and reads to followers; multi-leader allows writes in several regions; leaderless (quorum) writes to N nodes and reads from R.', keyPoints: ['Synchronous replication: durable, higher write latency', 'Asynchronous replication: fast writes, risk of data loss and stale reads', 'Read replicas scale reads but introduce replication lag', 'Failover promotes a follower; automated failover needs quorum/consensus'], interviewTips: ['Explain read-after-write consistency using sticky routing to the leader', 'Give a quorum example: N=3, W=2, R=2 tolerates one node failure'] },
];