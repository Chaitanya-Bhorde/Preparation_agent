// Notes - deep, detailed multi-paragraph notes
module.exports = [
  { topic: 'DSA Introduction', title: 'Introduction to Data Structures and Algorithms', content: `Data structures and algorithms together study how to organise, store, and manipulate data efficiently: a data structure is the way data is arranged and accessed (arrays, linked lists, trees, graphs, hash tables, stacks, queues), and an algorithm is a precise, finite sequence of steps for solving a problem. The twin questions every interview tests are the cost in time and the cost in memory, and choosing the structure that serves the use case cheapest.

The interview taxonomy: linear structures (arrays, linked lists, stacks, queues) where elements sit in a sequence and you trade access for insert/delete cost; tree structures (binary trees, BSTs, heaps) imposing hierarchy for log-time search; hash structures (hash maps, hash sets) giving near-constant average lookups; graph structures modelling relationships between arbitrary pairs; and string structures (tries) for prefix-heavy text work. Knowing the mapping - queues for BFS and scheduling, stacks for recursion, maps for caches, trees for sorted sets - is the applied skill screens separate candidates on.

The shared cost vocabulary is Big-O. Two design principles: amortised cost hides occasional expensive operations (a dynamic array resizes rarely so append is O(1) amortised) and locality of reference makes cache hits cheaper than pointer chasing.`, keyPoints: ['Structure + algorithm = how to store and solve a problem', 'Linear, tree, hash, graph, string are the interview taxonomy', 'Big-O for time and space', 'Amortised cost hides occasional expensive work', 'Locality of reference favours arrays over linked structures'], commonMistakes: ['Choosing a structure by syntax preference instead of access pattern', 'Ignoring pointer indirection cost on cache locality'], interviewTips: ['Map each major structure to one real use case', 'Quote amortised O(1) for dynamic-array append and why'] },
  { topic: 'Time and Space Complexity', title: 'Time and Space Complexity Analysis', content: `Complexity measures resource use as a function of input size n, not wall-clock time. Time complexity counts primitive operations: a double loop over n elements is Theta(n^2) (every pair examined), a binary search halving each step is Theta(log n), and a single scan is Theta(n). Space complexity counts extra memory: recursive calls grow the call stack (depth = cost) and auxiliary structures (visited set, queue, hash map) add their size. State BOTH and say whether the space is iterative-constant or recursive-linear.

Methods: count loops mechanically (nested multiply, sequential add), use a recursion tree for divide-and-conquer (merge sort: n work per level across log n levels = Theta(n log n)), and amortized analysis for data structures (a dynamic array doubling when full does one O(n) copy per n insertions, so amortized insertion is Theta(1)). Hold the hierarchy: constant < log < linear < linearithmic < quadratic < cubic < exponential; interview targets are linear, linearithmic, or quadratic for naive, exponential only as a warning. Big-O is an upper bound, Theta a tight bound, Omega a lower bound.

Complexity drives structure choice - a hash map over a list makes lookup O(1) instead of O(n). Interview framing: define both with loop-counting, name recursion-tree and amortized methods, recite the hierarchy, then the hash-vs-list example.`, keyPoints: ['Time counts operations; space counts extra memory', 'Double loop = O(n^2), binary search = O(log n), single scan = O(n)', 'Recursion: call-stack depth is the space cost', 'Amortized dynamic-array insert = O(1)', 'Hierarchy: log < linear < linear-log < quadratic < cubic < exponential'], commonMistakes: ['Forgetting call-stack space in recursive algorithms', 'Treating merge sort per-level n as O(n) instead of O(n log n)'], interviewTips: ['Count loops mechanically: nested multiply, sequential add', 'Explain why a dynamic array is O(1) amortized'] },
  { topic: 'Asymptotic Notation', title: 'Asymptotic Notation', content: `Asymptotic notation describes growth rate as input size n tends to infinity, abstracting away constants so we can speak of whether an algorithm scales. Big-O, Big-Theta and Big-Omega are defined by witnesses: f(n) = O(g(n)) if constants c and n0 exist with f(n) <= c*g(n) for all n >= n0 (upper bound); f(n) = Omega(g(n)) if c and n0 exist with f(n) >= c*g(n) (lower bound); f(n) = Theta(g(n)) when BOTH hold - a tight bound sandwiching f(n).

Conventions and traps: write 2n^2 + 3n + 1 as Theta(n^2) - drop lower terms and constants because they are irrelevant as n grows; Big-O is strictly an UPPER bound, so an O(n log n) algorithm is also correctly O(n^2) or O(2^n) - true but loose - while Theta(n log n) is the tightest; small-omega and small-o use strict inequality. Interviewers accept Big-O used loosely for complexity class, but define precisely if asked.

Three extras: Omega gives a lower bound so any comparison sort is Omega(n log n) under the decision model; little-o and little-omega are strict non-tight variants; worst-case, expected and amortized are axes of analysis that combine with notation. Interview framing: the witness inequalities for O, Theta, Omega, then drop lower terms on one example, then the loose-vs-tight distinction.`, keyPoints: ['f = O(g) needs c,n0 with f(n) <= c*g(n); Omega is the >= lower bound', 'Theta = both O and Omega = the tight bound', 'Drop constants and lower terms (2n^2+3n+1 = Theta(n^2))', 'Big-O is an upper bound, so overstating is technically correct, Theta is tightest', 'Worst-case/expected/amortized are analysis modes, not notation'], commonMistakes: ['Calling every upper bound Theta - Big-O and Theta differ', 'Forgetting the witnesses n0 and c when asked for the bound'], interviewTips: ['Write the witness inequalities, then simplify one concrete example', 'Explain why Big-O is correct but Theta is a stronger statement'] },
  { topic: 'Arrays', title: 'Arrays', content: `An array is a contiguous, fixed-length block of memory where each element occupies equal space, so addressing is arithmetic: address of element i = base + i * element_size, giving O(1) random access by index. That single property defines the trade. Reading is O(1) by index and writing one element is O(1). Because the block is contiguous, INSERTING or DELETING in the middle forces shifting every element past it, so arbitrary insert/delete is O(n). Searching unsorted is O(n); searching sorted is O(log n). Dynamic arrays (vector, ArrayList) emulate growth by allocating a larger backing array and copying over when full - O(n) resize but O(1) amortized across many appends.

The practical technique interviewers test is the two-pointer sweep over a sorted array: walk inward or outward in one pass instead of nested loops, reducing O(n^2) to O(n). The classic is two-sum on a sorted array. Interview framing: O(1) indexing from the address arithmetic, O(n) insertion/deletion in the middle, dynamic arrays and amortized O(1) appends, then the two-pointer idiom.`, keyPoints: ['Contiguous, fixed size; address = base + i * element_size', 'O(1) random access; O(n) insert/delete in the middle', 'Binary search on sorted arrays is O(log n)', 'Dynamic arrays resize by copying, O(1) amortized appends', 'Two-pointer sweep converts nested loops into one pass'], commonMistakes: ['Calling arbitrary insert O(1) because index access is O(1)', 'Forgetting the extra O(n) memory from a dynamic-array resize'], interviewTips: ['State the address-arithmetic reason random access is constant', 'Show one two-pointer problem to demonstrate the sweep'] },
  { topic: 'Two Pointers', title: 'Two Pointers Technique', content: `The two-pointers technique uses two indices walking a sequence - usually an array or linked list - to examine pairs or ranges in a single pass instead of nested loops. The indices move based on the data: typically both start at the ends of a SORTED array and move inward, or one leads and the other trails to maintain an invariant. Because each pointer moves monotonically without backtracking across the whole range, total work is linear in n even when every pair seems relevant.

The signature move is two-sum on a sorted array: left at the start, right at the end; if a[left] + a[right] is too small raise left (increase sum), if too large lower right (decrease it), if equal record the pair and advance. That is O(n) versus O(n^2) naive. The same pattern powers 3Sum (fix one element, two-pointer the rest), partitioning around a pivot, merging two sorted arrays, trapping-rain-water, and container-with-most-water. The interview tell: is the input sorted or orderable, and can pointers move monotonically toward the answer?

A second family handles linked lists with a fast/slow runner: slow advances one step, fast two, so when fast hits the end slow is at the middle (palindrome, merge-sort midpoint), and cycle detection follows. Warning: the inward-collision needs SORTED data for the directional move to be correct; without ordering the move is unjustified. Interview framing: define with the sorted-array inward walk, list common reductions, then the fast/slow runner for cycle and middle-finding.`, keyPoints: ['Two indices moving monotonically - O(n) vs O(n^2)', 'Inward-walk on sorted array: too small raises left, too big lowers right', 'Reduces to 3Sum, partitioning, merge, trapping-rain-water', 'Fast/slow runner for linked-list cycle detection and middle', 'Requires sorted or orderable input for the directional logic'], commonMistakes: ['Applying the inward-collision logic to a non-sorted array', 'Letting a pointer backtrack, silently reintroducing O(n^2)'], interviewTips: ['Write the two-sum inward-walk from memory', 'Connect fast-slow to both middle-finding and cycle detection'] },
  { topic: 'Sliding Window', title: 'Sliding Window', content: `The sliding window finds the optimal contiguous subarray or substring by maintaining two pointers that delimit a window and SHRINK or expand it based on a validity invariant, rather than recomputing every subarray. The key insight is monotonicity: once a shorter valid window exists and a larger one is invalid (or vice-versa), shrink from the left or extend the right so each element enters and leaves a constant number of times. Total work is O(n).

Two variants cover the canon. Fixed-size: k is given, maintain exactly length k and track a rolling statistic - max-sum-of-k-consecutive and running-maximum fall here. Variable-size: find the smallest subarray whose sum is at least S; right adds until invariant (sum >= S) holds, then left advances to shrink while still valid, recording the minimum. The tell: left moves only while valid, right only while invalid. Classic rolling optimizations: rolling hash for substring anagrams, frequency map of distinct elements.

Framing: state the invariant explicitly, prove the monotonicity justifying single-direction movement, cite anagram and smallest-window-sum. Interview framing: define via the monotonic invariant, give fixed vs variable size, then the two examples.`, keyPoints: ['Two pointers delimit a window moved by a validity invariant', 'Each element enters and leaves a constant number of times -> O(n)', 'Fixed-size: window of length k with rolling statistics', 'Variable-size: smallest/largest valid window, left shrinks on validity', 'Requires a monotonic invariant justifying single-direction movement'], commonMistakes: ['Forgetting to shrink from the left, reverting to O(n^2)', 'Applying to non-monotonic objectives where the greedy move is invalid'], interviewTips: ['State the invariant before moving pointers', 'Show the sum-at-least-S variable-size walkthrough'] },
  { topic: 'Prefix Sum', title: 'Prefix Sum and Difference Arrays', content: `A prefix-sum array stores cumulative totals so any range-sum query on the original array is answered in O(1): prefix[i] = prefix[i-1] + a[i-1], and sum(a[l..r]) = prefix[r+1] - prefix[l]. The technique pays off when a problem asks for many range sums - precompute prefixes in O(n) then answer each in constant time. The interview tell is multiple queries over a static array that look quadratic naively.

The 2D extension is the integral image: prefix2d[i][j] = prefix2d[i-1][j] + prefix2d[i][j-1] - prefix2d[i-1][j-1] + matrix[i-1][j-1], and any rectangle sum is O(1) from four corner lookups. The difference-array twin handles RANGE UPDATES: diff[l] += val, diff[r+1] -= val so after one O(n) prefix scan every element holds its final value, turning k range-adds on an n-array from O(k*n) into O(n + k).

Common reductions: subarray-sum-equals-k in O(n) via a prefix hash map (count pairs of prefix values differing by k), and equilibrium index (prefix[i-1] equals total - prefix[i]). Closing warning: a difference array is only useful if you query AFTER all updates; interleaved updates and queries need a Fenwick or segment tree. Interview framing: define 1D prefix sum with the O(1) query, equilibrium index, then 2D and difference arrays.`, keyPoints: ['prefix[i] = prefix[i-1] + a[i-1]; range sum = prefix[r+1] - prefix[l]', 'Many range queries on a static array -> O(n) preprocess, O(1) each', '2D integral image: four-corner lookup for any rectangle sum', 'Difference array: diff[l]+=v, diff[r+1]-=v, then prefix to finalise in O(n+k)', 'Hash-map prefix trick solves subarray-sum-equals-k in O(n)'], commonMistakes: ['Using a difference array for interleaved queries without a tree', 'Forgetting prefix[0] = 0 base case'], interviewTips: ['Derive equilibrium index from prefix sums in two lines', 'Trace subarray-sum-equals-k with the prefix hash map'] },
  { topic: 'String Algorithms', title: 'String Algorithms', content: `Strings are arrays of characters, so the same algorithmic tools apply plus domain-specific tricks. The interview workhorse is the frequency-array or hash-map comparison: to test whether two strings are anagrams, count characters in both and compare - O(n). Pattern matching asks whether pattern P appears in text T; naive search is O(n*m), KMP achieves O(n+m) with a failure table built from where a partial match can resume, and Rabin-Karp uses a rolling hash to find a match in O(n) expected then confirms - the tell is a rolling hash that updates the window fingerprint in constant time.

Subproblems: longest repeated substring and longest common substring (binary search plus hash or Rabin-Karp-style rolling), palindrome recognition (expand from each center in O(n^2), or Manacher in O(n) for the longest palindromic substring), and string-rotation and substring checks. The suffix-array/suffix-tree family gives full structural power at the cost of build complexity rarely asked directly but worth naming. Implementation discipline: arrays for alphabet strings, maps for Unicode, never call substring in an inner loop (copy cost O(length)), compare hashes with modulus and large base. Interview framing: anagram test with frequency array, KMP and Rabin-Karp with bounds, expand-from-center palindrome, alphabet-size caveats.`, keyPoints: ['Anagram: frequency counts compare in O(n)', 'KMP O(n+m), Rabin-Karp O(n) expected with rolling hash', 'Palindrome: expand-from-center O(n^2), Manacher O(n)', 'Rolling hash updates the window fingerprint in O(1)', 'Use arrays for alphabets, maps for Unicode'], commonMistakes: ['Calling substring repeatedly inside a loop - O(length) copy each time', 'Using a map for alphabet strings when an array is faster'], interviewTips: ['Derive the KMP failure table from the prefix-is-suffix idea', 'Show the expand-from-center two-pointer palindrome scan'] },
  { topic: 'Linked List', title: 'Linked Lists', content: `A linked list stores nodes scattered in memory, each holding a value and a pointer to the next node (singly) or to the next and previous (doubly). Access is sequential from the head - no index arithmetic - so indexing into a linked list is O(n) to walk to the position. That is the price for what linked lists do well: O(1) insert and O(1) delete at a known position by relinking pointers rather than shifting memory.

Traversal is foundational: walk with a current pointer until null, never indexing. The sentinel or dummy-head removes edge cases: a fake head so inserting before the first real element needs no special case. Two classics: slow/fast pointer finds the middle in one pass (fast moves two, slow one), and reversing in place by walking once and flipping each next pointer to point at the previous - O(n) time, O(1) space. Interview tell: linked-list problems decompose into traversal with careful pointer handling; the slow/fast and in-place-reverse patterns solve most. Memory cost is a pointer per node but locality is poor versus arrays.

Interview framing: O(n) indexing vs O(1) splice, the sentinel, slow/fast and in-place reverse, then cache-locality loss.`, keyPoints: ['Nodes hold value + next (singly) or next + prev (doubly)', 'O(n) to traverse/index, O(1) splice at a known position', 'Sentinel/dummy head removes the first-element edge case', 'Slow/fast runner finds middle; reverse is in-place O(n)', 'Pointer chasing defeats cache relative to arrays'], commonMistakes: ['Losing the head pointer on a reverse and orphaning the list', 'Forgetting to advance the pointer, infinite loop'], interviewTips: ['Write reverse-in-place with three pointers', 'Explain why a sentinel removes the first-element special case'] },
  { topic: 'Doubly Linked List', title: 'Doubly Linked Lists', content: `A doubly linked list extends the singly linked list with a SECOND pointer per node: next and previous, so traversal works in both directions and, critically, DELETION of a KNOWN node is O(1) - you unlink it from both neighbors without walking to find the predecessor, which singly-linked lists cannot do in constant time. The price is one extra pointer field per node (roughly double the pointer overhead).

Insertion and deletion are mechanical but error-prone in pointer order: to insert a new node y after node x, set y.next = x.next, y.prev = x, x.next.prev = y (if x.next exists), x.next = y - reading all neighbors BEFORE overwriting prevents losing links. Deletion of node x symmetrically patches x.prev.next = x.next and x.next.prev = x.prev (in both orders guarded by null checks), then frees x. The doubly-linked list is the node type behind Java's LinkedList, the Linux kernel list, and most hand-rolled intrusive lists; a circular sentinel that points to itself when empty unifies the head and tail edge cases.

Interview framing: two pointers give O(1) delete of a known node and bidirectional traversal; stress the safe field-update order (read neighbors first) and the circular-sentinel trick to remove empty-list edge cases. The cache-locality caveat versus arrays still applies.`, keyPoints: ['Two pointers per node: next and previous', 'O(1) delete of a known node - no predecessor walk', 'Read all neighbors before overwriting on insert/delete', 'Circular sentinel unifies head and tail edge cases', 'Bidirectional traversal, double pointer overhead'], commonMistakes: ['Losing a neighbor reference before relinking it', 'Null-dereferencing previous on the head or next on the tail'], interviewTips: ['Write insert-after with safe field-update order', 'Explain the circular sentinel that points to itself when empty'] },
  { topic: 'Stack', title: 'Stacks', content: `A stack is a LIFO (last-in, first-out) collection: push adds at the top, pop removes from the top - the most recent item comes out first, like a stack of plates. Array-backed the top is the last index (push = append, pop = pop last); linked-list-backed the head is the top (push/pop = O(1) pointer rewiring). All operations are O(1): push, pop, peek/top and isEmpty - the single invariant to keep right is checking before you pop so an empty stack underflows.

The real power is modelling recursion and nested structure. Function-call stacks are literal stacks; the browser-back button is a stack. Matching parentheses, balanced HTML tags, and evaluating or converting expressions (post-fix evaluation pushes operands and applies operators on pop) all reduce to a stack. Crucially, you can REWRITE RECURSION AS AN EXPLICIT STACK - tree traversals, DFS, backtracking - which also bounds call-stack overflow risk. Two interview patterns: the monotonic stack that keeps elements in sorted order by popping on each push, used for next-greater-element and largest-rectangle-histogram in O(n) amortized.

Interview framing: LIFO with O(1) ops and the underflow guard, the call-stack/recursion equivalence, then the monotonic-stack uses. Implementation note: a fixed array needs overflow handling (grow or reject); a linked-list stack never overflows except memory.`, keyPoints: ['LIFO: push and pop at the top, all O(1)', 'Array-backed or linked-list-backed; check before popping', 'Model recursion - can be rewritten as an explicit stack', 'Matching parens, expression evaluation, DFS, backtracking', 'Monotonic stack for next-greater and histogram'], commonMistakes: ['Popping an empty stack - always guard', 'Confusing LIFO (stack) with FIFO (queue)'], interviewTips: ['Explain recursion-as-stack and when to prefer explicit', 'Show the monotonic stack on next-greater-element'] },
  { topic: 'Queue', title: 'Queues', content: `A queue is a FIFO (first-in, first-out) collection: enqueue adds at the rear, dequeue removes from the front - the oldest item comes out first, like a checkout line. Array-backed with a circular buffer using front and rear indices modulo capacity keeps both operations O(1) without shifting - the circular-buffer wraparound is the interview detail. Linked-list-backed with head and tail pointers gives O(1) enqueue/dequeue. Guard against underflow (dequeue on empty) and, for circular arrays, distinguishing full from empty - conventionally by leaving one slot empty or keeping a separate size counter.

Queues model breadth-first search (the frontier grows FIFO), task scheduling, and producer-consumer buffering. The priority queue is the twist: a queue where dequeue removes the HIGHEST-priority element, not the oldest - typically a heap with O(log n) insert and extract-min/max, so it is still a queue interface with a different ordering guarantee. A deque (double-ended queue) adds push/pop at both ends and serves sliding-window-maximum problems with a monotonic deque.

Interview framing: FIFO with circular-buffer index arithmetic, BFS as the canonical use, then priority queue and deque as named variants. Two bugs to avoid: the front/rear pointer confusion on wraparound, and forgetting that a priority queue is not FIFO.`, keyPoints: ['FIFO: enqueue rear, dequeue front, both O(1)', 'Circular buffer with front/rear modulo capacity avoids shifting', 'Guard underflow; size counter or empty slot for full vs empty', 'BFS frontier is FIFO; priority queue orders by priority not age', 'Deque at both ends; monotonic deque for sliding-window-max'], commonMistakes: ['Forgetting circular wraparound and degrading to O(n) shifts', 'Calling a heap a FIFO queue - it is a priority queue'], interviewTips: ['Explain the circular-buffer modulo arithmetic', 'Link the BFS frontier directly to FIFO queueing'] },
];
