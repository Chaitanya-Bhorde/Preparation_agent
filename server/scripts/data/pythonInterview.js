// Python Interview Questions - one detailed question per Python topic
module.exports = [
  { topic: 'Python Introduction', question: 'Why is Python described as dynamically typed but strongly typed?', answer: 'Dynamically typed means the type is attached to the object and checked at runtime, so a name can be rebound from int to str freely. Strongly typed means operations are not silently coerced: "1" + 1 raises TypeError rather than producing "11" or 2, which is what a weakly typed language such as JavaScript would do with the other operand order.', explanation: 'This contrast with JavaScript is the exact point interviewers probe.', difficulty: 'easy', followUps: ['How do type hints change this?', 'When does mypy help in a CI pipeline?'] },
  { topic: 'Data Types', question: 'Explain mutability and hashability and why they are linked.', answer: 'Mutable objects (list, dict, set) can change in place, so their hash could change after being inserted into a hash table, which would break bucket lookup. Python therefore only allows hashable, effectively immutable objects (int, str, tuple of immutables, frozenset) as dict keys and set elements.', explanation: 'The reasoning about hash tables is what makes this answer complete.', difficulty: 'medium', followUps: ['Can a tuple containing a list be a dict key?', 'Why is frozenset useful?'] },
  { topic: 'Variables and Scope', question: 'Explain LEGB and the mutable default argument trap.', answer: 'Name resolution goes Local, Enclosing, Global, Built-in, with global and nonlocal to rebind outer names. The trap: default arguments are evaluated once at def time, so def add(item, bucket=[]) reuses one shared list across calls. The fix is to default to None and create the list inside the function.', explanation: 'This single trap causes real production bugs, so it is a favourite.', difficulty: 'medium', followUps: ['How does nonlocal behave with two nesting levels?', 'What is late binding in closures?'] },
  { topic: 'Operators', question: 'When must you use is rather than ==?', answer: 'Use is only for identity comparisons against singletons - None, True and False - and for checking whether two names refer to the same object. Use == for value comparison because it dispatches to __eq__ and works for numbers, strings and containers. Small integers and short strings may be interned, so is can accidentally be true in tests but fail in production.', explanation: 'The interning caveat shows practical awareness.', difficulty: 'easy', followUps: ['Why does x is None beat x == None?', 'How do you implement __eq__ and __hash__ together?'] },
  { topic: 'Control Flow', question: 'When is the for/else construct genuinely useful?', answer: 'It expresses search loops cleanly: the else block runs only when the loop completed without break, so you can scan for a value and handle the not-found case without a flag variable. The same pattern exists for while/else. Note that Python has no do-while, so while True with break is the idiom.', explanation: 'A concrete search example demonstrates real understanding.', difficulty: 'medium', followUps: ['How would you emulate do-while?', 'How does match/case differ from a switch?'] },
  { topic: 'Lists', question: 'Why is inserting at the front of a list expensive and what would you use instead?', answer: 'A list is a contiguous dynamic array, so inserting at index 0 shifts all n elements, costing O(n). For a queue use collections.deque, whose appendleft and popleft are O(1) because it is a doubly linked list of blocks. Only use list.insert(0, x) when the list is tiny or the operation is rare.', explanation: 'Naming deque and the O(n) reasoning is the expected answer.', difficulty: 'medium', followUps: ['How does a deque block structure help performance?', 'When is heapq a better choice than a sorted list?'] },
  { topic: 'Tuples', question: 'Give real uses for tuples beyond "immutable lists".', answer: 'Tuples are records with positional meaning, used for multiple return values, dictionary keys, set membership and function argument packing. namedtuple adds field names with the same memory profile, and because tuples are immutable they are safe to share across threads and are slightly faster to build and iterate than lists.', explanation: 'Framing tuples as records rather than immutable lists is the conceptual shift.', difficulty: 'easy', followUps: ['namedtuple versus dataclass - which and when?', 'How does tuple unpacking with * work?'] },
  { topic: 'Dictionaries', question: 'How does a dict resolve collisions and why is insertion order guaranteed?', answer: 'CPython uses an open-addressing hash table: the hash of the key picks a slot, and a probe sequence finds the next free slot when occupied. Since 3.6 the table stores indices into a compact entries array, which preserves insertion order and also reduces memory. Average operations are O(1); adversarial collisions degrade to O(n).', explanation: 'The compact entries array is the reason order is preserved.', difficulty: 'hard', followUps: ['How does hash randomisation mitigate DoS attacks?', 'When would Counter or defaultdict simplify your code?'] },
{ topic: 'Sets', question: 'How do sets turn an O(n^2) algorithm into O(n)?', answer: 'Instead of comparing every pair with a nested loop to find duplicates or common elements, insert the first collection into a set (O(n)) and test each element of the second with average O(1) membership, giving O(n) overall. Set algebra (intersection, union, difference) is implemented in C and is far faster than Python-level loops.', explanation: 'Always state the complexity change, not just the API.', difficulty: 'easy', followUps: ['How do you keep order while deduplicating?', 'How do frozensets enable nested sets?'] },
  { topic: 'Strings', question: 'Why is "".join(parts) preferred to repeated concatenation?', answer: 'Strings are immutable, so each += allocates a new string and copies all previous characters, making a loop O(n^2) and generating garbage. join computes the total length once and performs a single allocation and copy, which is O(n) and much friendlier to the allocator and cache.', explanation: 'Explain the quadratic behaviour - that is the whole point of the question.', difficulty: 'medium', followUps: ['How do f-strings compare in performance?', 'What does str.encode do to non-ASCII text?'] },
  { topic: 'Functions', question: 'Explain *args, **kwargs and keyword-only parameters.', answer: '*args collects extra positional arguments into a tuple and **kwargs collects extra keyword arguments into a dict, enabling wrappers to forward anything with func(*args, **kwargs). A bare * in the signature marks everything after it as keyword-only, which prevents positional misuse of flags such as def f(data, *, strict=False).', explanation: 'Keyword-only parameters are the detail most candidates forget.', difficulty: 'medium', followUps: ['How do you merge two dicts passed as kwargs?', 'What is functools.partial used for?'] },
  { topic: 'Lambda and Map Filter', question: 'When should you avoid map/filter in favour of comprehensions?', answer: 'Comprehensions are usually faster and clearly more readable when you need transformation plus filtering, because they avoid the lambda call overhead and express the intent in one construct. Reach for map or filter when you already have a named function (map(int, parts)) or when you want a lazy iterator without materialising a list.', explanation: 'Show both forms for the same task to prove the trade-off.', difficulty: 'easy', followUps: ['What does itertools.starmap add?', 'How does functools.reduce relate to sum()?'] },
  { topic: 'Comprehensions', question: 'How do you build a lookup dict from a list of records without a loop?', answer: 'Use a dict comprehension: {user["id"]: user for user in users}. For duplicates decide the policy explicitly, since later keys overwrite earlier ones. To avoid loading giant data, swap the brackets for parentheses and stream the pairs lazily, or use dict(zip(keys, values)).', explanation: 'Mentioning the duplicate-key rule shows attention to correctness.', difficulty: 'medium', followUps: ['How do you invert a dict safely?', 'Why does a comprehension have its own scope?'] },
  { topic: 'Modules and Packages', question: 'How do you structure a Python project so imports work in both tests and production?', answer: 'Use src layout with packages containing __init__.py, keep entry points in a thin main module guarded by if __name__ == "__main__", install the package in editable mode (pip install -e .) so imports resolve without sys.path hacks, and separate configuration from code. Absolute imports inside the package remove ambiguity about which module is loaded.', explanation: 'Editable installs plus absolute imports is the professional answer.', difficulty: 'hard', followUps: ['What causes circular imports and how do you break them?', 'Why use virtual environments and lock files?'] },
  { topic: 'Exception Handling', question: 'How do you design exception handling for a service that calls external APIs?', answer: 'Catch specific exceptions (requests.ConnectionError, Timeout), retry only idempotent operations with exponential backoff and jitter, translate library errors into your own domain exception with raise CustomError(...) from err to preserve the cause, and let programming errors (TypeError, ValueError) bubble up. Always log with context and never swallow silently.', explanation: 'Retry policy plus exception translation is what separates design from syntax.', difficulty: 'hard', followUps: ['Which operations are safe to retry?', 'How do you implement a circuit breaker?'] },
  { topic: 'File Handling', question: 'How do you read and write large files safely and efficiently?', answer: 'Stream instead of loading: iterate the file object (or read fixed-size chunks) for constant memory, always use with so handles close on exception, specify encoding="utf-8" explicitly, and for rewriting use a temporary file then os.replace to avoid a partially written target. Use binary mode plus a chosen encoding when the data is not text.', explanation: 'Temp file plus atomic replace and explicit encoding are the production details.', difficulty: 'medium', followUps: ['What does newline handling do on Windows?', 'How does pathlib simplify this?'] },
{ topic: 'OOP in Python', question: 'Explain self, name mangling and properties in Python classes.', answer: 'self is an explicit first parameter that receives the instance, because Python methods are plain functions stored on the class. A __name attribute is name-mangled to _ClassName__name, which prevents accidental override rather than enforcing privacy. @property turns a getter into attribute-style access and a matching setter validates assignments, so you can add logic later without breaking callers.', explanation: 'The "so you can add logic later" argument is why properties exist.', difficulty: 'medium', followUps: ['classmethod versus staticmethod?', 'How does __slots__ reduce memory?'] },
  { topic: 'Inheritance', question: 'How does the MRO work and why does super() use the MRO rather than the parent?', answer: 'Python computes a linearisation of the class hierarchy using the C3 algorithm, available as Class.__mro__. super() returns a proxy that resolves to the next class in that linearisation, not simply the immediate parent. That is what makes cooperative multiple inheritance work: each class calls super() once and every base runs in a well-defined order.', explanation: 'Saying "next in the MRO, not the parent" is the key insight.', difficulty: 'hard', followUps: ['How does C3 avoid an inconsistent hierarchy?', 'When should you prefer composition?'] },
  { topic: 'Iterators and Generators', question: 'How do generators let you process data larger than memory?', answer: 'A generator suspends at each yield and keeps only its frame and local variables, so memory use is O(1) in the number of items rather than O(n). That lets you build pipelines - read lines, filter, parse - where each stage pulls from the previous one on demand. The same laziness means a generator is exhausted after one pass, so you must re-create it or tee it.', explanation: 'Mentioning single-pass exhaustion shows you have used them in practice.', difficulty: 'medium', followUps: ['How does itertools.tee help?', 'What is the difference between yield and return?'] },
  { topic: 'Decorators', question: 'Write and explain a retry decorator with a maximum attempt count.', answer: 'The decorator takes max_attempts, returns a wrapper that loops over attempts, calls the function inside try and re-raises the last error after the final attempt, optionally sleeping with exponential backoff between tries. functools.wraps must decorate the wrapper so the function name, docstring and signature metadata survive for logging and introspection.', explanation: 'Re-raising instead of swallowing is the correctness point.', difficulty: 'hard', followUps: ['How do you preserve the signature for IDEs?', 'Where would you put the sleep to avoid thundering herds?'] },
  { topic: 'Context Managers', question: 'Why is "with" better than try/finally for resource management?', answer: 'with delegates cleanup to reusable, testable __exit__ logic, so correctness does not depend on every caller remembering to write finally, and it handles multiple resources and exceptions in one place. __exit__ also receives the exception, which allows context managers such as contextlib.suppress or database transactions to decide whether to swallow or roll back.', explanation: 'Reusability and correct exception plumbing are the real advantages.', difficulty: 'medium', followUps: ['How do you write a class-based context manager?', 'What does contextlib.ExitStack do?'] },
  { topic: 'Multithreading', question: 'Explain the GIL and when you would still choose threads.', answer: 'The GIL is a mutex protecting the CPython interpreter state, so only one thread runs bytecode at a time on one core. Choose threads for IO-bound work because the GIL is released during blocking calls such as sockets, files and time.sleep, letting other threads run. For CPU-bound work use multiprocessing or native libraries (NumPy) that release the GIL internally.', explanation: 'Naming which operations release the GIL proves practical knowledge.', difficulty: 'hard', followUps: ['How does threading.Lock differ from the GIL?', 'What changes in free-threaded Python 3.13+ builds?'] },
  { topic: 'Multiprocessing', question: 'When do you choose processes over threads or asyncio?', answer: 'Choose multiprocessing for CPU-bound work that must use multiple cores, because each process has its own GIL. Choose asyncio for very high concurrency on IO where coroutines are far cheaper than threads or processes. Choose threads for moderate blocking IO with simple synchronous code. Trade-offs: processes cost start-up and pickling, asyncio requires non-blocking libraries, threads share memory and need locks.', explanation: 'A decision rule plus trade-offs is exactly what is being tested.', difficulty: 'hard', followUps: ['How do you avoid pickling large payloads?', 'What is a ProcessPoolExecutor good for?'] },
  { topic: 'JSON and Serialization', question: 'How do you serialise a custom class to JSON safely?', answer: 'Convert the object to plain built-in types first: define a to_dict() method, or use dataclasses.asdict, then call json.dumps with a default= handler for anything unconvertible. This keeps the JSON schema explicit and avoids coupling your classes to a serializer. Never use pickle for data that crosses a trust boundary, because unpickling can execute arbitrary code.', explanation: 'The security warning about pickle is the mark of a strong answer.', difficulty: 'medium', followUps: ['How do you validate JSON input in an API?', 'Why must JSON keys be strings?'] },
];