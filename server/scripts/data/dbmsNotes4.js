// DBMS Notes - deep rewrite for all DBMS topics (detailed multi-paragraph notes)
module.exports = [
  { topic: 'DBMS Introduction', title: 'What is a DBMS?', content: 'A Database Management System (DBMS) is system software that sits between users/applications and the physical data. It lets you define schemas, insert/update/query data (usually via SQL), and administer security, concurrency and recovery - all while hiding storage details such as pages, buffers and indexes behind a logical view of the data.\n\nA file system forces every program to implement its own searching, locking, crash recovery and access control, which leads to redundancy and inconsistency. A DBMS centralises those services once: it offers data independence, ACID transactions, concurrent access with locking, query optimisation, backup/recovery and fine-grained authorisation.\n\nExamples: MySQL, PostgreSQL, Oracle, SQL Server (relational); MongoDB, Redis (NoSQL). Anchor every DBMS answer in the problems it solves: redundancy, inconsistency, lost updates and unauthorised access.', keyPoints: ['DBMS = data + query processor + storage manager + transaction manager + metadata catalogue', 'Provides data independence, integrity constraints, security and concurrency control', 'Declarative queries (SQL): you say what, the optimiser decides how', 'Serves many users concurrently with isolation between them', 'Examples: MySQL, PostgreSQL, Oracle, SQL Server, MongoDB'], commonMistakes: ['Saying a DBMS is just a place to store data - the services (transactions, recovery, concurrency) are the point', 'Confusing data (raw facts) with information (processed data)'], interviewTips: ['Open with the definition, then immediately list the services a DBMS provides over a file system', 'Be ready for the follow-up: why is DBMS preferred over file systems?'] },
  { topic: 'DBMS vs File System', title: 'DBMS vs File System', content: 'In a file system each application owns its own files, so the same data is duplicated across programs and any change must be applied everywhere - the classic redundancy and inconsistency problem. There is no central schema, no enforced integrity constraints, and every program hard-codes how data is laid out, so changing storage breaks code.\n\nA DBMS removes these issues: one shared catalogue describes the data; declarative queries replace hand-written searching; transactions give atomicity and durability; locks give safe concurrent access; and views plus authorisation control who sees what. Recovery logs mean a crash does not leave half-written records.\n\nConcrete example: a college marks spreadsheet copied into department folders goes out of sync; the same data in one DBMS table stays single-source. Trade-off: a DBMS adds setup overhead and needs tuning, which is why tiny single-user apps may still use flat files.', keyPoints: ['File system: redundancy, inconsistency, no integrity enforcement', 'DBMS: single source of truth with a shared schema', 'DBMS adds transactions (ACID), locking, recovery and access control', 'Data independence: programs survive storage or schema changes', 'File systems remain fine for simple, single-user, no-concurrency workloads'], commonMistakes: ['Claiming file systems cannot index - they can, but indexing is per-application and not integrated with queries', 'Forgetting the trade-off: a DBMS brings overhead and administration cost'], interviewTips: ['Use the college/payroll duplicate-file example, then map each problem to the DBMS feature that fixes it', 'Expect the follow-up: when would you still choose a file system?'] },
  { topic: 'Three Schema Architecture', title: 'ANSI-SPARC Three Schema Architecture', content: 'The ANSI-SPARC three-level architecture separates how users see data from how it is physically stored. The external level is the set of individual user views (virtual tables, reports); the conceptual level is the single logical schema - entities, relationships, constraints - for the whole organisation; the internal level describes physical storage: files, indexes, access paths, compression.\n\nEach level hides details from the one above: the conceptual schema unifies all external views, and the internal schema implements the conceptual one. The mappings between levels are what make data independence possible - the external/conceptual mapping supports logical data independence, the conceptual/internal mapping supports physical independence.\n\nInterview framing: it is a design discipline, not a product feature. Any RDBMS implements it approximately: views = external, base tables = conceptual, storage engine internals = internal.', keyPoints: ['External level: multiple user views over the same data', 'Conceptual level: one logical schema with entities and constraints', 'Internal level: physical storage structures and access paths', 'External/conceptual mapping gives logical data independence', 'Conceptual/internal mapping gives physical data independence'], commonMistakes: ['Calling views the conceptual level - views are external; base tables are conceptual', 'Saying the internal level is the OS file system - it is the storage structures inside the DBMS'], interviewTips: ['Draw the three boxes, label both mappings, then attach each type of data independence to its mapping', 'Mention ANSI-SPARC by name for extra credit'] },
  { topic: 'Data Independence', title: 'Data Independence (Logical and Physical)', content: 'Data independence is the ability to change the schema at one level without rewriting the levels above it. Logical data independence: you can add a column, split a table or add a table at the conceptual level without breaking existing external views and applications - only the external/conceptual mappings change.\n\nPhysical data independence: you can change how data is stored - new index, different file organisation, repartitioned pages - without touching the conceptual schema or any application; only the conceptual/internal mapping changes. Every query keeps working when you create or drop an index.\n\nWhy it matters: applications stay stable while the database evolves, and the DBMS can optimise storage freely. Logical independence is the harder of the two to achieve fully.', keyPoints: ['Logical: change conceptual schema, views/apps keep working', 'Physical: change storage or indexes, conceptual schema unaffected', 'Achieved via the two mappings of the ANSI-SPARC architecture', 'Logical independence is harder to achieve fully than physical', 'Indexes are the standard physical-independence example'], commonMistakes: ['Swapping the two types in the answer', 'Saying data independence means data is portable between DBMS products - it is about schema levels, not portability'], interviewTips: ['Give one concrete example of each type: adding a column (logical) vs adding an index (physical)', 'Link the answer back to the three-schema architecture'] },
  { topic: 'ER Model', title: 'Entity-Relationship Model', content: 'The Entity-Relationship model is a conceptual design tool: you model the domain as entities (things that exist independently, e.g. Student, Course), attributes (properties, e.g. roll_no, name) and relationships (associations, e.g. enrols). Attributes can be simple, composite (name -> first + last), multivalued (phones), derived (age from dob) or key attributes.\n\nRelationships carry cardinality (1:1, 1:N, M:N) and participation (total - every entity must participate - vs partial). An M:N relationship like Student-Enrols-Course becomes a separate table with two foreign keys when mapped to the relational model; 1:N relationships become a foreign key on the N side.\n\nDesign flow: requirements -> ER diagram -> relational schema -> normalisation. Symbols: rectangle = entity, diamond = relationship, oval = attribute, double oval = multivalued attribute, dashed oval = derived, double rectangle = weak entity.', keyPoints: ['Entity: real-world object with independent existence', 'Attribute types: simple, composite, multivalued, derived, key', 'Cardinality: 1:1, 1:N, M:N; participation: total vs partial', 'Weak entity: no key of its own; needs identifying relationship plus owner key', 'M:N relationships become junction tables with composite keys'], commonMistakes: ['Marking a derived attribute like age as stored - it is computed, shown with a dashed oval', 'Using a foreign key on one side for an M:N relationship instead of a junction table'], interviewTips: ['Practice drawing an ER diagram for a library/college/e-commerce scenario in under 5 minutes', 'Know how each ER construct maps to a relational table - that question is a classic'] },
  { topic: 'Entities and Relationships', title: 'Entities, Relationship Sets and Degrees', content: 'An entity is an object with independent existence - a physical thing (Car) or a concept (Course, Account). An entity type is the blueprint (Student); an entity set is the collection of actual instances. A strong entity has its own key; a weak entity (Payment under Loan, Room under Building) cannot be identified alone and depends on an identifying relationship with its owner, using a partial key (dashed underline) plus the owner key.\n\nA relationship is an association among entities; a relationship set is the collection of such associations. Relationships can carry their own attributes (Enrols has grade even though it connects Student and Course) and have a degree: unary (Employee manages Employee), binary, or ternary (Supplier-Part-Project).\n\nCommon confusions: an entity is not an attribute (Course is an entity, course_name is an attribute), and existence dependency is what makes a weak entity weak - not merely lacking a key.', keyPoints: ['Strong entity: own key; weak entity: partial key plus owner key', 'Identifying relationship connects the weak entity to its owner', 'Relationships may carry attributes (grade on Enrols)', 'Degree: unary, binary, ternary relationships', 'Entity set = the instances of one entity type'], commonMistakes: ['Modelling a property as an entity (or vice versa) - test: does it need its own key and relationships?', 'Forgetting that total participation is mandatory on the weak-entity side'], interviewTips: ['Convert a weak-entity example (Loan-Payment) into tables with the owner key propagated', 'Mention aggregation when asked how a relationship can relate to another relationship'] },
  { topic: 'Keys', title: 'Keys in DBMS (Super, Candidate, Primary, Foreign)', content: 'Keys identify tuples uniquely and link tables. Super key: any attribute set that uniquely identifies a tuple. Candidate key: a minimal super key - remove any attribute and uniqueness breaks (e.g. {email}, {roll_no}). Primary key: the candidate key the designer chooses; never NULL, never changes. Alternate keys: the candidate keys not chosen. Foreign key: an attribute referencing the primary/unique key of another table, enforcing referential integrity.\n\nComposite key: a key made of multiple columns. Surrogate key: an artificial auto-increment or UUID with no business meaning. A table can have many unique constraints but only one primary key.\n\nIn Student(roll_no, email, name): {roll_no}, {email} may both be candidate keys; you pick one as primary. Foreign keys create the relationships the ER diagram drew: Enrols(roll_no FK, course_id FK) implements the M:N between students and courses.', keyPoints: ['Super key ⊇ candidate key; candidate keys are minimal super keys', 'Primary key: chosen candidate key, NOT NULL, stable over time', 'Foreign key enforces referential integrity (restrict / cascade / set null)', 'Composite vs surrogate: business meaning vs artificial identifier', 'Alternate key = candidate key not chosen as primary'], commonMistakes: ['Saying a primary key can be NULL or duplicated', 'Treating UNIQUE and PRIMARY KEY as identical - a UNIQUE column may contain a NULL in standard SQL, a PK may not'], interviewTips: ['Given a schema, identify all candidate keys quickly - a standard 2-minute question', 'Mention ON DELETE CASCADE / SET NULL behaviour of foreign keys'] },
  { topic: 'Functional Dependencies', title: 'Functional Dependencies and Armstrong Axioms', content: 'A functional dependency X -> Y means: for any two tuples with equal X values, their Y values must also be equal - X functionally determines Y. It is a semantic property of the business, not of the current data: roll_no -> name holds because roll numbers identify students, even if two students happen to share a name.\n\nArmstrong axioms derive all valid dependencies: Reflexivity (Y subset of X implies X -> Y), Augmentation (X -> Y implies XZ -> YZ), Transitivity (X -> Y and Y -> Z imply X -> Z). From these come union, decomposition and pseudo-transitivity rules.\n\nUses: compute the closure X+ of an attribute set (all attributes reachable from X); if X+ covers every attribute, X is a super key - this is how candidate keys are found from an FD set, and how normal forms are checked. Example: roll_no -> dept, dept -> building gives roll_no -> building by transitivity.', keyPoints: ['X -> Y: equal X values force equal Y values', 'X+ (closure): X is a super key iff X+ contains all attributes', 'Armstrong axioms: reflexivity, augmentation, transitivity', 'Derived rules: union, decomposition, pseudo-transitivity', 'FDs come from business rules, not from inspecting current rows'], commonMistakes: ['Confusing FD direction: roll_no -> name, not name -> roll_no', 'Declaring an FD invalid just because it is not listed - derive it with the axioms first'], interviewTips: ['Practice computing closures and candidate keys from a given FD set - the most common normalisation question', 'Quote the three Armstrong axioms by name'] },
  { topic: 'Normalization', title: 'Database Normalization (1NF to 5NF)', content: 'Normalisation decomposes wide, redundant tables into smaller ones so every fact is stored exactly once. Goals: eliminate insert, update and delete anomalies, reduce redundancy, keep constraints easy to enforce. It works on functional dependencies: each normal form removes one class of bad dependency.\n\nThe ladder: 1NF (atomic values) -> 2NF (no partial dependency on a composite key) -> 3NF (no transitive dependency) -> BCNF (every determinant is a superkey) -> 4NF (no non-trivial multivalued dependency) -> 5NF (join dependencies). Higher forms are achieved by lossless-join decomposition.\n\nIn practice 3NF/BCNF is the sweet spot; denormalisation is a deliberate later step for read-heavy workloads (reporting, analytics), trading redundancy for join speed. Always present normalisation with an example table and show each decomposition step.', keyPoints: ['Fixes insert, update and delete anomalies', '1NF -> 2NF -> 3NF -> BCNF -> 4NF -> 5NF, each stricter than the last', 'Decomposition must be lossless-join; dependency preservation is desired', '3NF/BCNF is the usual practical target', 'Denormalisation is a performance trade-off, not a design failure'], commonMistakes: ['Normalising blindly to 5NF when the workload needs fast reads', 'Decomposing in a way that loses a dependency that can no longer be enforced with constraints'], interviewTips: ['Walk through one table from unnormalised to 3NF step by step', 'Be ready to explain when you would denormalise and why'] },
  { topic: '1NF', title: 'First Normal Form (1NF)', content: 'First Normal Form requires every attribute to hold a single atomic value: no repeating groups (Phone1, Phone2, Phone3) and no comma-separated lists inside a cell. A table violating 1NF cannot be queried or indexed cleanly - finding one phone number needs string parsing instead of a WHERE clause.\n\nFix: move multivalued data to its own table or its own rows. Student(roll_no, name, phones) becomes Student(roll_no, name) plus StudentPhone(roll_no, phone), where phone joins the primary key.\n\nNote on atomicity: it is defined by the domain, not by physics. A single address column is 1NF if the domain treats addresses as one value; it becomes a violation the moment you must query parts of it (city, pincode) - then split into separate columns.', keyPoints: ['Every cell holds exactly one atomic value', 'No repeating groups or multi-valued columns', 'Multivalued data moves to a child table keyed by the parent', '1NF is about domain atomicity, not about physical storage', 'A primary key is still required after 1NF'], commonMistakes: ['Splitting name into first/last for 1NF when the domain never queries the parts - atomicity is use-case defined', 'Keeping a CSV column and calling the table 1NF because the column type is VARCHAR'], interviewTips: ['Use the phones-in-one-column example and show the fixed two-table design', 'Clarify that 1NF alone removes no redundancy - higher forms do that'] },
  { topic: '2NF', title: 'Second Normal Form (2NF)', content: 'Second Normal Form = 1NF + no partial dependency: no non-key attribute may depend on only part of a composite primary key. It can only be violated when the key is composite - a table with a single-column key is automatically in 2NF.\n\nExample: Enrolment(roll_no, course_id, student_name, grade) with key (roll_no, course_id). roll_no -> student_name is a partial dependency, so student_name repeats for every course the student takes. Fix: split into Student(roll_no, student_name) and Enrolment(roll_no, course_id, grade).\n\nWhy it matters: partial dependencies cause update anomalies (name changes in many rows), insert anomalies (cannot add a student without a course) and delete anomalies (deleting the last enrolment erases the student).', keyPoints: ['2NF = 1NF + no partial dependency on part of a composite key', 'A single-column primary key implies the table is already 2NF', 'Fix: move each partially-dependent attribute to the table whose key determines it', 'Symptoms: redundancy plus update, insert and delete anomalies', 'Test: check every non-key attribute depends on the whole key'], commonMistakes: ['Applying 2NF to tables with single-column keys - partial dependency is impossible there', 'Confusing partial dependency (part of key) with transitive dependency (non-key -> non-key)'], interviewTips: ['Show the (roll_no, course_id) -> student_name split - the canonical interview example', 'State the formal definition first, then give the example'] },
  { topic: '3NF', title: 'Third Normal Form (3NF)', content: 'Third Normal Form = 2NF + no transitive dependency: no non-key attribute may depend on another non-key attribute. Formal definition: for every non-trivial FD X -> A, either X is a superkey or A is a prime attribute (part of some candidate key).\n\nExample: Student(roll_no, dept_id, dept_name, dept_building) - roll_no -> dept_id and dept_id -> dept_name, so dept_name transitively depends on roll_no. Fix: Student(roll_no, dept_id) plus Department(dept_id, dept_name, dept_building).\n\n3NF vs BCNF: 3NF permits the one case where a prime attribute is determined by a non-superkey (overlapping candidate keys). 3NF decomposition is always lossless and dependency-preserving; BCNF may sacrifice dependency preservation, which is why real designs often stop at 3NF when BCNF would split tables awkwardly.', keyPoints: ['3NF = 2NF + no transitive dependency (non-key -> non-key)', 'Formal test: X -> A requires X superkey or A prime', 'Removes anomalies from chained dependencies', '3NF decomposition is lossless and dependency-preserving', 'Weaker than BCNF: allows some redundancy with prime attributes'], commonMistakes: ['Fixing partial dependencies again and calling it 3NF - transitive ones are the target', 'Missing the "or A is prime" clause in the formal definition'], interviewTips: ['Use roll_no -> dept_id -> dept_name and show the two-table split', 'Quote the formal rule "X is a superkey or A is prime" for extra credit'] },
  { topic: 'BCNF', title: 'Boyce-Codd Normal Form (BCNF)', content: 'BCNF is the strict practical form of 3NF: for every non-trivial functional dependency X -> Y, X must be a superkey. It removes the residual anomaly 3NF allows when a non-superkey determines a prime attribute - typically with overlapping composite candidate keys.\n\nClassic example: Candidate(Student, Subject, Teacher) where a teacher teaches exactly one subject (Teacher -> Subject) and the keys are (Student, Teacher) and (Student, Subject). Teacher -> Subject violates BCNF (Teacher is not a superkey) though 3NF is satisfied because Subject is prime. Decompose into TeacherSubject(Teacher, Subject) and StudentTeacher(Student, Teacher).\n\nCost: BCNF decomposition is always lossless but may not preserve dependencies - a constraint that spanned the original table may now need a join to check, which is why some designs deliberately stay at 3NF.', keyPoints: ['Every determinant must be a superkey - stricter than 3NF', 'Differs from 3NF only with overlapping candidate keys', 'BCNF decomposition is lossless but can lose dependency preservation', 'Reached by decomposing on each violating FD', 'No 3NF-style prime-attribute exception'], commonMistakes: ['Claiming BCNF always preserves dependencies', 'Trying to normalise further a table whose only determinant is already a key - it is already in BCNF'], interviewTips: ['Use the (Student, Subject, Teacher) example where BCNF forces a split', 'Note that real designs sometimes stay at 3NF to keep constraints checkable'] },
  { topic: 'Transactions', title: 'Database Transactions', content: 'A transaction is a logical unit of work: one or more operations that must execute as a single all-or-nothing step (transfer 500 from A to B = debit A + credit B). Boundaries: BEGIN/START, COMMIT (make permanent) and ROLLBACK (undo to the start). SAVEPOINTs allow partial rollback inside a transaction.\n\nThe DBMS guarantees ACID for every transaction. States move: active -> partially committed (last statement done, buffers not yet flushed) -> committed (durable), or failed -> aborted (rolled back, optionally restarted).\n\nConcurrency means several transactions interleave; without control you get lost updates, dirty reads, unrepeatable reads and phantom reads. Isolation levels (READ UNCOMMITTED to SERIALIZABLE) define how much interleaving is tolerated.', keyPoints: ['Transaction = the smallest all-or-nothing unit of work', 'COMMIT makes changes permanent; ROLLBACK undoes them', 'SAVEPOINT enables partial rollback', 'States: active, partially committed, committed, failed, aborted', 'Bank transfer is the standard example: both legs succeed or both fail'], commonMistakes: ['Treating each SQL statement as the transaction unit without understanding autocommit', 'Saying rollback restores data from backups - it uses undo logs, not backups'], interviewTips: ['Explain with the bank transfer example and point at where debit/credit could fail', 'Know autocommit behaviour and explicit transaction syntax in your SQL dialect'] },
  { topic: 'ACID Properties', title: 'ACID Properties Explained', content: 'Atomicity: a transaction executes completely or not at all - managed by the undo log/recovery subsystem. Consistency: the database moves from one valid state to another; keys, constraints and triggers hold before and after (application logic must also cooperate). Isolation: concurrent transactions do not observe each other intermediate states; achieved by locking or MVCC, tunable via isolation levels. Durability: once committed, changes survive crashes; the redo log/WAL flushed to disk guarantees it.\n\nExample - transfer 500 from A to B: atomicity means no debit without credit; consistency means total money unchanged; isolation means nobody sees A debited but B not yet credited; durability means a power cut after COMMIT does not lose the transfer.\n\nRelated: BASE (Basically Available, Soft state, Eventually consistent) is the distributed/NoSQL counterpart when strict ACID across nodes is too costly.', keyPoints: ['A: all or nothing (undo log)', 'C: valid state to valid state (constraints plus app logic)', 'I: no interference between concurrent transactions (locks/MVCC)', 'D: committed data survives crashes (WAL/redo log)', 'BASE is the relaxed distributed alternative'], commonMistakes: ['Attributing consistency to the DBMS alone - constraints help, but application logic can still violate intent', 'Mixing up which subsystem delivers which property (undo vs redo logs)'], interviewTips: ['Narrate the bank-transfer example touching all four letters in order', 'Expect a follow-up on isolation levels and anomalies - know them cold'] },
  { topic: 'Transaction States', title: 'Transaction State Diagram', content: 'A transaction moves through: Active (executing statements, can read/write), Partially Committed (final statement executed, changes still in main-memory buffers), Committed (changes made durable - the only state from which effects are permanent), Failed (normal execution can no longer proceed - error, deadlock victim, crash), and Aborted (rolled back; the system may restart or kill it).\n\nKey transitions: Active -> Partially Committed on the last statement; Partially Committed -> Committed after the log force succeeds; Active or Partially Committed -> Failed on error; Failed -> Aborted always; Aborted may restart as Active.\n\nTricky points interviewers probe: a partially-committed transaction can still fail (its pages are not yet on disk); a transaction waiting for a lock is still active; and an aborted transaction must undo everything it touched.', keyPoints: ['Five states: active, partially committed, committed, failed, aborted', 'Committed = durable; partially committed is not yet safe', 'Failure before commit leads to a full undo', 'Aborted transaction may be restarted or killed', 'Waiting for a lock is still active, not failed'], commonMistakes: ['Claiming a committed transaction can be rolled back after a crash', 'Calling a lock-waiting transaction failed - it is active'], interviewTips: ['Draw the state diagram with arrows for commit, failure and restart', 'Explain what happens between partially committed and committed (log force)'] },
  { topic: 'Concurrency Control', title: 'Concurrency Control (Locks and MVCC)', content: 'Concurrency control lets many transactions run together while producing results identical to some serial order. Two families: locking (pessimistic) - shared locks for reads, exclusive for writes, and two-phase locking (a grow phase then a shrink phase) guarantees serializability; and MVCC/optimistic - each transaction works on snapshots, with conflicts detected at commit.\n\nProblems it prevents: lost update (two writes overwrite each other), dirty read (reading uncommitted data), unrepeatable read (a row changes between two reads in one transaction), phantom read (new matching rows appear). SQL isolation levels map to these: READ UNCOMMITTED allows dirty reads, READ COMMITTED stops them, REPEATABLE READ stops unrepeatable reads, SERIALIZABLE also stops phantoms.\n\nCosts: locking risks deadlock and throttles throughput; MVCC buys non-blocking reads at the price of version storage and write-write conflicts at commit.', keyPoints: ['Goal: interleaved execution equivalent to a serial one', '2PL (two-phase locking) guarantees conflict serializability', 'MVCC: readers never block writers, snapshot per transaction', 'Anomalies: lost update, dirty read, unrepeatable read, phantom read', 'Isolation levels trade consistency guarantees for concurrency'], commonMistakes: ['Saying serializability means serial execution - it means equivalent to a serial order', 'Forgetting that only SERIALIZABLE fully prevents phantoms in classic SQL'], interviewTips: ['Name the four anomalies and which isolation level fixes each', 'Mention both pessimistic and optimistic families before choosing one'] },
  { topic: 'Serializability', title: 'Serializability and the Precedence Graph', content: 'Serializability is the correctness criterion for concurrent schedules: an interleaved schedule is serializable if it is equivalent to some serial order of the same transactions. Conflict serializability (the practical test) looks at conflicting pairs - operations on the same data item where at least one is a write. Build a precedence (serialization) graph: one node per transaction, an edge Ti -> Tj whenever Ti performed a conflicting operation before Tj. The schedule is conflict-serializable iff the graph is acyclic; a topological order gives the equivalent serial schedule.\n\nView serializability is a weaker, more permissive equivalence - same reads-from relationships and same final writes - but is NP-hard to test, so real systems use conflict serializability.\n\nHow DBMSs achieve it: two-phase locking produces conflict-serializable schedules; serializable snapshot isolation (MVCC plus validation) approximates it optimistically.', keyPoints: ['Serializable = equivalent to some serial execution', 'Conflict: same item, at least one write, order matters', 'Precedence graph acyclic iff conflict-serializable', '2PL always produces conflict-serializable schedules', 'View serializability is broader but impractical to test'], commonMistakes: ['Confusing serial with serializable - serial is one-after-another; serializable allows interleaving', 'Drawing precedence graph edges in the wrong direction'], interviewTips: ['Practice building a precedence graph for a 2-3 transaction schedule quickly', 'State the equivalence conditions if asked about view serializability'] },
  { topic: 'Deadlocks', title: 'Deadlocks in DBMS: Prevention, Avoidance, Detection', content: 'A deadlock is a cycle of transactions each holding a lock the next one needs - nobody can proceed. The four Coffman conditions must all hold simultaneously: mutual exclusion, hold and wait, no preemption, circular wait. Break any one to prevent it.\n\nHandling strategies: Prevention - impose rules that make a cycle impossible (lock resources in a fixed global order, or acquire all locks up front). Avoidance - Banker-style runtime decisions using knowledge of future needs (rarely used in DBMSs). Detection and recovery - build a wait-for graph, detect cycles periodically, then abort a victim (smallest work done, fewest locks) and roll it back. Timeout - abort anything waiting too long. Most DBMSs detect and kill a victim, returning a deadlock error to the application.\n\nApplications should retry aborted transactions; keep transactions short and touch rows in a consistent order to make deadlocks rare.', keyPoints: ['Deadlock = cycle in the wait-for graph', 'Coffman conditions: mutual exclusion, hold-and-wait, no preemption, circular wait', 'Prevention: ordered locking or acquire-all-at-once', 'Most DBMSs: periodic detection + victim rollback + retry', 'Resource ordering is the usual application-level fix'], commonMistakes: ['Mixing up prevention (a rule makes cycles impossible) with avoidance (runtime decisions using future knowledge)', 'Forgetting victim selection and rollback during recovery'], interviewTips: ['Draw a two-transaction wait-for graph and show how each prevention rule breaks the cycle', 'Mention retry logic - interviewers like the practical angle'] },
  { topic: 'Indexing', title: 'Database Indexing in Depth', content: 'An index is an auxiliary data structure that turns full table scans into targeted lookups, like a book index. Typical structures: B+ tree (ordered, shallow, great for ranges) and hash (equality only). Clustered index: the rows themselves are stored in key order - one per table (usually the primary key in InnoDB). Non-clustered (secondary) index: a separate structure whose leaves point to rows (row id or primary key), so lookups may need a bookmark lookup into the table.\n\nTrade-offs: SELECTs with WHERE/JOIN/ORDER BY get dramatically faster; every INSERT/UPDATE/DELETE must also update each index, and indexes consume disk and cache. Composite indexes follow the leftmost-prefix rule: index (a, b, c) serves filters on a, a+b, a+b+c but not on b alone. Covering indexes (containing all queried columns) skip the table lookup entirely.\n\nGuidelines: index columns used in frequent filters, joins and sorts; avoid indexing low-cardinality or rarely used columns; watch out for functions on indexed columns in WHERE, which defeat the index.', keyPoints: ['B+ tree for ranges and ordering; hash for equality only', 'Clustered = data stored in key order (one per table); secondary = separate structure + pointer', 'Every write updates all indexes - the write penalty', 'Composite index: leftmost-prefix rule', 'Covering index can answer from the index alone'], commonMistakes: ['Adding indexes everywhere - writes and storage pay the price', 'Expecting LIKE %term to use an index - a leading wildcard prevents it', 'Applying a function to an indexed column in WHERE, defeating the index'], interviewTips: ['Explain when NOT to index - that is the senior-level part of the question', 'Walk through the leftmost-prefix rule with a concrete composite index'] },
  { topic: 'B-Tree', title: 'B-Tree Structure and Operations', content: 'A B-Tree of order m is a balanced multiway search tree: every node holds at most m children and at most m-1 keys in sorted order; internal nodes (except the root) keep at least ceil(m/2) children; all leaves sit at the same depth, so the tree stays short and wide and every lookup costs O(log n) - ideal when each node is one disk block.\n\nSearch descends one path comparing keys. Insert may split a full node on the way up; delete borrows from siblings or merges nodes to keep occupancy. In the classic variant, keys and record pointers live in ALL nodes - internal nodes contain routing keys plus data, so a key found high in the tree can end the search early.\n\nUsed in file systems and older DBMS indexes. Compared with a B+ Tree: a B-Tree can be slightly faster for a random single-key hit (found sooner), but range scans are poor because data is scattered across all levels and in-order traversal climbs up and down the tree.', keyPoints: ['Order m: at most m-1 keys and m children per node', 'Internal nodes keep at least ceil(m/2) children (except root)', 'All leaves at the same depth -> uniform O(log n) height', 'Keys and record pointers live in internal nodes too', 'Split on insert; borrow or merge on delete maintains balance'], commonMistakes: ['Reciting B+ Tree properties for a B-Tree question - the key difference is where data lives', 'Forgetting the minimum-occupancy rule that keeps nodes at least half full'], interviewTips: ['Contrast with B+ Trees in one sentence: data everywhere vs data only in leaves', 'Explain why short, fat trees suit disk-based storage'] },
  { topic: 'B+ Tree', title: 'B+ Tree: The Standard DBMS Index', content: 'The B+ Tree is the standard database index structure. All data pointers live in the leaves; internal nodes hold only routing keys. Leaves are linked left-to-right, so range scans (BETWEEN, ORDER BY, prefix LIKE) walk one linear list instead of climbing the tree. Height stays tiny - a 3-4 level B+ tree can index hundreds of millions of rows, so each lookup costs 3-4 page reads.\n\nEvery search traverses root to leaf at uniform cost. Inserts split full nodes; deletes fill via redistribution or merging; the minimum fill factor (typically half) keeps the tree dense. Because keys are duplicated in internal nodes as separators, the leaf level forms a complete sorted index of the table.\n\nA clustered (primary) B+ tree stores the actual rows at the leaves. A secondary B+ tree stores the primary key at its leaves and needs a second lookup into the clustered tree (bookmark lookup) unless the index covers all queried columns.', keyPoints: ['Data only in leaves; internal nodes are pure routers', 'Linked leaf list makes range and ordered scans efficient', 'Uniform root-to-leaf cost; height 3-4 even for huge tables', 'Half-full minimum keeps nodes dense after deletes', 'Secondary index leaves hold the primary key, then a bookmark lookup'], commonMistakes: ['Saying range queries are fast on a plain B-Tree - linked leaves are what make B+ Trees range-friendly', 'Forgetting why internal-node keys may repeat as routing separators'], interviewTips: ['Draw root -> internal -> linked leaves and explain a range query walk', 'Explain why databases prefer B+ Trees over B-Trees and hash indexes'] },
  { topic: 'Views', title: 'Views and Materialised Views', content: 'A view is a stored named query that behaves like a virtual table: SELECT from active_students reads fine, but no data is stored - the DBMS rewrites queries against the view to use base tables. Views provide simplicity (hide complex joins), security (expose only chosen columns/rows - e.g. a salary-free employee view), logical data independence (the base schema can evolve while views stay stable) and consistency for recurring reports.\n\nUpdatable views: simple single-table views without GROUP BY, DISTINCT or aggregates can accept INSERT/UPDATE/DELETE, which pass through to base tables. Complex views are read-only unless you add INSTEAD OF triggers.\n\nMaterialised views physically store the result and must be refreshed (eagerly or lazily) - they speed up expensive aggregates at the cost of staleness and storage. Regular views cost nothing to store but re-execute on every use.', keyPoints: ['Virtual table defined by a stored SELECT', 'Security: column/row-level restriction without new accounts', 'A shield of logical data independence for applications', 'Simple views are updatable; complex ones need INSTEAD OF triggers', 'Materialised view = stored result plus refresh strategy'], commonMistakes: ['Assuming views always improve performance - plain views re-run the query each time', 'Trying to update a view with joins or aggregates directly'], interviewTips: ['Contrast ordinary vs materialised views with a refresh-cost example', 'Use the hide-salary-columns case as the security argument'] },
  { topic: 'Database Security', title: 'Database Security: Auth, Authz, Encryption, Auditing', content: 'Database security controls who can do what to which data. Authentication verifies identity (passwords, certificates, integrated OS auth). Authorisation grants privileges: GRANT SELECT, UPDATE(salary) ON employees TO role - covering objects (tables, views, procedures) and actions. REVOKE removes them; roles bundle privileges for manageability. Views give row/column-level restriction for users who should see partial data.\n\nBeyond access control: encryption at rest (TDE) and in transit (TLS) protects data if disks or the network are compromised; auditing records who did what (audit logs); backups must be secured too. SQL injection is the classic application-layer attack - prevent it with parameterised queries and least-privilege DB accounts, never string concatenation.\n\nTheory: discretionary access control (owners grant access) vs mandatory access control (system-enforced labels such as secret/top-secret with Bell-LaPadula rules).', keyPoints: ['Authentication (who you are) vs authorisation (what you may do)', 'GRANT/REVOKE with roles; always apply least privilege', 'Views restrict column/row exposure cleanly', 'Encryption at rest and in transit; auditing for accountability', 'SQL injection: prevented with parameterised queries'], commonMistakes: ['Storing passwords unhashed - passwords need salted hashes (bcrypt/argon2), not encryption', 'Confusing DAC (owner-controlled grants) with MAC (system labels)'], interviewTips: ['Structure the answer as authentication -> authorisation -> encryption -> auditing', 'Give the parameterised-query answer immediately when injection comes up'] },
  { topic: 'Recovery', title: 'Crash Recovery, WAL and Checkpoints', content: 'Recovery restores a consistent database after crashes. The log is central: every change is recorded with before-images and after-images, and write-ahead logging (WAL) forces the log record to disk BEFORE the data page. With undo and redo information in the log, the DBMS can replay history after any crash.\n\nAfter a crash: REDO transactions that committed (their changes may sit in lost buffers) and UNDO transactions that never committed. ARIES-style recovery runs three passes: analysis (identify dirty pages and in-flight transactions), redo (repeat history from the last checkpoint), undo (roll back the losers). Checkpoints periodically flush dirty buffers and write a checkpoint record, bounding how much log must be scanned at restart.\n\nOther failures need other tools: disk loss -> backups plus archived logs (point-in-time recovery); media errors -> RAID/mirrors. Redo must be idempotent because a crash during recovery may repeat the replay.', keyPoints: ['WAL: log reaches disk before the data page', 'After crash: redo committed, undo uncommitted transactions', 'Checkpoints bound log scanning and recovery time', 'ARIES passes: analysis, redo (repeat history), undo', 'Backups plus archived logs give point-in-time recovery'], commonMistakes: ['Saying recovery restores from the last backup - logs do the fine-grained work', 'Confusing undo (rollback uncommitted work) with redo (replay committed work)'], interviewTips: ['Explain WAL and why the log write must precede the data write', 'Walk the three recovery passes in order - a favourite hard question'] },
  { topic: 'Joins', title: 'SQL Joins: Types, Semantics and Algorithms', content: 'A join combines rows of two tables on a related condition. INNER JOIN keeps only matching pairs. LEFT/RIGHT (OUTER) JOIN keep all rows of one side, filling the other side with NULLs. FULL OUTER JOIN keeps all rows of both. CROSS JOIN produces the cartesian product. SELF JOIN joins a table to itself (employee-manager hierarchies) using aliases.\n\nSemantics to master: putting a right-table filter in WHERE of a LEFT JOIN silently degrades it to an INNER JOIN - such filters belong in the ON clause. The ANTI JOIN pattern (LEFT JOIN ... WHERE right IS NULL) finds non-matches. NATURAL JOIN matches same-named columns and is avoided in production.\n\nExecution algorithms matter for performance: nested loop (good when one side is small or an index exists), hash join (big equi-joins), sort-merge join (sorted inputs, range joins). The optimiser picks based on size estimates and available indexes.', keyPoints: ['INNER vs LEFT/RIGHT/FULL OUTER: which side keeps unmatched rows', 'SELF JOIN with aliases for hierarchical data', 'Outer-table filters belong in ON, not WHERE, for LEFT JOINs', 'ANTI JOIN pattern: LEFT JOIN plus IS NULL', 'Engines choose nested loop, hash or sort-merge by size and indexes'], commonMistakes: ['Putting the right-table filter in the WHERE of a LEFT JOIN, accidentally making it an INNER JOIN', 'Assuming FULL OUTER JOIN exists everywhere - some engines emulate it with UNION of left and right'], interviewTips: ['Write a LEFT JOIN with an unmatched-rows example on the whiteboard', 'Explain which join algorithm the optimiser would pick and why'] },
  { topic: 'Relational Algebra', title: 'Relational Algebra and Query Optimisation', content: 'Relational algebra is the theoretical query language underlying SQL: operations take relations and return relations. Select (sigma) filters rows -> SQL WHERE; Project (pi) picks columns -> the SELECT list (and removes duplicates, unlike SQL); Rename (rho) renames relations or attributes. Set operations - union, intersection, difference - need union-compatible schemas; cartesian product pairs every row with every row.\n\nThe join family: theta join = selection over a cartesian product; equi-join uses equality predicates; natural join = equi-join plus projecting out duplicate columns. Division (R ÷ S) answers for-all queries - students enrolled in EVERY course.\n\nWhy it matters: the SQL optimiser rewrites queries as relational-algebra expression trees and applies equivalence rules (push selections down, combine projections) to shrink intermediate results. Reading a query plan is reading an algebra tree.', keyPoints: ['sigma = select rows, pi = project columns, rho = rename', 'Set operations need union-compatible schemas', 'theta/equi/natural joins; division for universal (all) queries', 'Algebra is set-based; SQL is bag-based (duplicates allowed)', 'Optimisers rewrite algebra trees: push selections down early'], commonMistakes: ['Forgetting that projection eliminates duplicates in algebra while plain SELECT keeps them', 'Misreading division - it returns tuples of R related to ALL tuples of S'], interviewTips: ['Translate a SQL query into algebra and show a push-down rewrite - a strong optimiser signal', 'Know the division trick; it appears in find-students-who-took-every-course questions'] },








];
