module.exports = [
  { topic: 'MERN Introduction', question: 'Why would you choose the MERN stack for a project, and what are its trade-offs?', answer: 'MERN uses one language (JavaScript) across client, server and database layer, which reduces context switching and lets a small team ship faster. React gives a component model for the UI, Express a minimal HTTP layer, and MongoDB a flexible document schema for evolving requirements. Trade-offs: MongoDB sacrifices strict relational integrity, Node is single-threaded for CPU work, and the flexibility can lead to inconsistent structure without discipline.', explanation: 'Name one concrete trade-off per layer to sound experienced, not promotional.', difficulty: 'medium', followUps: ['When would SQL be a better choice than MongoDB here?', 'How does the data flow from React to MongoDB and back?'] },
  { topic: 'HTML Basics', question: 'What is semantic HTML and why does it matter for accessibility and SEO?', answer: 'Semantic HTML uses elements that describe meaning, such as header, nav, main, article, section and footer, instead of generic div and span. Screen readers navigate by landmarks, so semantics improve accessibility; crawlers weight headings and landmarks for SEO; and browsers give built-in keyboard behaviour, for example with button versus a clickable div.', explanation: 'Tie semantics to concrete consumers: screen readers, crawlers and keyboard users.', difficulty: 'easy', followUps: ['What is the difference between section and article?', 'Why is alt text on images required?'] },
  { topic: 'CSS Basics', question: 'Explain the box model and how box-sizing changes layout calculations.', answer: 'Every element is a box of content, padding, border and margin. By default box-sizing is content-box, so width sets only the content and padding and border are added, which makes width arithmetic painful. With border-box the declared width includes padding and border, so columns fit grids predictably, which is why resets apply border-box globally.', explanation: 'The width arithmetic example is what interviewers listen for.', difficulty: 'easy', followUps: ['How do margins collapse?', 'Difference between block, inline and inline-block?'] },
  { topic: 'JavaScript Basics', question: 'What are the differences between var, let and const, and why does const not make objects immutable?', answer: 'var is function-scoped and hoisted with undefined initialisation, allowing redeclaration and accidental loop-variable leaks. let and const are block-scoped with a temporal dead zone before declaration, preventing use-before-define. const forbids rebinding the identifier, but the object it points to can still mutate, so immutability needs Object.freeze.', explanation: 'The temporal dead zone detail separates a strong answer from a memorised one.', difficulty: 'medium', followUps: ['What happens if you read a let variable before its declaration?', 'How does hoisting differ for function declarations?'] },
  { topic: 'ES6 Features', question: 'Which ES6+ features changed how you write React and Node code daily?', answer: 'Arrow functions keep lexical this for callbacks, template literals build strings, destructuring extracts props and array items, spread and rest copy or collect values, default parameters simplify APIs, and modules replaced require with import/export. Optional chaining and nullish coalescing later removed defensive checks.', explanation: 'Give one sentence per feature showing the problem it removed.', difficulty: 'medium', followUps: ['How does spread differ for arrays and objects?', 'When do rest parameters beat arguments?'] },
  { topic: 'JavaScript Async', question: 'Explain the event loop and the order in which callbacks, promises and timeouts run.', answer: 'The call stack runs synchronous code first. Microtasks (promise callbacks, queueMicrotask) drain completely after each macrotask, then macrotasks (setTimeout, I/O, UI events) run one at a time. So a resolved promise executes before an earlier-scheduled setTimeout(0), because microtask priority is higher.', explanation: 'The promise-before-timeout ordering is the classic interview test.', difficulty: 'hard', followUps: ['What runs first, process.nextTick or a promise callback?', 'How does await desugar into the microtask queue?'] },
  { topic: 'JavaScript DOM', question: 'How does event delegation work and why is it more efficient than binding per element?', answer: 'Events bubble up the DOM, so one listener on a stable ancestor can handle clicks for many descendants using event.target and closest() to find the origin. This avoids leaking listeners when lists rerender, keeps memory flat for thousands of rows and automatically covers dynamically added nodes.', explanation: 'Mention the memory and dynamic-node benefits explicitly.', difficulty: 'medium', followUps: ['Which events do not bubble?', 'How does the React synthetic event system relate to delegation?'] },
  { topic: 'React Introduction', question: 'What is the virtual DOM and how does reconciliation decide what to update?', answer: 'React keeps a lightweight in-memory tree of elements and, on state change, renders a new tree, diffs it against the previous one and applies the minimum DOM mutations. Reconciliation compares element type: same type updates props in place, different type unmounts and rebuilds the subtree, and the key prop tells React which list items persist.', explanation: 'Explaining type comparison and keys proves understanding beyond the buzzword.', difficulty: 'hard', followUps: ['Why do list keys need to be stable identifiers?', 'What is the cost of a render pass?'] },
  { topic: 'JSX', question: 'Is JSX required in React and what does it compile to?', answer: 'JSX is optional syntactic sugar. Babel transpiles it to jsx function calls that create element objects describing type, props and children, for example jsx("div", { children: "hi" }). Anything JSX does, createElement or jsx can do directly, which is why some teams use hyperscript helpers instead.', explanation: 'Showing the compiled output is the technical depth expected.', difficulty: 'medium', followUps: ['Why must JSX return one root element?', 'What rules do attribute names follow?'] },
  { topic: 'Components', question: 'How do you decide when to split a component, and how do you keep components testable?', answer: 'Split when a component handles more than one responsibility, when JSX nesting becomes hard to scan, when a fragment could be reused elsewhere, or when state and effects cluster into a separate concern. Keep components presentational where possible, push logic into custom hooks or pure functions, and pass data and handlers as props so the component renders in isolation with mocked props.', explanation: 'Single-responsibility plus hooks extraction is the practical heuristic.', difficulty: 'medium', followUps: ['Controlled versus uncontrolled components?', 'How do you test a component that fetches data?'] },
  { topic: 'Props', question: 'What is prop drilling and how do you avoid it without jumping straight to a state library?', answer: 'Prop drilling is passing data through intermediate components that never use it, only forward it. First try composition: pass the element itself as children or a render prop so the middle layer stays generic. Context then removes intermediate layers for genuinely global data such as theme or auth. Redux or Zustand is justified only when many distant components share frequently changing state.', explanation: 'Escalating in order composition, context, store shows architectural judgement.', difficulty: 'medium', followUps: ['What does memoizing context value prevent?', 'Why can context cause broad rerenders?'] },

  { topic: 'State and useState', question: 'Why is state updated asynchronously in React and how do you update based on the previous value?', answer: 'React batches multiple setState calls inside one event handler into a single rerender for performance, so reading state immediately after calling the setter returns the old value. When the next value depends on the current one, pass a function: setCount(c => c + 1) queues an updater that React applies in order, which is also safe under Strict Mode double-invocation.', explanation: 'The functional-updater form is the concrete fix interviewers want.', difficulty: 'medium', followUps: ['Why must state be replaced, not mutated?', 'What is the difference between state and a ref?'] },
  { topic: 'useEffect', question: 'How do you choose the dependency array of useEffect, and what problems do wrong dependencies cause?', answer: 'Include every reactive value read inside the effect: props, state and component functions. An empty array runs once after mount and suits subscriptions or analytics; missing dependencies cause stale closures where the effect keeps reading old values; over-broad dependencies with unstable references like inline objects or functions cause the effect to run every render, so memoize them or move the logic inside the effect.', explanation: 'Stale closure versus excessive reruns is the trade-off being tested.', difficulty: 'hard', followUps: ['How do you cancel a fetch inside useEffect?', 'When is useLayoutEffect the right choice?'] },
  { topic: 'React Hooks', question: 'What are the rules of hooks and why do they exist?', answer: 'Hooks must be called at the top level of a component or custom hook, never inside conditions, loops or nested functions, and only in React function components. React tracks hooks by call order in a list per component, so conditional calls would shift positions and pair state with the wrong hook, which is why the linter enforces the rules statically.', explanation: 'Explaining the call-order mechanism justifies the rules instead of reciting them.', difficulty: 'medium', followUps: ['How do you conditionally run an effect then?', 'What does a custom hook return?'] },
  { topic: 'React Router', question: 'How does client-side routing differ from server routing, and how do you handle params and nested routes?', answer: 'The server always serves the same HTML bundle and React Router renders the view matching the URL via the History API, avoiding full page reloads but requiring a catch-all server route for deep links. Dynamic segments such as /users/:id are read with useParams, nested routes render children through Outlet, and loaders can fetch data as navigation starts.', explanation: 'The deep-link catch-all route is the operational detail that shows real experience.', difficulty: 'medium', followUps: ['How do you protect a route for authenticated users?', 'What is the difference between navigate and Link?'] },
  { topic: 'Context API', question: 'When is Context the right tool and when does it hurt performance?', answer: 'Context suits low-frequency global values such as theme, locale or the current user. Every consumer rerenders when the context value changes, so putting fast-changing data like keystrokes or frequently updated lists in context rerenders the whole subtree on each change. Split contexts so unrelated consumers are isolated, memoize the provider value, or move hot data to component state or a store with selectors.', explanation: 'The rerender-scope analysis is the core of the answer.', difficulty: 'medium', followUps: ['How do you split one context into two?', 'Why does an inline object in the provider value cause rerenders?'] },
  { topic: 'React Performance', question: 'How do you diagnose and fix a slow React list page?', answer: 'Measure with React DevTools Profiler to find which components rerender and why. Fixes in order of impact: derive less, memoize expensive computations with useMemo, memoize stable callbacks with useCallback so memo-wrapped rows skip rerenders, virtualize long lists with react-window so only visible rows mount, paginate or infinite-scroll the data, and split the bundle with lazy and Suspense so the initial render is smaller.', explanation: 'Always start with measurement, then name the specific tool per cause.', difficulty: 'hard', followUps: ['When does React.memo not help?', 'How does key misuse degrade list performance?'] },
  { topic: 'Node.js Introduction', question: 'Is Node.js single-threaded, and how does it handle thousands of concurrent connections?', answer: 'JavaScript runs on one event-loop thread, but Node delegates blocking operations to the OS and a thread pool (libuv) for DNS, file I/O and crypto. Handlers register callbacks or await promises, so while one request waits for I/O the loop serves others. CPU-bound work is the real limitation, because it blocks the loop, so heavy computation belongs in worker threads or a separate service.', explanation: 'Naming libuv and the thread pool shows where concurrency actually lives.', difficulty: 'medium', followUps: ['What happens during a long synchronous loop in a request handler?', 'When do worker threads help?'] },
  { topic: 'Node Event Loop', question: 'Describe the phases of the Node event loop and where nextTick fits.', answer: 'Each iteration runs timers (expired setTimeout/interval), pending callbacks, poll (fetches new I/O events and runs their callbacks), check (setImmediate) and close callbacks. process.nextTick callbacks run immediately after the current operation, before any promise microtask or setImmediate, which lets you escape the current phase but can starve the loop if abused.', explanation: 'nextTick versus setImmediate ordering is the classic discriminator.', difficulty: 'hard', followUps: ['Why can setImmediate run before setTimeout(0)?', 'How do you detect event-loop blocking in production?'] },
  { topic: 'Node Modules and npm', question: 'Explain CommonJS versus ES modules and how npm resolves dependencies.', answer: 'CommonJS uses require and module.exports with synchronous loading and dynamic resolution; ES modules use import/export, load asynchronously and statically, enabling tree shaking. npm installs packages into node_modules following the nested-plus-dedup layout, records the resolved tree in package-lock.json for reproducibility, and semantic version ranges in package.json decide when lockfile updates occur.', explanation: 'Tie the lockfile to reproducible builds, which is what interviewers probe.', difficulty: 'medium', followUps: ['What is the difference between dependencies and devDependencies?', 'How does peerDependencies prevent duplication?'] },
// Part 3 - express, rest, mongo, auth, deployment
  { topic: 'Express Basics', question: 'What is middleware in Express and how does the request pipeline execute?', answer: 'Middleware is a function receiving request, response and next, executed in registration order. It can mutate req and res, end the response, or call next() to pass control. Errors are handled by four-argument middleware that Express routes to when next(err) is called, and the router, body parsers and your routes are all middleware in the same chain.', explanation: 'Describing the chain plus error middleware covers the whole model.', difficulty: 'medium', followUps: ['Where should the error middleware be registered?', 'What does express.json() actually do?'] },
  { topic: 'Express Routing', question: 'How do you structure routes in a growing Express app?', answer: 'Use express.Router per resource, mount with app.use("/api/users", usersRouter), keep handlers thin by delegating to controllers and services, and validate inputs with middleware like express-validator before hitting the controller. Apply common concerns such as auth or ownership checks at the router level with router.use, and order routes so specific paths are registered before parameterised ones.', explanation: 'Routers plus validation middleware is the maintainable structure they want to hear.', difficulty: 'medium', followUps: ['How do route params and query params differ?', 'How do you version an API?'] },
  { topic: 'Express Middleware', question: 'Write the flow of a request through auth middleware and explain where it can fail.', answer: 'The request hits helmet/cors, then the auth middleware verifies the JWT from the Authorization header, decodes the payload, loads the user and attaches req.user, or calls next(err) with 401/403. It fails when the token is missing, expired or signed with a different secret, when the user was deleted or role changed after issuance, and when secrets or clock skew are misconfigured across replicas.', explanation: 'Listing real-world failure modes shows production experience.', difficulty: 'hard', followUps: ['How do you revoke a JWT before expiry?', 'Where does rate limiting belong in the chain?'] },
  { topic: 'REST API Design', question: 'What makes a REST API good, and how do you handle pagination and errors consistently?', answer: 'Use nouns for resources, HTTP verbs for actions, correct status codes (200/201/204, 400/401/403/404/409/500), and statelessness. For pagination prefer cursor-based parameters returning meta with next cursor, since offset pagination degrades on large tables. Return a consistent error envelope with code, message and details, and document everything with OpenAPI so clients can be generated.', explanation: 'Cursor versus offset pagination is the detail that elevates the answer.', difficulty: 'medium', followUps: ['When is a PUT not idempotent in practice?', 'How do you version a breaking change?'] },
  { topic: 'MongoDB Basics', question: 'How do you decide between embedding documents and referencing collections in MongoDB?', answer: 'Embed when data is accessed together, has a bounded size and belongs to the parent, such as order line items, because one read fetches everything atomically. Reference when the child is large, shared, or unbounded, such as reviews for a product, and populate or aggregate on read. Model for your query patterns first; the rule of thumb is data that is accessed together should be stored together.', explanation: 'Bounded, accessed-together, atomicity are the three decision axes.', difficulty: 'medium', followUps: ['What is the 16MB document limit and when does it bite?', 'How do multi-document transactions change the picture?'] },
  { topic: 'Mongoose', question: 'What does Mongoose add on top of the MongoDB driver, and how do schemas and hooks work?', answer: 'Mongoose provides schema validation, types, defaults, indexes declared in the schema, middleware hooks such as pre-save for hashing passwords, virtuals for computed fields, population for references and query building. Hooks run around operations, so a pre-save hook can normalize data before persistence, and the schema doubles as documentation of the collections shape.', explanation: 'Hooks plus virtuals plus population covers the value-add concisely.', difficulty: 'medium', followUps: ['When would you bypass Mongoose for the raw driver?', 'How do you handle schema migrations in MongoDB?'] },
  { topic: 'MongoDB Indexing', question: 'How does an index speed up queries and what are the costs of over-indexing?', answer: 'An index is a B-tree keyed on the indexed fields, letting the planner seek instead of scanning every document; compound indexes serve equality-then-sort patterns and covering indexes satisfy queries from the index alone. Costs: every write updates all indexes, each consumes RAM and disk, and unused indexes mislead the planner, so audit with explain and remove dead ones.', explanation: 'Write-path cost plus explain-based auditing is the complete picture.', difficulty: 'hard', followUps: ['How does the ESR rule order compound index fields?', 'What does a COLLSCAN in explain output mean?'] },
  { topic: 'Authentication with JWT', question: 'Explain the JWT authentication flow and how you would store tokens securely in a MERN app.', answer: 'On login the server verifies credentials, signs a short-lived access token with claims like userId and role, and sets a long-lived refresh token in an httpOnly, Secure, SameSite cookie. The client sends the access token in the Authorization header; the server verifies the signature and expiry statelessly. Refresh rotation with a server-side denylist handles revocation, and storing the access token in localStorage is avoided because XSS can read it.', explanation: 'httpOnly refresh cookie plus short access token is the secure default.', difficulty: 'hard', followUps: ['How do refresh token rotation and reuse detection work?', 'Why is localStorage risky for tokens?'] },
  { topic: 'Error Handling in Express', question: 'How do you centralise error handling in an Express + Mongoose app?', answer: 'Wrap async handlers with a helper that forwards rejections to next, or use Express 5 auto-catch, then define one error middleware that maps known errors: Mongoose ValidationError to 400 with field details, CastError to 404, duplicate key 11000 to 409, JsonWebTokenError to 401, and everything else to 500 with logging. Also define a 404 handler for unmatched routes and never leak stack traces in production.', explanation: 'Mapping each driver error to a status code shows systematic thinking.', difficulty: 'hard', followUps: ['Why do unhandled async rejections crash older Express?', 'How do you log errors with request context?'] },
  { topic: 'MERN Deployment', question: 'Walk through deploying a MERN app end to end.', answer: 'Build the React app into static assets served by a CDN or the Node server; run Express behind a process manager (PM2) or a container platform; put Nginx or a load balancer in front for TLS and gzip; host MongoDB on Atlas with IP allowlisting and credentials in environment variables or a secret manager; wire CI to test, build and deploy on merge; enable health checks, structured logs and uptime alerts. Configure CORS to the real frontend origin and use separate config per environment.', explanation: 'Cover build, process, proxy, database, secrets, CI and observability in that order.', difficulty: 'hard', followUps: ['How do you run zero-downtime deploys?', 'Where do environment variables live in each host?'] },



  { topic: 'Redux Toolkit', question: 'Why does Redux Toolkit use Immer, and how do async thunks fit the data flow?', answer: 'Immer lets reducers write mutating-looking code while producing a new immutable state behind the scenes, removing spread boilerplate and accidental mutation bugs. For async work createAsyncThunk dispatches pending, fulfilled and rejected actions that reducers in extraReducers handle, so the flow stays unidirectional: dispatch, middleware runs the request, state transitions through the three phases.', explanation: 'Naming the three thunk lifecycle actions is the expected detail.', difficulty: 'hard', followUps: ['When is RTK Query better than manual thunks?', 'How does a memoized selector help performance?'] },

];