// System Design Notes Part 2 - distributed systems theory
module.exports = [
  { topic: 'CAP Theorem', title: 'CAP Theorem', content: 'CAP states that during a network partition a distributed system can guarantee only one of Consistency (every read sees the latest write), Availability (every request gets a response) or Partition tolerance (the system survives network splits). Partitions are unavoidable, so the real choice is C over A or A over C.', keyPoints: ['P is not optional; networks fail', 'CP systems (HBase, ZooKeeper) reject writes to stay consistent', 'AP systems (Cassandra, DynamoDB) serve possibly stale data', 'CA only exists on a single node'], interviewTips: ['Say "partition -> choose C or A", which is the precise statement', 'Give a per-operation example: product page vs payment'] },
  { topic: 'Consistency Models', title: 'Consistency Models', content: 'Consistency models define what reads observe. Strong (linearizable) consistency behaves like one machine; sequential consistency preserves program order per process; causal consistency guarantees causally related writes are seen in order; eventual consistency only promises replicas converge if writes stop.', keyPoints: ['Strong consistency costs latency and availability', 'Eventual consistency is the default for many NoSQL stores', 'Read-your-own-writes can be added via session affinity', 'Quorum reads/writes (W+R>N) tune the point in between'], interviewTips: ['Use the "friend posts a photo" example to explain eventual consistency', 'Mention quorum arithmetic when asked how to strengthen reads'] },
  { topic: 'SQL vs NoSQL', title: 'SQL vs NoSQL Databases', content: 'SQL databases (MySQL, PostgreSQL) offer schemas, ACID transactions, joins and mature tooling. NoSQL families are document (MongoDB), key-value (Redis, DynamoDB), wide-column (Cassandra) and graph (Neo4j), trading joins and strict consistency for flexible schemas and horizontal scale.', keyPoints: ['Relational: strong integrity, ad-hoc queries', 'Document: flexible schema, nested reads', 'Key-value: microsecond lookups, caching and sessions', 'Wide-column: huge write throughput, time-series'], interviewTips: ['Choose by access pattern and consistency need, not fashion', 'Note that modern SQL databases also scale via replicas and partitions'] },
  { topic: 'Message Queues', title: 'Message Queues and Event Streaming', content: 'Queues (RabbitMQ, SQS) decouple producers from consumers, buffer spikes and enable retries; a job is processed once and acknowledged. Event streams (Kafka) persist an ordered log that many consumers read independently, enabling replay and multiple derived views.', keyPoints: ['Queue: work distribution, at-least-once delivery', 'Stream: ordered, replayable event log with consumer groups', 'Consumers must be idempotent because duplicates happen', 'Dead-letter queues capture poison messages'], interviewTips: ['Say "queues delete after ack, logs keep for replay" to contrast them', 'Mention at-least-once plus idempotency as the standard combo'] },
  { topic: 'Microservices', title: 'Microservices Architecture', content: 'Microservices split an application into independently deployed services aligned with business capabilities, each owning its database. Benefits are independent scaling, deployment and tech choice; costs are network latency, distributed transactions, operational overhead and the need for mature DevOps.', keyPoints: ['One database per service avoids coupling', 'Communication: REST/gRPC sync, or events async', 'API gateway fronts the services for clients', 'Start modular monolith; split when pain is proven'], interviewTips: ['Admit the operational cost before listing benefits', 'Know the saga pattern for cross-service workflows'] },
  { topic: 'API Gateway', title: 'API Gateway Pattern', content: 'An API gateway is the single entry point that routes client requests to services and cross-cuts concerns: TLS termination, authentication, rate limiting, request shaping, response aggregation and caching. It hides internal topology so services can be split or merged freely.', keyPoints: ['Central place for auth, throttling and metrics', 'Can aggregate several service calls into one response', 'Adds a hop; must scale horizontally and be highly available', 'Alternatives: service mesh for east-west traffic'], interviewTips: ['Distinguish north-south (gateway) from east-west (mesh) traffic', 'Mention BFF (backend for frontend) as a specialised gateway'] },
  { topic: 'Rate Limiting', title: 'Rate Limiting Strategies', content: 'Rate limiting caps how many requests a client may make. Fixed window counts per period (simple, bursty at boundaries), sliding window log is exact but memory hungry, sliding window counter approximates well, and token bucket allows controlled bursts by refilling tokens at a steady rate.', keyPoints: ['Token bucket: burst-friendly, industry default', 'Limit by user, IP or API key; return 429 with Retry-After', 'Enforce at the gateway or edge for global accuracy', 'Redis with atomic INCR/EXPIRE is the common store'], interviewTips: ['Explain why fixed window allows a 2x burst at the boundary', 'Mention distributed limiting needs a shared counter store'] },
  { topic: 'Horizontal vs Vertical Scaling', title: 'Horizontal vs Vertical Scaling', content: 'Vertical scaling buys a bigger machine (more CPU/RAM) and is simple but hits hardware ceilings and a single point of failure. Horizontal scaling adds more machines, giving near-linear growth and fault tolerance, at the price of load balancing, statelessness and data partitioning complexity.', keyPoints: ['Vertical: quick win, hard ceiling, no code change', 'Horizontal: needs stateless services and shared state outside', 'Sessions move to Redis; files move to object storage', 'Databases scale horizontally via replicas and sharding'], interviewTips: ['Say "make the app stateless first, then scale out"', 'Know which layer usually needs sharding: the database'] },
  { topic: 'Latency vs Throughput', title: 'Latency vs Throughput', content: 'Latency is the time one request takes; throughput is requests per second the system handles. They trade off: batching raises throughput but adds per-request wait, while adding parallelism hides latency but can saturate shared resources. Tail latency (p99) matters more than the average for user experience.', keyPoints: ['p50, p95, p99 percentiles describe real experience', 'Queuing theory: utilisation near 100% explodes latency', 'Caching lowers both', 'Backpressure protects throughput under overload'], interviewTips: ['Always quote percentiles, never averages, for latency', 'Use the supermarket checkout analogy for batching trade-offs'] },
];